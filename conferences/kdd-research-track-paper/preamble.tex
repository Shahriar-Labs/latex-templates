% Packages, colours and commands for this template, on top of what acmart already loads.
%
% acmart sets its own fonts, page geometry, section headings, captions and hyperlinks, and
% the ACM format depends on them, so this file adds nothing that changes those. In
% particular: no geometry, no font packages, no titlesec, no caption settings, and no
% amssymb (acmart's math fonts already define its symbols, and loading it stops the build).

\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\definecolor{kddblue}{RGB}{22,54,92}
\definecolor{kddgold}{RGB}{188,141,26}
\definecolor{kddgray}{RGB}{95,95,100}

\newcommand{\sysname}{\textsc{Meridian}}
