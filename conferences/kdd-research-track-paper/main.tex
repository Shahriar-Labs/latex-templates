\begin{filecontents*}{refs.bib}
@inproceedings{oyelaran2023heterogeneous,
  author    = {Oyelaran, Femi and Castellano, Marta},
  title     = {Heterogeneous Graph Attention Networks for Large-Scale Recommendation},
  booktitle = {Proceedings of the ACM SIGKDD Conference on Knowledge Discovery and Data Mining},
  year      = {2023},
  pages     = {112--122}
}

@inproceedings{brandt2022temporal,
  author    = {Brandt, Solveig and Kowalczyk, Aurek},
  title     = {Temporal Graph Attention for Dynamic Link Prediction},
  booktitle = {Proceedings of the Web Conference (WWW)},
  year      = {2022},
  pages     = {2044--2054}
}

@article{nakashima2021survey,
  author  = {Nakashima, Hiro and Ferreira, Bianca},
  title   = {A Survey of Representation Learning on Dynamic Graphs},
  journal = {IEEE Transactions on Knowledge and Data Engineering},
  volume  = {35},
  number  = {4},
  pages   = {3301--3320},
  year    = {2021}
}

@inproceedings{alavi2020contrastive,
  author    = {Alavi, Parisa and Dumont, Etienne},
  title     = {Contrastive Self-Supervised Learning on Graphs},
  booktitle = {Advances in Neural Information Processing Systems (NeurIPS)},
  year      = {2020},
  pages     = {5812--5823}
}

@inproceedings{tesfaye2019han,
  author    = {Tesfaye, Nardos and Whitfield, Grant},
  title     = {Heterogeneous Attention Networks for Node Classification},
  booktitle = {Proceedings of the Web Conference (WWW)},
  year      = {2019},
  pages     = {2022--2032}
}

@inproceedings{lindqvist2018graphsage,
  author    = {Lindqvist, Elin and Ohara, Kenji},
  title     = {Inductive Representation Learning on Large Graphs},
  booktitle = {Advances in Neural Information Processing Systems (NeurIPS)},
  year      = {2017},
  pages     = {1024--1034}
}

@inproceedings{marceau2021dysat,
  author    = {Marceau, Louis and Abubakar, Zainab},
  title     = {Dynamic Self-Attention for Temporal Graph Embedding},
  booktitle = {Proceedings of the ACM International Conference on Web Search and Data Mining (WSDM)},
  year      = {2021},
  pages     = {519--527}
}

@article{petrosyan2022scalable,
  author  = {Petrosyan, Ani and Deveaux, Marion},
  title   = {Scalable Sampling Strategies for Billion-Edge Graph Training},
  journal = {Proceedings of the VLDB Endowment},
  volume  = {15},
  number  = {6},
  pages   = {1201--1213},
  year    = {2022}
}

@inproceedings{husejnovic2020fraud,
  author    = {Husejnovic, Amar and Delacroix, Noemie},
  title     = {Graph-Based Fraud Detection at Industrial Scale},
  booktitle = {Proceedings of the ACM SIGKDD Conference on Knowledge Discovery and Data Mining},
  year      = {2020},
  pages     = {2755--2765}
}

@inproceedings{yamazaki2021simclr,
  author    = {Yamazaki, Sora and Bergstrom, Elias},
  title     = {A Simple Framework for Contrastive Representation Learning},
  booktitle = {Proceedings of the International Conference on Machine Learning (ICML)},
  year      = {2020},
  pages     = {1597--1607}
}

@inproceedings{quintero2023hyperbolic,
  author    = {Quintero, Valentina and Sandberg, Filip},
  title     = {Hyperbolic Embeddings for Hierarchical Heterogeneous Graphs},
  booktitle = {Proceedings of the IEEE International Conference on Data Mining (ICDM)},
  year      = {2023},
  pages     = {611--620}
}

@article{osei2022benchmark,
  author  = {Osei, Kwabena and Lindgren, Maja},
  title   = {Benchmarking Temporal Graph Neural Networks: A Reproducibility Study},
  journal = {ACM Transactions on Knowledge Discovery from Data},
  volume  = {16},
  number  = {5},
  pages   = {1--29},
  year    = {2022}
}
\end{filecontents*}

\documentclass[10pt,twocolumn]{article}

% ---------- Geometry ----------
\usepackage[letterpaper,top=0.85in,bottom=1in,left=0.75in,right=0.75in,columnsep=0.28in]{geometry}

% ---------- Fonts & math ----------
\usepackage{mathptmx}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}

% ---------- Color ----------
\usepackage[table]{xcolor}
\definecolor{kddblue}{RGB}{22,54,92}
\definecolor{kddgold}{RGB}{188,141,26}
\definecolor{kddgray}{RGB}{95,95,100}
\definecolor{kddbg}{RGB}{244,247,251}

% ---------- Layout helpers ----------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{caption}
\captionsetup{font=small,labelfont={bf,color=kddblue},skip=6pt}
\usepackage{enumitem}
\setlist[itemize]{leftmargin=*,itemsep=1pt,topsep=2pt}
\setlist[enumerate]{leftmargin=*,itemsep=1pt,topsep=2pt}

% ---------- Section titles ----------
\usepackage{titlesec}
\titleformat{\section}{\normalfont\large\bfseries\color{kddblue}}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries\color{kddblue}}{\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{10pt}{5pt}
\titlespacing*{\subsection}{0pt}{7pt}{3pt}

% ---------- Figures ----------
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ---------- Links ----------
\usepackage[colorlinks=true,linkcolor=kddblue,citecolor=kddblue,urlcolor=kddblue]{hyperref}

\newcommand{\sysname}{\textsc{Meridian}}

\begin{document}

\twocolumn[
{\centering
{\LARGE\bfseries\color{kddblue} \sysname: Scalable Contrastive Representation Learning\\ for Heterogeneous Temporal Graphs\par}
\vspace{0.7em}
{\large
Priya Anand$^{1}$ \quad Tobias Renner$^{2}$ \quad Chidi Okonkwo$^{3}$\par}
\vspace{0.35em}
{\normalsize
$^{1}$Nexa Institute for Data Science \quad
$^{2}$Synapse Research Labs \quad
$^{3}$Vertex University, Dept.\ of Computer Science\par}
\vspace{0.15em}
{\small\ttfamily\color{kddgray}
p.anand@nexa-institute.example \quad
t.renner@synapse-labs.example \quad
c.okonkwo@vertex-university.example\par}
\vspace{1.0em}
}

\par\noindent
{\setlength{\fboxrule}{0.8pt}\setlength{\fboxsep}{6pt}%
\fcolorbox{kddblue}{kddbg}{\begin{minipage}{\dimexpr\textwidth-2\fboxsep-2\fboxrule\relax}
\small\textbf{Unofficial template.} This document is an independent, community-produced
\LaTeX{} template intended to approximate the visual style of a KDD Research Track
submission. It is \textbf{not affiliated with, endorsed by, or produced by} ACM SIGKDD
or the KDD Organizing Committee. Author names, affiliations, results, and citations
below are fictional and provided for layout demonstration only. Before submitting,
authors \textbf{must} obtain the official call for papers and the current year's
formatting files from the SIGKDD website, as page limits, required class files, and
formatting rules change from year to year.
\end{minipage}}}
\par\vspace{10pt}\noindent
{\setlength{\fboxrule}{0.6pt}\setlength{\fboxsep}{7pt}%
\fcolorbox{kddgray}{white}{\begin{minipage}{\dimexpr\textwidth-2\fboxsep-2\fboxrule\relax}
\small\textbf{\color{kddblue}Abstract.}\ \,
Modeling graphs that are simultaneously heterogeneous in node and edge type and
dynamic in time remains difficult: most encoders specialize in one axis of
variation and degrade on the other. We present \sysname{}, a contrastive
representation learning framework that jointly encodes type heterogeneity and
temporal dynamics through a type-aware attention module coupled with a
time-decayed neighborhood sampler. \sysname{} is trained with an
in-batch contrastive objective that treats temporally adjacent interactions of
the same node as positive pairs, requiring no labeled supervision at
pretraining time. Across three benchmark graphs spanning e-commerce, social,
and citation domains, \sysname{} improves link-prediction AUC-ROC by 2.1--4.6
points over the strongest baseline while using 31\% fewer parameters. We
release ablations isolating the contribution of type-awareness versus
temporal decay, and discuss failure modes on sparsely connected node types.
\end{minipage}}}
\par\vspace{14pt}
]

\section{Introduction}
Graphs that mix multiple node and edge types while also evolving over time are
now the default, not the exception, in industrial data mining: a marketplace
graph links \emph{users}, \emph{items}, and \emph{sellers} through
\emph{purchase}, \emph{view}, and \emph{return} edges that all carry
timestamps; a citation graph links \emph{papers}, \emph{authors}, and
\emph{venues} through relations that accumulate over decades. Two lines of
prior work address pieces of this problem. Heterogeneous graph neural
networks~\cite{tesfaye2019han,oyelaran2023heterogeneous} model type structure
well but typically collapse the temporal axis into a single static snapshot.
Temporal graph networks~\cite{brandt2022temporal,marceau2021dysat} capture
dynamics faithfully but assume a homogeneous node/edge type, or treat type as
a low-cardinality categorical feature bolted onto an otherwise
type-agnostic encoder. In practice this means practitioners either discard
timestamps or discard type, and both discards cost accuracy.

We introduce \sysname{}, an encoder-plus-objective pair designed to avoid
this trade-off. The encoder applies type-specific projection heads before a
shared attention layer, so that messages from a \emph{seller} node are never
averaged with messages from an \emph{item} node using the same weights. The
sampler that feeds this encoder applies an exponential time-decay to
candidate neighbors, so that a purchase from three years ago contributes
less to a node's representation than one from three hours ago without
discarding it outright. Training uses a self-supervised contrastive
objective (Equation~\ref{eq:contrastive}) rather than a downstream label,
which lets the same pretrained encoder transfer across link prediction,
node classification, and anomaly-scoring heads.

Our contributions are:
\begin{itemize}
  \item A type-aware, time-decayed encoder that unifies heterogeneous and
        temporal graph representation learning under one architecture
        (Section~\ref{sec:method}).
  \item A contrastive pretraining objective requiring no labels, using
        temporally local interactions as positive pairs.
  \item An empirical study on three benchmark graphs showing consistent
        gains over five fictionalized-for-this-template baselines, plus
        ablations isolating each architectural component
        (Sections~\ref{sec:experiments}--\ref{sec:results}).
\end{itemize}

\section{Related Work}
\label{sec:related}
\textbf{Heterogeneous graph learning.} Type-aware message passing was
popularized by attention-based encoders that learn a distinct transformation
per node/edge type~\cite{tesfaye2019han}, later scaled to
billion-edge industrial graphs via type-partitioned
sampling~\cite{oyelaran2023heterogeneous}. These methods assume a fixed
snapshot and do not model recency.

\textbf{Temporal graph learning.} Sequence- and attention-based temporal
encoders~\cite{brandt2022temporal,marceau2021dysat} model how a node's
representation drifts as new edges arrive, typically through a memory
module or a sliding attention window over recent interactions. A survey by
Nakashima and Ferreira~\cite{nakashima2021survey} catalogs over forty
variants but notes that fewer than a fifth handle more than one node type.

\textbf{Contrastive and self-supervised objectives.} Contrastive
pretraining, first popularized for images~\cite{yamazaki2021simclr}, was
adapted to graphs by treating augmented views of the same subgraph as
positive pairs~\cite{alavi2020contrastive}. \sysname{} departs from
augmentation-based positives, instead using temporally adjacent real
interactions, which avoids hand-tuned augmentation policies.

\textbf{Scalability and evaluation.} Petrosyan and
Deveaux~\cite{petrosyan2022scalable} study sampling strategies for
billion-edge training, and Husejnovic and
Delacroix~\cite{husejnovic2020fraud} apply graph encoders to industrial
fraud detection, both motivating our efficiency-focused design. Osei and
Lindgren~\cite{osei2022benchmark} highlight reproducibility gaps in
temporal-graph benchmarks, which informed our choice to release full
hyperparameters in Section~\ref{sec:experiments}.

\section{Method}
\label{sec:method}
\subsection{Problem Formulation}
Let $G = (V, E, \phi, \psi, \tau)$ be a heterogeneous temporal graph, where
$\phi: V \to \mathcal{A}$ maps nodes to a set of types $\mathcal{A}$,
$\psi: E \to \mathcal{R}$ maps edges to relation types $\mathcal{R}$, and
$\tau: E \to \mathbb{R}$ assigns each edge a timestamp. Given a query node
$v$ at reference time $t$, \sysname{} produces an embedding
$z_v^t \in \mathbb{R}^d$ by aggregating over the time-decayed neighborhood
$N_t(v) = \{u : (u,v) \in E,\ \tau(u,v) \le t\}$.

\subsection{Type-Aware Encoder}
Each node type $a \in \mathcal{A}$ owns a projection matrix
$W_a \in \mathbb{R}^{d \times d_{\text{in}}}$. A neighbor $u$ of type
$\phi(u)$ contributes a message $m_u = W_{\phi(u)} x_u$, where $x_u$ is its
input feature vector. Messages are combined with a shared multi-head
attention layer so that type-specific projections stay disjoint while the
attention weights are learned jointly across types, letting the model
transfer attention patterns learned on data-rich types (e.g.\ \emph{item})
to data-sparse ones (e.g.\ \emph{seller}).

\subsection{Time-Decayed Sampling}
Neighbors are drawn with sampling probability
$p(u \mid v, t) \propto \exp\!\big(-\lambda\,(t - \tau(u,v))\big)$,
where $\lambda$ controls how quickly influence decays; we set $\lambda$
per-dataset via the validation split (Section~\ref{sec:experiments}). This
keeps the neighborhood size bounded — Figure~\ref{fig:arch} sketches the
resulting pipeline from raw edges to the final embedding.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
  node distance=6mm and 6mm,
  box/.style={draw=kddblue,fill=kddblue!8,rounded corners=2pt,
              minimum height=8mm,minimum width=27mm,align=center,
              font=\footnotesize},
  arr/.style={-{Stealth[length=2mm]},kddblue,thick}
]
\node[box] (input) {Heterogeneous\\temporal edges};
\node[box, below=of input] (sample) {Time-decayed\\neighbor sampler};
\node[box, below=of sample] (encode) {Type-aware\\attention encoder};
\node[box, below=of encode, fill=kddgold!15, draw=kddgold] (embed) {Embedding $z_v^t$};
\draw[arr] (input) -- (sample);
\draw[arr] (sample) -- (encode);
\draw[arr] (encode) -- (embed);
\end{tikzpicture}
\caption{\sysname{} pipeline: edges are sampled with exponential time decay,
routed through type-specific projections, then fused by shared attention.}
\label{fig:arch}
\end{figure}

\subsection{Contrastive Objective}
For an anchor interaction between node $i$ and a temporally adjacent
partner $i^+$, and a batch of $N$ negatives drawn uniformly from the same
type, we minimize the InfoNCE-style loss
\begin{equation}
\mathcal{L}_{\text{con}} = -\log
\frac{\exp\!\big(\mathrm{sim}(z_i, z_{i^+}) / \tau_s\big)}
     {\sum_{j=1}^{N} \exp\!\big(\mathrm{sim}(z_i, z_j) / \tau_s\big)},
\label{eq:contrastive}
\end{equation}
where $\mathrm{sim}(\cdot,\cdot)$ is cosine similarity and $\tau_s$ is a
learned temperature, initialized at $\tau_s = 0.07$. Sampling positives from
real temporal adjacency, rather than synthetic augmentation, means the
objective directly reflects the recency structure the downstream tasks care
about. Encoding a node's neighborhood costs $O(k \log k)$ for a sampled
neighborhood of size $k$, dominated by the decay-weighted sort; end-to-end
training on the largest benchmark graph runs in $O(|E| \log k)$.

\section{Experiments}
\label{sec:experiments}
\subsection{Datasets}
We evaluate on three benchmark graphs constructed for this study:
\textsc{NexaCommerce} (4.8M nodes across \emph{user}/\emph{item}/\emph{seller}
types, 62M timestamped edges), \textsc{SynapseSocial} (2.1M \emph{user}
nodes, 5 interaction-relation types, 38M edges), and
\textsc{VertexCitation} (1.3M \emph{paper}/\emph{author}/\emph{venue} nodes,
14M edges spanning 25 years). Each dataset is split temporally: the final
10\% of edges by timestamp form the test set, the preceding 10\% form
validation, ensuring no future leakage into training.

\subsection{Baselines and Setup}
We compare against four representative baselines re-implemented under a
shared training budget: \textsc{GraphPulse} (homogeneous inductive
aggregation), \textsc{HetEmbed} (static heterogeneous attention),
\textsc{TempoGAT} (temporal attention, type-agnostic), and
\textsc{NodeFlow} (memory-based temporal encoder). All models use embedding
dimension $d=128$, are trained with Adam at learning rate $3\times10^{-4}$
for 40 epochs on a single accelerator, and are evaluated on link prediction
(AUC-ROC) with an 80/10/10 temporal split. Hyperparameters were selected on
the validation split only.

\section{Results}
\label{sec:results}
Table~\ref{tab:results} reports link-prediction AUC-ROC on all three
datasets, along with parameter count in millions. \sysname{} is the best
performer on every dataset and uses fewer parameters than three of the four
baselines, since type-specific projections are lower rank than the shared
wide layers those baselines rely on.

\begin{table}[t]
\centering
\small
\caption{Link-prediction AUC-ROC (\%) and parameter count.
Best result per column in \textbf{bold}.}
\label{tab:results}
\begin{tabular}{lcccc}
\toprule
\rowcolor{kddblue!10}
Method & Nexa & Synapse & Vertex & Params (M) \\
\midrule
GraphPulse & 81.4 & 78.9 & 83.2 & 4.1 \\
HetEmbed   & 84.7 & 80.1 & 85.6 & 6.8 \\
TempoGAT   & 86.2 & 82.4 & 84.9 & 5.9 \\
NodeFlow   & 87.0 & 83.6 & 86.7 & 7.4 \\
\midrule
\textbf{\sysname{} (ours)} & \textbf{90.1} & \textbf{87.0} & \textbf{89.8} & \textbf{4.7} \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:results} shows how AUC-ROC on \textsc{SynapseSocial}
responds to embedding dimension $d$. \sysname{} reaches within 0.4 points
of its peak accuracy at $d=64$, while \textsc{NodeFlow} requires $d=256$ to
approach a comparable score, consistent with our parameter-efficiency claim
in Table~\ref{tab:results}.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\columnwidth, height=4.4cm,
  xlabel={Embedding dimension $d$},
  ylabel={AUC-ROC (\%)},
  xtick={16,32,64,128,256},
  ymin=74, ymax=90,
  legend style={font=\scriptsize,at={(0.98,0.05)},anchor=south east,draw=kddgray},
  label style={font=\small},
  tick label style={font=\scriptsize},
  grid=major, grid style={gray!20},
  every axis plot/.append style={thick,mark size=1.6pt},
]
\addplot[color=kddblue,mark=*] coordinates
  {(16,82.1) (32,86.0) (64,88.9) (128,89.7) (256,90.1)};
\addlegendentry{\sysname{}}
\addplot[color=kddgold,mark=square*] coordinates
  {(16,76.5) (32,79.8) (64,82.0) (128,83.6) (256,84.4)};
\addlegendentry{NodeFlow}
\addplot[color=kddgray,mark=triangle*] coordinates
  {(16,75.2) (32,78.1) (64,80.3) (128,82.4) (256,83.1)};
\addlegendentry{TempoGAT}
\end{axis}
\end{tikzpicture}
\caption{AUC-ROC vs.\ embedding dimension on \textsc{SynapseSocial}.
\sysname{} saturates at lower dimension than either baseline.}
\label{fig:results}
\end{figure}

\section{Discussion}
\textbf{Ablations.} Removing type-aware projections (sharing $W$ across all
types) drops \textsc{NexaCommerce} AUC-ROC from 90.1 to 85.9, roughly half
the total gain over \textsc{NodeFlow}. Removing time-decay sampling in favor
of uniform sampling drops it to 87.3, confirming both components contribute
independently rather than one subsuming the other.

\textbf{Limitations.} Performance gains are smallest on node types with
fewer than 50 observed interactions (e.g.\ newly onboarded sellers), where
the type-specific projection has little data to specialize on; a
shrinkage prior toward the shared representation may help and is left to
future work. \sysname{} also assumes edge timestamps are reasonably
accurate — datasets with coarse, batched timestamps (e.g.\ daily rollups)
see the time-decay sampler degrade toward uniform sampling, eroding its
advantage over \textsc{TempoGAT}.

\textbf{Threats to validity.} All three benchmark graphs were constructed
for this study rather than drawn from an external, independently
maintained suite, so absolute numbers should not be compared across papers
without re-running baselines under identical splits, in the spirit of the
reproducibility concerns raised by Osei and
Lindgren~\cite{osei2022benchmark}.

\section{Conclusion}
We presented \sysname{}, a contrastive representation learning framework
that jointly models type heterogeneity and temporal dynamics through a
type-aware encoder and a time-decayed neighborhood sampler. Across three
benchmark graphs, \sysname{} improves link-prediction AUC-ROC over four
strong baselines while using fewer parameters, and ablations confirm that
both architectural components contribute independently. Future work
includes extending the shrinkage behavior of sparse node types and testing
robustness to coarse-grained timestamps.

\bibliographystyle{plain}
\bibliography{refs}

\end{document}
