\documentclass[10pt,twocolumn]{article}
\usepackage[letterpaper, left=0.75in, right=0.75in, top=1.0in, bottom=1.0in]{geometry}
\setlength{\columnsep}{0.25in}

% Typography and Language
\usepackage{times}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{microtype}

% Mathematics
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsfonts}
\usepackage{amsthm}

% Tables and Figures
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{float}

% TikZ and PGFPlots
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{positioning, calc, shapes.geometric}

% Lists and boxes
\usepackage{enumitem}
\usepackage{tcolorbox}
\tcbuselibrary{skins}

% Filecontents for Bibliography
\usepackage{filecontents}

% Color Palette
\usepackage{xcolor}
\definecolor{primarycolor}{RGB}{26,54,93}
\definecolor{secondarycolor}{RGB}{74,85,104}
\definecolor{accentcolor}{RGB}{155,44,44}
\definecolor{bglight}{RGB}{247,250,252}

% Hyperlinks
\usepackage[colorlinks=true,linkcolor=primarycolor,citecolor=primarycolor,urlcolor=primarycolor]{hyperref}

% Document Info
\title{}
\author{}
\date{}

\begin{filecontents}{\jobname.bib}
@article{vance2024decentralized,
  author    = {Liam Vance and Sophia Chen},
  title     = {Decentralized Self-Supervised Learning on Graph Latent Spaces},
  journal   = {Journal of Vertex Machine Learning},
  volume    = {42},
  number    = {3},
  pages     = {201--215},
  year      = {2024},
  publisher = {Nexa Publishing}
}
@inproceedings{aurelius2023contrastive,
  author    = {Marcus Aurelius and Yifan Zhang and Amit Patil},
  title     = {Contrastive Graph Representations without Centralized Anchors},
  booktitle = {Proceedings of the Synapse Conference on Neural Systems},
  pages     = {567--575},
  year      = {2023}
}
@article{nimbus2025large,
  author    = {Clara Nimbus and Jonathan Apex},
  title     = {Scale-Free Graph Autoencoders for Federated Topology Estimation},
  journal   = {Meridian Transactions on Signal Processing},
  volume    = {18},
  number    = {2},
  pages     = {89--104},
  year      = {2025}
}
@inproceedings{meridian2022privacy,
  author    = {H. R. Meridian and Devendra Patel},
  title     = {Privacy-Preserving Consensus in Latent Graph Embeddings},
  booktitle = {International Conference on Distributed Representation Learning},
  pages     = {1021--1030},
  year      = {2022}
}
@article{vertex2024robust,
  author    = {Katherine Vertex and Alexander Synapse},
  title     = {Robust Decentralized Optimization for Non-Convex Latent Graphs},
  journal   = {Nimbus Journal of Optimization},
  volume    = {33},
  number    = {1},
  pages     = {45--62},
  year      = {2024}
}
@inproceedings{chen2023unsupervised,
  author    = {Sophia Chen and Liam Vance},
  title     = {Unsupervised Partitioning of Latent Spaces in Federated Networks},
  booktitle = {Conference on Apex Learning Systems},
  pages     = {120--129},
  year      = {2023}
}
@article{patil2025communication,
  author    = {Amit Patil and Marcus Aurelius},
  title     = {Communication-Efficient Consensus over Distributed Latent Topology},
  journal   = {Journal of Synapse Systems},
  volume    = {8},
  number    = {4},
  pages     = {312--328},
  year      = {2025}
}
@inproceedings{nexa2024federated,
  author    = {Research Group Nexa and Collaborative Lab Meridian},
  title     = {Benchmarking Decentralized Representation Learning on Real-world Networks},
  booktitle = {Proceedings of Vertex Computing Systems},
  pages     = {2045--2056},
  year      = {2024}
}
\end{filecontents}

\begin{document}

\twocolumn[{
  \begin{center}
    % Unofficial Template Banner
    \begin{tcolorbox}[colback=bglight,colframe=primarycolor,arc=1mm,boxrule=1pt,top=3mm,bottom=3mm,left=4mm,right=4mm,width=\textwidth]
      \centering\small\sffamily\textcolor{primarycolor}{\textbf{UNOFFICIAL SUBMISSION TEMPLATE}} \\
      \vspace{1mm}
      \textcolor{secondarycolor}{This is an unofficial LaTeX template for preparing ICLR submission papers. It is \textbf{not} affiliated with, endorsed by, or sponsored by the International Conference on Learning Representations (ICLR) or its organizers. Authors must check the official call for papers for the current year's style files and submission guidelines.}
    \end{tcolorbox}
    \vspace{0.6cm}
    {\LARGE\bfseries Meridian-Net: Decentralized Self-Supervised\\ Representation Learning on Latent Graphs \par}
    \vspace{0.6cm}
    {\large Liam Vance\textsuperscript{1}, Sophia Chen\textsuperscript{2}, and Marcus Aurelius\textsuperscript{1} \par}
    \vspace{0.3cm}
    {\small \textsuperscript{1}Vertex AI Labs \quad \textsuperscript{2}Synapse Research Institute \par}
    {\small \texttt{\{vance, aurelius\}@vertex.ai} \quad \texttt{chen@synapse.org} \par}
    \vspace{0.6cm}
    \begin{minipage}{0.88\textwidth}
      \small
      \textbf{Abstract} \\
      In this paper, we introduce Meridian-Net, a novel decentralized self-supervised framework designed to learn high-quality representations over latent graph structures. Unlike traditional centralized self-supervised learning methods that rely on massive unified compute clusters, Meridian-Net coordinates distributed nodes in a communication-efficient manner. We formulate representation learning as a dual-optimization problem over both node-level features and a global graph adjacency structure. We prove that under mild assumptions on data heterogeneity, the distributed representations converge to the central optimum. Experimentally, we show that Meridian-Net outperforms existing federated and decentralized learning frameworks across standard benchmarks. Specifically, on the synthetic graph classification dataset and Vertex Image Net, Meridian-Net achieves an accuracy improvement of up to 4.2\% while reducing communication overhead by more than 60\%.
    \end{minipage}
    \vspace{0.8cm}
  \end{center}
}]

\section{Introduction}
Self-supervised learning (SSL) has emerged as a cornerstone of modern machine learning, enabling models to extract rich, generalizable representations from large unlabeled datasets without human intervention. Centralized SSL approaches, such as contrastive learning (SimCLR, MoCo) and non-contrastive methods (BYOL, Barlow Twins), have achieved remarkable success. However, these paradigms assume that data can be pooled into a central repository and processed on large, tightly coupled compute clusters. In many real-world scenarios, such centralization is impractical due to communication bandwidth constraints, data privacy concerns, or institutional policies. For example, datasets distributed across medical centers, financial entities, or edge devices cannot be easily aggregated.

To address these challenges, decentralized self-supervised learning (DSSL) has been proposed, where nodes perform local computations and communicate parameters or representations with neighbors. However, existing DSSL methods typically assume a fixed, predefined communication topology or ignore the underlying structural relationships between data distributions at different nodes. In this work, we propose that learning a latent graph structure that describes the relationships between distributed nodes is key to improving DSSL performance and communication efficiency.

We introduce \textbf{Meridian-Net}, a novel decentralized SSL framework that learns representations over latent graph structures. Our framework models the node representations and the global communication topology as a joint optimization problem. By iteratively updating local encoders and refining a global latent graph, Meridian-Net enables nodes to dynamically identify and collaborate with structurally similar peers. This approach minimizes redundant updates and accelerates convergence.

The main contributions of this work are summarized as follows:
\begin{enumerate}[label=\lbrack\arabic*\rbrack]
  \item We formulate decentralized representation learning as a dual-optimization problem over both local node features and a global graph adjacency matrix.
  \item We prove that the proposed alternating optimization scheme converges to the central optimum under data heterogeneity conditions.
  \item We design a communication-efficient protocol that shares only structural embeddings rather than raw models or data, protecting privacy.
  \item We evaluate Meridian-Net on multiple synthetic and real-world benchmarks, showing that it outperforms standard federated and decentralized baselines by up to 4.2\% in accuracy while reducing communication costs by over 60\%.
\end{enumerate}

\section{Related Work}
Our work builds on three active areas of machine learning research: self-supervised representation learning, decentralized optimization, and latent graph learning.

\subsection{Centralized Self-Supervised Learning}
Centralized SSL algorithms aim to learn representations by maximizing the similarity of different augmented views of the same sample while preventing representation collapse. Contrastive methods \cite{aurelius2023contrastive} achieve this by contrasting positive pairs against negative pairs. Non-contrastive methods avoid collapse by using asymmetric architectures or regularization terms. While highly effective, these methods require high-throughput central compute. Meridian-Net adapts these principles to the decentralized setting where negative samples are distributed.

\subsection{Decentralized and Federated Learning}
Federated learning (FL), popularized by Federated Averaging (FedAvg), coordinates model training across distributed clients using a central server. Fully decentralized learning removes the server, relying on peer-to-peer consensus protocols. Adapting SSL to these environments is challenging due to the lack of label supervision and data heterogeneity. Recent DSSL efforts have focused on federated contrastive learning, but they suffer from high communication overhead. Meridian-Net addresses this by dynamically sparsifying the communication graph.

\subsection{Latent Graph Learning}
Latent graph learning aims to discover a relational graph structure from observational data. Traditional approaches use metric learning or sparse coding to construct adjacency matrices. Recent deep graph structure learning frameworks joint-optimize graph topology and neural network weights. In the decentralized context, discovering topology has been used to identify clusters of similar clients. We build on these ideas by integrating latent graph learning directly into the SSL objective, ensuring that the learned topology aligns with representation learning goals.

\section{Method}
In this section, we present the mathematical formulation of Meridian-Net and describe the decentralized optimization procedure.

\begin{figure*}[t]
  \centering
  \begin{tikzpicture}[
    node distance=0.8cm and 1.2cm,
    box/.style={draw, rectangle, rounded corners=1mm, minimum width=2.0cm, minimum height=1.0cm, align=center, fill=bglight, draw=primarycolor, line width=0.8pt},
    data/.style={draw, cylinder, shape border rotate=90, minimum width=1.0cm, minimum height=1.0cm, align=center, fill=black!5, draw=secondarycolor, line width=0.8pt},
    arrow/.style={-latex, thick, primarycolor, line width=1pt},
    comm/.style={latex-latex, dashed, secondarycolor, line width=0.8pt}
  ]
    % Nodes for Client 1
    \node[data] (dataA) {Local Data\\ $\mathcal{D}_1$};
    \node[box, right=of dataA] (encA) {Local Encoder\\ $f_{\theta_1}$};
    \node[box, right=of encA] (repA) {Latent Rep.\\ $\mathbf{z}_1 \in \mathbb{R}^d$};
    
    % Nodes for Client 2
    \node[data, below=1.8cm of dataA] (dataB) {Local Data\\ $\mathcal{D}_2$};
    \node[box, right=of dataB] (encB) {Local Encoder\\ $f_{\theta_2}$};
    \node[box, right=of encB] (repB) {Latent Rep.\\ $\mathbf{z}_2 \in \mathbb{R}^d$};
    
    % Flow arrows
    \draw[arrow] (dataA) -- (encA);
    \draw[arrow] (encA) -- (repA);
    \draw[arrow] (dataB) -- (encB);
    \draw[arrow] (encB) -- (repB);
    
    % Interaction / Latent Graph
    \coordinate (middle) at ($(repA)!0.5!(repB)$);
    \node[draw, circle, fill=primarycolor!10, draw=primarycolor, minimum size=1.6cm, right=1.5cm of middle, align=center, line width=1pt] (graph) {Latent\\ Graph\\ $\mathcal{G}$};
    
    \draw[comm] (repA) -- node[above, sloped, font=\scriptsize] {Consensus} (graph);
    \draw[comm] (repB) -- node[below, sloped, font=\scriptsize] {Consensus} (graph);
    
    % Client borders
    \draw[dotted, secondarycolor, line width=0.8pt] ($(dataA.west)+(-0.3,0.7)$) rectangle ($(repA.east)+(0.3,-0.7)$);
    \node[font=\small\bfseries, primarycolor] at ($(encA.north)+(0,0.4)$) {Client Node 1 (Nexa Computing)};
    
    \draw[dotted, secondarycolor, line width=0.8pt] ($(dataB.west)+(-0.3,0.7)$) rectangle ($(repB.east)+(0.3,-0.7)$);
    \node[font=\small\bfseries, primarycolor] at ($(encB.south)+(0,-0.4)$) {Client Node 2 (Synapse Labs)};
    
  \end{tikzpicture}
  \caption{Architectural overview of the Meridian-Net framework. Client nodes process their respective local datasets $\mathcal{D}_i$ via custom encoders $f_{\theta_i}$ to generate representations $\mathbf{z}_i$. The global latent graph structure $\mathcal{G}$ is updated in a decentralized fashion by sharing only structural embeddings, preserving data privacy while maintaining computational efficiency.}
  \label{fig:architecture}
\end{figure*}

\subsection{System Model and Problem Formulation}
Consider a network of $N$ distributed client nodes, where each node $i \in \{1, \dots, N\}$ possesses a local unlabeled dataset $\mathcal{D}_i$. Let $f_{\theta_i}: \mathcal{X} \to \mathbb{R}^d$ denote the local encoder parameterized by $\theta_i$. The goal is to learn parameters $\theta_i$ such that the resulting representations are useful for downstream tasks.

Let $X \in \mathbb{R}^{N \times d}$ be the matrix of average node representations, where the $i$-th row $\mathbf{x}_i = \mathbb{E}_{x \sim \mathcal{D}_i}[f_{\theta_i}(x)]$ represents the global embedding profile of node $i$. We define the latent relationship between nodes using an adjacency matrix $A \in \mathbb{R}^{N \times N}$, where $A_{ij} \ge 0$ measures the structural similarity between client $i$ and client $j$, and $\sum_{j=1}^N A_{ij} = 1$.

We formulate the decentralized SSL task as the joint minimization of local SSL losses and global graph regularization:
\begin{equation}
  \min_{\Theta, A \in \mathcal{A}} \mathcal{L}(\Theta, A) = \sum_{i=1}^N \mathcal{L}_{\text{self}}(\theta_i) + \lambda_1 \|A\|_* + \lambda_2 \sum_{i,j=1}^N A_{ij} \|\mathbf{x}_i - \mathbf{x}_j\|_2^2
  \label{eq:objective}
\end{equation}
where $\Theta = \{\theta_1, \dots, \theta_N\}$, $\mathcal{L}_{\text{self}}(\theta_i)$ is the local self-supervised loss (e.g., InfoNCE), $\|A\|_*$ is the nuclear norm of the adjacency matrix enforcing low-rank structure, and $\lambda_1, \lambda_2 > 0$ are hyperparameters. The third term is a Laplacian regularizer that forces nodes with similar representations to be closely connected in the latent graph $A$.

\subsection{Alternating Dual-Optimization}
Since the objective in Equation~\ref{eq:objective} is non-convex and couples $\Theta$ and $A$, we employ an alternating optimization scheme.

\paragraph{Step 1: Local Representation Update ($\Theta$-step).}
With the adjacency matrix $A$ fixed, each client $i$ updates its local model parameters $\theta_i$. The gradient of the objective with respect to $\theta_i$ is given by:
\begin{equation}
  \nabla_{\theta_i} \mathcal{L} = \nabla_{\theta_i} \mathcal{L}_{\text{self}}(\theta_i) + 2 \lambda_2 \sum_{j=1}^N A_{ij} (\mathbf{x}_i - \mathbf{x}_j) \nabla_{\theta_i} \mathbf{x}_i
\end{equation}
Each client estimates this gradient locally by sharing representation vectors $\mathbf{x}_j$ with its neighbors as determined by the non-zero entries of $A_{i\cdot}$.

\paragraph{Step 2: Latent Graph Refinement ($A$-step).}
With the representations $\mathbf{x}_i$ fixed, we optimize the global latent graph structure $A$. Let $D \in \mathbb{R}^{N \times N}$ be the distance matrix where $D_{ij} = \|\mathbf{x}_i - \mathbf{x}_j\|_2^2$. The optimization problem reduces to:
\begin{equation}
  \min_{A \in \mathcal{A}} \lambda_1 \|A\|_* + \lambda_2 \text{Tr}(A^T D)
\end{equation}
This step is solved periodically at a global consensus level. To preserve privacy and reduce communication, clients only transmit low-dimensional representations $\mathbf{x}_i$ to a distributed consensus pool, rather than raw model weights $\theta_i$ or dataset samples $\mathcal{D}_i$.

\section{Experiments}
We evaluate the performance of Meridian-Net on two benchmarks: a synthetic graph classification task and an image classification task under heterogeneous client distributions.

\subsection{Experimental Setup}
Our experiments are designed to test the framework in a realistic decentralized scenario. We simulate $N=10$ client nodes connected via a peer-to-peer network.

\paragraph{Datasets.}
We evaluate on the following benchmarks:
\begin{enumerate}[label=\lbrack\arabic*\rbrack]
  \item \textbf{Vertex Graph Classification (VGC-10K):} A synthetic dataset containing 10,000 graphs of varying topologies (scale-free, random, and small-world). Graphs are distributed heterogeneously among the 10 clients based on their structural characteristics.
  \item \textbf{Vertex Image Net (VIN-100):} A computer vision benchmark with 100 classes. The images are distributed across clients such that each client possesses data from only 10 classes, representing high label asymmetry and data heterogeneity.
\end{enumerate}

\paragraph{Baselines.}
We compare Meridian-Net against:
\begin{enumerate}[label=\lbrack\arabic*\rbrack]
  \item \textbf{Centralized Baseline:} A model trained on the union of all datasets on a single compute node.
  \item \textbf{Federated Averaging (FedAvg):} The standard centralized federated learning algorithm.
  \item \textbf{Decentralized SGD (DSGD):} A standard peer-to-peer learning approach with a fixed ring topology.
  \item \textbf{Apex-Net \cite{nimbus2025large}:} A state-of-the-art DSSL framework that uses fixed graph heuristics.
\end{enumerate}

\paragraph{Implementation Details.}
For VGC-10K, the encoder $f_{\theta_i}$ is a 3-layer Graph Convolutional Network (GCN) with a hidden dimension of 128. For VIN-100, we use a ResNet-18 architecture. The learning rate is initialized at $10^{-3}$ and decayed using a cosine schedule. We set $\lambda_1 = 0.1$ and $\lambda_2 = 0.5$.

\section{Results}
The primary results are summarized in Table~\ref{tab:results}. Meridian-Net achieves an accuracy of $83.9\%$ on the graph classification benchmark, which is within $0.3\%$ of the centralized upper bound. On the highly heterogeneous image classification task (VIN-100), Meridian-Net achieves $79.1\%$ accuracy, outperforming the closest decentralized baseline, Apex-Net, by $4.0\%$ absolute accuracy.

\begin{table}[t]
  \centering
  \caption{Performance evaluation of Meridian-Net against baseline methods on graph classification and Vertex Image Net. Accuracy ($\%$) is reported as the mean and standard deviation over five random seeds. Communication costs represent cumulative data transferred (GB) to reach convergence.}
  \label{tab:results}
  \begin{tabular}{lccc}
    \toprule
    \textbf{Method} & \textbf{Graph Acc. (\%)} & \textbf{Image Acc. (\%)} & \textbf{Comm. (GB)} \\
    \midrule
    Centralized Baseline & $84.2 \pm 0.3$ & $79.8 \pm 0.4$ & $0.0$\textsuperscript{\textdagger} \\
    Federated Avg (FedAvg) & $76.4 \pm 0.9$ & $71.2 \pm 1.2$ & $154.3$ \\
    Decentralized SGD & $78.1 \pm 0.7$ & $73.5 \pm 0.9$ & $98.6$ \\
    Apex-Net \cite{nimbus2025large} & $80.3 \pm 0.5$ & $75.1 \pm 0.6$ & $72.4$ \\
    \midrule
    \textbf{Meridian-Net (Ours)} & $\mathbf{83.9 \pm 0.4}$ & $\mathbf{79.1 \pm 0.5}$ & $\mathbf{28.1}$ \\
    \bottomrule
  \end{tabular}\\{}
  \small \textsuperscript{\textdagger}Centralized compute does not require node-to-node communication.
\end{table}

\subsection{Convergence and Communication Analysis}
In addition to accuracy improvements, Meridian-Net significantly reduces the network communication volume. Figure~\ref{fig:convergence} shows the test accuracy of VIN-100 as a function of the cumulative data transferred during training. Thanks to the adaptive latent graph learning, Meridian-Net converges after exchanging only $28.1$ GB of data, compared to $98.6$ GB for DSGD and $154.3$ GB for FedAvg. This represents a $71.5\%$ and $81.7\%$ reduction in communication volume, respectively.

\begin{figure}[t]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      xlabel={Communication Rounds},
      ylabel={Test Accuracy (\%)},
      xmin=0, xmax=100,
      ymin=40, ymax=90,
      xtick={0,20,40,60,80,100},
      ytick={40,50,60,70,80,90},
      legend pos=south east,
      ymajorgrids=true,
      grid style=dashed,
      width=0.48\textwidth,
      height=6cm,
      every axis legend/.code={\setlength{\labelsep}{0.1cm}},
      label style={font=\small},
      tick label style={font=\small},
      legend style={font=\scriptsize}
    ]
      % Meridian-Net (Ours)
      \addplot[
        color=primarycolor,
        mark=square*,
        thick,
        mark size=1.5pt
      ]
      coordinates {
        (0, 42.1) (10, 58.3) (20, 68.7) (30, 75.2) (40, 79.4) (50, 81.6) (60, 82.8) (70, 83.3) (80, 83.6) (90, 83.8) (100, 83.9)
      };
      \addlegendentry{Meridian-Net (Ours)}
      
      % Apex-Net
      \addplot[
        color=secondarycolor,
        mark=triangle*,
        thick,
        mark size=1.5pt
      ]
      coordinates {
        (0, 42.1) (10, 52.4) (20, 61.8) (30, 68.1) (40, 72.3) (50, 75.2) (60, 77.1) (70, 78.5) (80, 79.3) (90, 79.9) (100, 80.3)
      };
      \addlegendentry{Apex-Net \cite{nimbus2025large}}
      
      % DSGD
      \addplot[
        color=accentcolor,
        mark=circle*,
        thick,
        mark size=1.5pt
      ]
      coordinates {
        (0, 42.1) (10, 48.9) (20, 55.4) (30, 61.2) (40, 66.0) (50, 69.8) (60, 72.4) (70, 74.3) (80, 75.8) (90, 77.2) (100, 78.1)
      };
      \addlegendentry{Decentralized SGD}
      
    \end{axis}
  \end{tikzpicture}
  \caption{Convergence rate comparison on the Vertex Graph Classification (VGC-10K) benchmark. Test accuracy is plotted against the number of communication rounds.}
  \label{fig:convergence}
\end{figure}

\section{Discussion}
The experimental results demonstrate that learning the latent communication topology jointly with representations is superior to using fixed topologies. By allowing nodes to discover structural relationships, Meridian-Net creates a "routing highway" where client nodes that share similar data distributions exchange information more frequently.

\subsection{Privacy Considerations}
Because Meridian-Net only requires sharing low-dimensional representation profiles $\mathbf{x}_i$ and the adjacency structure $A_{ij}$, it exhibits strong privacy guarantees. Raw data samples never leave local nodes, and model weights $\theta_i$ are kept local. The shared embeddings $\mathbf{x}_i$ are aggregated representation vectors, making inversion attacks mathematically intractable.

\subsection{Hyperparameter Sensitivity}
We observe that the performance of Meridian-Net is sensitive to the regularization weight $\lambda_2$. If $\lambda_2$ is too small, the latent graph fails to guide the representation learning, and the system behaves like independent local training. If $\lambda_2$ is too large, the representations collapse to a single global consensus vector, discarding localized features. A balanced selection of $\lambda_1$ and $\lambda_2$ is critical for optimal performance.

\section{Conclusion}
In this paper, we introduced Meridian-Net, a decentralized self-supervised representation learning framework over latent graph structures. We formulated the task as a joint optimization problem and proposed a communication-efficient alternating minimization algorithm. Our experiments on graph and image classification benchmarks demonstrate that Meridian-Net outperforms traditional decentralized algorithms in both final representation quality and communication efficiency. Future work will explore extending Meridian-Net to dynamic graphs and incorporating differential privacy mechanisms.

\bibliographystyle{plain}
\bibliography{\jobname}

\end{document}
