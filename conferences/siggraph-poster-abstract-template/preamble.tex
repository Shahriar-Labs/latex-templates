% preamble.tex - packages, colours, custom commands
\usepackage{tikz}
\usetikzlibrary{positioning, calc, shapes.geometric, backgrounds, patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage[most]{tcolorbox}
\tcbuselibrary{skins}

% Define custom colors (no trailing spaces in values)
\definecolor{SynapseBlue}{HTML}{0A4D68}
\definecolor{MeridianCoral}{HTML}{EF5B25}
\definecolor{VertexIndigo}{HTML}{563A9C}
\definecolor{NexaTeal}{HTML}{05BFDB}
\definecolor{NimbusGray}{HTML}{F5F5F7}

% Custom configuration for pgfplots to match styling
\pgfplotsset{
  synapsestyle/.style={
    color=SynapseBlue,
    mark=*,
    thick
  },
  vertexstyle/.style={
    color=VertexIndigo,
    mark=square*,
    thick
  },
  grid style={dashed, gray!30}
}
