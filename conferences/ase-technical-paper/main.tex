\documentclass[10pt,twocolumn]{article}

% ---------- Page geometry (US letter, two-column submission style) ----------
\usepackage[letterpaper,margin=0.75in,columnsep=0.28in]{geometry}

% ---------- Fonts & encoding (pdflatex only) ----------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{newtxtext}
\usepackage{newtxmath}
\usepackage{microtype}

% ---------- Graphics, tables, boxes ----------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,calc,shapes.geometric,backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{titlesec}
\usepackage{enumitem}

% ---------- Color palette (sober academic, minimal color) ----------
\definecolor{aseblue}{RGB}{27,58,94}
\definecolor{aseaccent}{RGB}{163,52,44}
\definecolor{asegray}{RGB}{95,95,100}
\definecolor{aselight}{RGB}{238,241,245}

% ---------- Section styling ----------
\titleformat{\section}{\normalfont\large\bfseries\color{aseblue}}{\thesection}{0.8em}{}
\titlespacing*{\section}{0pt}{1.4em}{0.6em}
\titleformat{\subsection}{\normalfont\normalsize\bfseries\color{aseblue}}{\thesubsection}{0.8em}{}
\titlespacing*{\subsection}{0pt}{1.0em}{0.4em}

\begin{filecontents*}{refs.bib}
@inproceedings{okafor2023searchapr,
  author    = {Devon A. Okafor and Priya S. Raghavan},
  title     = {Search-Based Program Repair Revisited: A Decade of Empirical Lessons},
  booktitle = {Proceedings of the 45th International Conference on Software Engineering (ICSE)},
  year      = {2023},
  pages     = {112--124}
}
@inproceedings{marchetti2022neuralrepair,
  author    = {Elena Marchetti and Tomas Lindqvist},
  title     = {Neural Patch Synthesis with Masked Span Infilling},
  booktitle = {Proceedings of the 37th IEEE/ACM International Conference on Automated Software Engineering (ASE)},
  year      = {2022},
  pages     = {801--813}
}
@article{osei2021survey,
  author  = {Kwame Osei and Lucia Fontana},
  title   = {A Survey of Automated Program Repair Techniques: 2015--2021},
  journal = {ACM Transactions on Software Engineering and Methodology},
  volume  = {30},
  number  = {4},
  pages   = {1--45},
  year    = {2021}
}
@inproceedings{nakamura2020rag,
  author    = {Sora Nakamura and Ben Whitfield},
  title     = {Retrieval-Augmented Code Generation for Bug Fixing},
  booktitle = {Proceedings of the 42nd International Conference on Software Engineering (ICSE)},
  year      = {2020},
  pages     = {455--466}
}
@inproceedings{alvarez2019classifier,
  author    = {Marta Alvarez and Chidi Eze},
  title     = {Learning to Rank Candidate Patches with Overfitting-Aware Features},
  booktitle = {Proceedings of the 34th IEEE/ACM International Conference on Automated Software Engineering (ASE)},
  year      = {2019},
  pages     = {210--221}
}
@inproceedings{feldman2018quirkbugs,
  author    = {Ari Feldman and Ingrid Solberg},
  title     = {QuirkBugs: A Small-Scale Benchmark for Program Repair Evaluation},
  booktitle = {Proceedings of the International Symposium on Software Testing and Analysis (ISSTA)},
  year      = {2018},
  pages     = {330--341}
}
@inproceedings{tanaka2021faultloc,
  author    = {Haruto Tanaka and Renata Costa},
  title     = {Spectrum-Based Fault Localization at Scale: An Industrial Study},
  booktitle = {Proceedings of the 43rd International Conference on Software Engineering (ICSE)},
  year      = {2021},
  pages     = {988--999}
}
@article{oyelaran2020overfitting,
  author  = {Ade Oyelaran and Karin Novak},
  title   = {The Overfitting Problem in Automated Program Repair: Causes and Mitigations},
  journal = {IEEE Transactions on Software Engineering},
  volume  = {46},
  number  = {9},
  pages   = {1005--1021},
  year    = {2020}
}
@inproceedings{delgado2022llmcode,
  author    = {Sofia Delgado and Marcus Reinholt},
  title     = {Large Language Models for Code: An Empirical Study of Repair Capabilities},
  booktitle = {Proceedings of the 44th International Conference on Software Engineering (ICSE)},
  year      = {2022},
  pages     = {1420--1432}
}
@inproceedings{blackwood2017genprog,
  author    = {Owen Blackwood and Ji-woo Han},
  title     = {Revisiting Genetic Program Repair with Modern Fitness Functions},
  booktitle = {Proceedings of the 39th International Conference on Software Engineering (ICSE)},
  year      = {2017},
  pages     = {77--88}
}
@inproceedings{singh2023faultline,
  author    = {Aarav Singh and Beatrix Moreau},
  title     = {FaultLine-140: A Multilingual Benchmark for Fault Repair Research},
  booktitle = {Proceedings of the 45th IEEE/ACM International Conference on Automated Software Engineering (ASE)},
  year      = {2023},
  pages     = {655--667}
}
@inproceedings{park2019embeddings,
  author    = {Ji-hoon Park and Naledi Mokoena},
  title     = {Code Embeddings for Similarity-Based Patch Retrieval},
  booktitle = {Proceedings of the ACM SIGPLAN Conference on Programming Language Design and Implementation (PLDI)},
  year      = {2019},
  pages     = {544--556}
}
\end{filecontents*}

% ---------- Hyperlinks (loaded last) ----------
\usepackage[colorlinks=true,linkcolor=aseblue,citecolor=aseblue,urlcolor=aseblue]{hyperref}
\hypersetup{
  pdftitle={Retrieval-Augmented Patch Ranking for Automated Program Repair},
  pdfauthor={Elena Marchetti, Devon Okafor, Priya Raghavan}
}

\setlength{\parindent}{1em}

\begin{document}

\twocolumn[
  {\centering
    {\Large\bfseries Retrieval-Augmented Patch Ranking for Automated Program Repair: An Empirical Study\par}
    \vspace{0.9em}
    {\large
      Elena Marchetti$^{1}$ \quad Devon Okafor$^{2}$ \quad Priya Raghavan$^{3}$ \par}
    \vspace{0.5em}
    {\small\color{asegray}
      $^{1}$Nexa Institute of Software Systems, Rindport \quad
      $^{2}$Synapse Research Labs, Calderbrook \quad
      $^{3}$Vertex University, Department of Computer Science, Oakmere \par
      \texttt{e.marchetti@nexa-iss.org} \quad
      \texttt{d.okafor@synapselabs.io} \quad
      \texttt{p.raghavan@vertex.edu} \par}
    \vspace{1.1em}
  }

  \noindent{\color{aseaccent}\rule{\linewidth}{1.1pt}}\par
  \vspace{4pt}
  \noindent{\small\color{black}
    \textbf{Unofficial template.} This document is an independently produced \LaTeX{} template that mimics the general visual style of a two-column automated-software-engineering conference submission. It is \textbf{not affiliated with, endorsed by, or sponsored by ACM, IEEE, ASE, or any conference organizing committee}. Formatting requirements change from year to year --- always download the current official style files and consult the official call for papers before preparing a submission.\par}
  \vspace{4pt}
  \noindent{\color{aseaccent}\rule{\linewidth}{1.1pt}}\par
  \vspace{0.7em}

  \begin{center}
  \begin{minipage}{0.94\textwidth}
    \small
    \textbf{Abstract.} Automated program repair (APR) pipelines routinely generate dozens of candidate patches per bug, only a small fraction of which are both test-passing and semantically correct. We present \textsc{RankGen}, a retrieval-augmented ranking layer that scores candidate patches by combining (i) similarity to a corpus of previously accepted human patches, (ii) the log-likelihood assigned by a masked span-infilling language model, and (iii) a lightweight overfitting prior estimated from static patch features. RankGen sits downstream of any candidate generator and requires no change to the underlying repair search. Across three benchmarks spanning 412 real-world defects, RankGen improves the correct-patch rate within a top-10 budget by 12.4 percentage points on average over a strong retrieval-free baseline, while adding under 3 seconds of ranking latency per bug. We release our scoring formulation, ablations over each score component, and an analysis of failure modes where retrieval similarity misleads the ranker.
    \par\vspace{0.3em}
    \textit{Keywords:} automated program repair, retrieval-augmented generation, patch ranking, empirical software engineering
  \end{minipage}
  \end{center}
  \vspace{1.0em}
]

\section{Introduction}
Automated program repair promises to turn a failing test suite directly into a candidate fix, but the practical bottleneck has shifted from \emph{generating} plausible patches to \emph{selecting} the correct one among many. A typical search-based or learning-based generator emits tens to hundreds of candidates that pass the visible tests yet only a minority match the developer's intended fix~\cite{oyelaran2020overfitting,alvarez2019classifier}. Developers reviewing a ranked list therefore pay an attention tax proportional to how far the correct patch sits from the top of that list.

This paper studies patch ranking as a first-class problem, independent of which generator produced the candidates. We introduce \textsc{RankGen}, a scorer that blends three complementary signals: (1) retrieval similarity against an index of accepted historical patches, (2) the likelihood a pretrained code language model assigns to the candidate conditioned on its surrounding context, and (3) a small set of static features that are known to correlate with overfitting, such as patch size relative to the fault footprint. Section~\ref{sec:approach} formalizes the combination as a single scoring function (Equation~\ref{eq:score}) and describes how each term is estimated.

Our contributions are:
\begin{itemize}[leftmargin=1.1em,itemsep=0.1em,topsep=0.2em]
  \item A retrieval-augmented ranking formulation that is generator-agnostic and adds negligible latency.
  \item An empirical study across three benchmarks (\S\ref{sec:setup}) showing consistent gains in correct-patch rate at fixed candidate budgets.
  \item An ablation and error analysis (\S\ref{sec:results}, \S\ref{sec:discussion}) isolating when retrieval similarity helps and when it actively misleads the ranker.
\end{itemize}

\section{Related Work}
\label{sec:related}
\textbf{Search-based and genetic repair.} Early APR systems mutate the buggy program guided by a fitness function derived from the test suite~\cite{blackwood2017genprog,okafor2023searchapr}. These approaches generate large candidate pools but provide no principled ranking beyond test-suite pass/fail, motivating downstream ranking layers such as ours.

\textbf{Learning-based generation.} Neural approaches synthesize patches directly, either by sequence-to-sequence translation of the buggy statement or by span infilling over a masked hunk~\cite{marchetti2022neuralrepair,delgado2022llmcode}. RankGen is complementary to this line of work: it consumes whatever candidates such a generator produces and reorders them.

\textbf{Patch correctness and overfitting.} A substantial body of work shows that test-suite-passing patches frequently overfit to the visible tests~\cite{oyelaran2020overfitting}. Classifier-based approaches learn to separate correct from overfitting patches using static and dynamic features~\cite{alvarez2019classifier}. RankGen incorporates a subset of these features as one term in its score rather than as a standalone classifier, which we find improves calibration when combined with retrieval and likelihood signals.

\textbf{Retrieval and embeddings for code.} Retrieval-augmented generation has been explored for general code synthesis~\cite{nakamura2020rag} and for embedding-based patch or code search~\cite{park2019embeddings}. We build directly on this idea but apply it specifically to ranking, not generation, and combine it with a language-model prior in a single interpretable scoring function.

\textbf{Fault localization and benchmarks.} Our pipeline consumes off-the-shelf spectrum-based fault localization~\cite{tanaka2021faultloc} and evaluates against community benchmarks~\cite{feldman2018quirkbugs,singh2023faultline}, plus a benchmark we curate ourselves (\S\ref{sec:setup}). Surveys of the broader APR landscape provide additional context on the taxonomy of generation strategies we build on~\cite{osei2021survey}.

\section{Approach}
\label{sec:approach}
\textsc{RankGen} is a post-hoc scoring layer applied to a candidate set $\mathcal{P} = \{p_1, \dots, p_n\}$ produced by any upstream generator, given a buggy context $c$ (the surrounding function together with the failing test names). Figure~\ref{fig:pipeline} shows the full pipeline: fault localization narrows the edit location, a candidate generator proposes patches, and \textsc{RankGen} reorders them before the top-$k$ are sent to the test verifier.

\begin{figure}[t]
\centering
\resizebox{\columnwidth}{!}{%
\begin{tikzpicture}[
  node distance=7mm and 8mm,
  box/.style={draw=aseblue,thick,rounded corners,fill=aselight,align=center,minimum height=8mm,minimum width=19mm,font=\scriptsize},
  ret/.style={draw=asegray,thick,rounded corners,fill=white,align=center,minimum height=8mm,minimum width=19mm,font=\scriptsize},
  arr/.style={-{Latex[length=1.8mm]},thick,aseblue}
]
\node[box] (bug) {Buggy\\Program};
\node[box, right=of bug] (fl) {Fault\\Localizer};
\node[box, right=of fl] (gen) {Candidate\\Generator};
\node[box, below=of gen] (rank) {Ranking\\Scorer};
\node[ret, left=of rank] (ret) {Retrieval\\Index};
\node[box, right=of rank] (ver) {Test\\Verifier};

\draw[arr] (bug) -- (fl);
\draw[arr] (fl) -- (gen);
\draw[arr] (gen) -- (rank);
\draw[arr] (ret) -- (rank);
\draw[arr] (rank) -- (ver);
\end{tikzpicture}}
\caption{Pipeline overview. \textsc{RankGen} (the ranking scorer) is generator-agnostic and consults a retrieval index built from historical accepted patches.}
\label{fig:pipeline}
\end{figure}

\subsection{Scoring function}
For a candidate patch $p$, context $c$, and retrieval corpus $\mathcal{R}$, we define the score
\begin{equation}
\label{eq:score}
\mathrm{score}(p) \;=\; \lambda_1\,\mathrm{sim}(p,\mathcal{R}) \;+\; \lambda_2\,\log P_{\theta}(p \mid c) \;+\; \lambda_3\,\pi(p)
\end{equation}
where $\mathrm{sim}(p,\mathcal{R}) \in [0,1]$ is the maximum cosine similarity between an embedding of $p$ and the embeddings of the $k$-nearest patches retrieved from $\mathcal{R}$, $P_{\theta}(p\mid c)$ is the likelihood assigned by a masked span-infilling model $\theta$, and $\pi(p) \in [0,1]$ is an overfitting prior computed from static features (patch size, number of edited statements, and structural distance from the fault-localization ranking). The weights satisfy $\lambda_1 + \lambda_2 + \lambda_3 = 1$ and are fit on a held-out validation split via grid search.

\subsection{Retrieval index}
The corpus $\mathcal{R}$ is built once per project from its commit history: every commit whose message matches a bug-fix heuristic and whose diff touches a single function is embedded and indexed. At inference time we retrieve the $k{=}20$ nearest neighbors of each candidate using approximate nearest-neighbor search, which keeps retrieval latency under 40ms per candidate even for indices exceeding $10^5$ entries.

\subsection{Overfitting prior}
The prior $\pi(p)$ reuses the intuition that overfitting patches tend to be small, syntactically shallow edits that happen to satisfy the visible assertions without generalizing~\cite{oyelaran2020overfitting}. We fit a lightweight logistic model over five static features rather than the larger feature sets used by dedicated classifiers~\cite{alvarez2019classifier}, trading a small amount of standalone accuracy for a term that composes cleanly with $\mathrm{sim}$ and $\log P_\theta$ inside Equation~\ref{eq:score}.

\section{Experimental Setup}
\label{sec:setup}
\textbf{Benchmarks.} We evaluate on three defect collections: \emph{RepairBench}, a set of 210 real-world defects we curate for this study from seven open-source Java projects; \emph{QuirkBugs}~\cite{feldman2018quirkbugs}, a small, well-studied collection of 42 single-line defects; and \emph{FaultLine-140}~\cite{singh2023faultline}, a 160-defect multilingual benchmark spanning Java and Python.

\textbf{Baseline.} Our baseline ranks candidates by test-suite pass count followed by a simple textual-similarity tiebreak, matching common practice in prior ranking work~\cite{nakamura2020rag} without the retrieval-index or overfitting-prior terms.

\textbf{Candidate generator.} Both the baseline and \textsc{RankGen} consume candidates from the same masked span-infilling generator, holding the generation strategy fixed so that all measured differences are attributable to ranking.

\textbf{Metrics.} We report the \emph{plausible} rate (fraction of defects for which some candidate in the top-$k$ passes the full held-out test suite) and the \emph{correct} rate (fraction for which the top-$k$ list contains a patch manually verified as semantically equivalent to the developer fix), together with average wall-clock ranking time per defect.

\textbf{Implementation.} The retrieval index uses 256-dimensional embeddings from a code encoder trained by the generator's own pretraining pipeline; approximate nearest-neighbor search uses a graph-based index with $k{=}20$. Weights $\lambda_1,\lambda_2,\lambda_3$ are tuned on a validation split disjoint from all reported test defects.

\section{Results}
\label{sec:results}
Table~\ref{tab:results} reports plausible and correct rates at $k{=}10$ across all three benchmarks. \textsc{RankGen} improves the correct rate by 8--17.5 percentage points depending on the benchmark, with the largest absolute gain on QuirkBugs, whose small single-line defects make retrieval similarity especially informative.

\begin{table}[t]
\centering
\caption{Repair effectiveness at a top-10 candidate budget. Plausible: passes the held-out test suite. Correct: manually verified equivalent to the developer patch.}
\label{tab:results}
\small
\setlength{\tabcolsep}{3.6pt}
\begin{tabular}{@{}llccc@{}}
\toprule
\rowcolor{aseblue!10}
Benchmark & Method & Plausible (\%) & Correct (\%) & Time (s) \\
\midrule
\multirow{2}{*}{RepairBench} & Baseline & 41.2 & 22.6 & 38.4 \\
 & Ours & \textbf{58.7} & \textbf{34.9} & 41.1 \\
\midrule
\multirow{2}{*}{QuirkBugs} & Baseline & 55.0 & 40.0 & 12.7 \\
 & Ours & \textbf{72.5} & \textbf{57.5} & 14.0 \\
\midrule
\multirow{2}{*}{FaultLine-140} & Baseline & 33.6 & 19.3 & 51.9 \\
 & Ours & \textbf{47.1} & \textbf{29.3} & 55.6 \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:topk} shows how the correct-patch rate scales with the candidate budget $k$, averaged across all three benchmarks. The gap between \textsc{RankGen} and the baseline is largest at small $k$, which is precisely the regime that matters for a developer reviewing a short ranked list.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar,
  bar width=6pt,
  width=\linewidth,
  height=4.3cm,
  ylabel={Correct patches (\%)},
  xlabel={Top-$k$ candidates},
  symbolic x coords={1,5,10,20,50},
  xtick=data,
  ymin=0, ymax=60,
  legend style={font=\tiny,at={(0.5,1.16)},anchor=south,legend columns=-1,draw=none},
  enlarge x limits=0.15,
  axis line style={aseblue},
  tick label style={font=\tiny},
  label style={font=\small},
]
\addplot[fill=aseblue,draw=aseblue] coordinates {(1,18) (5,29) (10,35) (20,41) (50,47)};
\addplot[fill=aseaccent,draw=aseaccent] coordinates {(1,11) (5,19) (10,24) (20,28) (50,33)};
\legend{Ours, Baseline}
\end{axis}
\end{tikzpicture}
\caption{Correct-patch rate as a function of candidate budget $k$, averaged over the three benchmarks.}
\label{fig:topk}
\end{figure}

\textbf{Ablation.} Removing the retrieval term ($\lambda_1{=}0$) drops the average correct rate by 6.1 points; removing the language-model term drops it by 8.7 points; removing the overfitting prior drops it by 3.4 points. All three terms contribute, and the language-model likelihood is individually the strongest single signal, consistent with prior findings that generation confidence correlates with correctness~\cite{delgado2022llmcode}.

\section{Discussion}
\label{sec:discussion}
\textbf{When retrieval helps.} Retrieval similarity is most useful when the fix pattern recurs across a project's history --- null checks, off-by-one bounds, and resource-cleanup idioms all show strong nearest-neighbor structure. It is least useful for defects requiring a novel algorithmic change, where the nearest historical patch is a poor analogue and can actively pull the score toward a superficially similar but incorrect candidate.

\textbf{Threats to validity.} Our correctness labels rely on manual verification against developer patches, which is subject to annotator judgment despite a double-review protocol. RepairBench, though curated to be representative, is smaller than some community benchmarks and may not generalize to defect distributions outside our seven source projects. Finally, the retrieval index requires project history with a reasonable density of prior bug-fix commits; very young repositories may see reduced gains from the $\mathrm{sim}$ term.

\textbf{Latency.} The added ranking cost (2--4 seconds per defect) is dominated by embedding the candidate set, not by the nearest-neighbor search itself, suggesting batched embedding could reduce this further without changing the scoring formulation.

\section{Conclusion}
We presented \textsc{RankGen}, a retrieval-augmented ranking layer for automated program repair that combines historical-patch similarity, language-model likelihood, and a static overfitting prior into a single interpretable score. Applied downstream of a fixed candidate generator, it improves correct-patch rate by double-digit percentage points at practical candidate budgets across three benchmarks, with the gains concentrated exactly where they matter most: near the top of the ranked list. Future work includes extending the retrieval corpus across projects with similar dependency stacks and studying how ranking interacts with interactive, developer-in-the-loop repair workflows.

\section*{Acknowledgments}
{\small We thank the anonymous reviewers for their feedback. This work was supported in part by a Nexa Institute Faculty Research Award and by Synapse Research Labs internal research funding.}

\bibliographystyle{plain}
\bibliography{refs}

\end{document}
