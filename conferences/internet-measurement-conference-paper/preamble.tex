% Packages and colours for this template, on top of what acmart already loads.
%
% acmart sets its own fonts, page geometry, section headings, captions, spacing and
% hyperlinks, and the ACM format depends on them, so this file adds nothing that changes
% those. In particular: no geometry, no font packages, no titlesec, no caption settings, no
% \parskip or \parindent changes, and no amssymb (acmart's math fonts already define its
% symbols, and loading it stops the build).

\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\definecolor{nexablue}{RGB}{31,71,115}
\definecolor{synapseteal}{RGB}{43,124,120}
\definecolor{vertexcopper}{RGB}{176,98,50}
