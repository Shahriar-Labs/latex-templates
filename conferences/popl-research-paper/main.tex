\documentclass[10pt,twocolumn]{article}

% Packages and styling live in preamble.tex; each section has its own file under
% sections/. Edit those directly — this file is the outline of the document.
\input{preamble}

\begin{document}

\maketitle

\begin{tcolorbox}[
    colback=white, colframe=poplrule, boxrule=0.6pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt,
    before skip=0.4em, after skip=0.9em
  ]
  \small\color{poplgray}
  \textbf{Unofficial template.} This document is an independent LaTeX
  template inspired by the general look of POPL-style two-column
  submissions. It is \textbf{not affiliated with, endorsed by, or
  derived from official ACM SIGPLAN or POPL production files}. Authors
  preparing an actual submission \textbf{must} obtain the current
  year's official call for papers and \texttt{acmart} style files from
  the POPL/SIGPLAN organizers before use.
\end{tcolorbox}

\begin{abstract}
  \noindent
  Static resource analyses for higher-order languages typically force a
  choice between annotation-heavy type systems that place a real
  burden on programmers and purely dynamic budgets that offer no
  compile-time guarantee. We present \textsc{Bidir}, a lightweight
  bidirectional discipline for checking resource budgets that infers
  cost annotations for the bulk of a program while asking for
  explicit signatures only at module boundaries. \textsc{Bidir}
  is parametrized by an abstract cost monoid, so the same checker
  supports allocation counts, heap high-water marks, and wall-clock
  step budgets without modification to the core rules. We give a
  declarative specification, a syntax-directed bidirectional
  algorithm, and a proof that the algorithm is sound and complete
  with respect to the declarative system. An implementation on top of
  a small higher-order calculus is evaluated on a corpus of $482$
  functions drawn from three open-source libraries; \textsc{Bidir}
  infers a tight budget without any local annotation for
  $91.3\%$ of functions and reduces total annotation burden by
  $6.4\times$ relative to a fully annotated baseline, at a median
  checking overhead of $1.8\times$ over a budget-free typechecker.
\end{abstract}

\input{sections/01-introduction}
\input{sections/02-related-work}
\input{sections/03-method}
\input{sections/04-experiments}
\input{sections/05-results}
\input{sections/06-discussion}
\input{sections/07-conclusion}
\end{document}
