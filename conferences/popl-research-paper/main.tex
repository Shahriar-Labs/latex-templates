\documentclass[10pt,twocolumn]{article}

% ---------------------------------------------------------------------------
% POPL Research Paper (Unofficial) -- two-column US-letter submission style
% ---------------------------------------------------------------------------
\usepackage[letterpaper,margin=0.85in,columnsep=0.28in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns}
\usepackage[colorlinks=true,linkcolor=poplblue,citecolor=poplblue,urlcolor=poplblue,breaklinks=true]{hyperref}
\usepackage[capitalize]{cleveref}
\usepackage{enumitem}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{caption}
\usepackage{authblk}
\usepackage{filecontents}

% ---------------------------------------------------------------------------
% Palette: sober academic, minimal color
% ---------------------------------------------------------------------------
\definecolor{poplblue}{RGB}{31,58,102}
\definecolor{poplgray}{RGB}{90,90,90}
\definecolor{poplrule}{RGB}{200,200,200}
\definecolor{poplaccent}{RGB}{176,58,46}

\renewcommand{\Authfont}{\bfseries\normalsize}
\renewcommand{\Affilfont}{\normalsize\itshape\color{poplgray}}
\setlength{\affilsep}{0.6em}

\captionsetup{font=small,labelfont={bf,color=poplblue},labelsep=period}

% ---------------------------------------------------------------------------
% Bibliography source (filecontents so this file is fully standalone)
% ---------------------------------------------------------------------------
\begin{filecontents*}[overwrite]{references.bib}
@inproceedings{oyelaran2019gradual,
  author    = {Oyelaran, Femi and Kastrinakis, Elina},
  title     = {Gradual Effect Typing for Higher-Order Modules},
  booktitle = {Proceedings of the 46th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2019},
  pages     = {1--14},
  publisher = {ACM}
}

@inproceedings{brandvold2020region,
  author    = {Brandvold, Sigrid and Petrossian, Aram},
  title     = {Region Inference without Annotation Burden},
  booktitle = {Proceedings of the 47th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2020},
  pages     = {112--126},
  publisher = {ACM}
}

@article{nakashima2018session,
  author  = {Nakashima, Rieko and Villaverde, Ines},
  title   = {Session-Typed Actors: A Denotational Account},
  journal = {Journal of Functional Programming},
  volume  = {28},
  year    = {2018},
  pages   = {e3}
}

@inproceedings{holsapple2021relational,
  author    = {Holsapple, Trent and Mbeki, Naledi},
  title     = {Relational Cost Analysis via Bidirectional Refinement},
  booktitle = {Proceedings of the 48th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2021},
  pages     = {201--215},
  publisher = {ACM}
}

@inproceedings{fennimore2017linear,
  author    = {Fennimore, Adaeze and Kowalczyk, Piotr},
  title     = {Linear Capabilities for Safe Shared-Memory Concurrency},
  booktitle = {Proceedings of the 44th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2017},
  pages     = {33--46},
  publisher = {ACM}
}

@inproceedings{delacroix2022verified,
  author    = {Delacroix, Manon and Osei, Kwabena},
  title     = {A Verified Compilation Pipeline for Dependent Pattern Matching},
  booktitle = {Proceedings of the 49th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2022},
  pages     = {88--102},
  publisher = {ACM}
}

@article{thorvaldsen2016abstract,
  author  = {Thorvaldsen, Ingrid and Basu, Ronojoy},
  title   = {Abstract Interpretation of Probabilistic Programs at Scale},
  journal = {ACM Transactions on Programming Languages and Systems},
  volume  = {38},
  number  = {4},
  year    = {2016},
  pages   = {1--29}
}

@inproceedings{vantassel2023effect,
  author    = {Van Tassel, Miriam and Odusanya, Bolaji},
  title     = {Effect Handlers with Statically Bounded Reinstallation},
  booktitle = {Proceedings of the 50th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2023},
  pages     = {301--316},
  publisher = {ACM}
}

@inproceedings{casagrande2015refinement,
  author    = {Casagrande, Umberto and Lindqvist, Saga},
  title     = {Refinement Types for Incremental Computation},
  booktitle = {Proceedings of the 42nd ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2015},
  pages     = {455--468},
  publisher = {ACM}
}

@article{marchetti2019coeffect,
  author  = {Marchetti, Dario and Yeboah, Abena},
  title   = {Coeffect Systems for Resource-Sensitive Reactive Programs},
  journal = {Proceedings of the ACM on Programming Languages},
  volume  = {3},
  number  = {POPL},
  year    = {2019},
  pages   = {1--28}
}

@inproceedings{halvorsen2020gradual,
  author    = {Halvorsen, Torbjorn and Anand, Priyamvada},
  title     = {Gradual Ownership: Migrating Legacy Code to Linear Types},
  booktitle = {Proceedings of the 47th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2020},
  pages     = {560--574},
  publisher = {ACM}
}

@inproceedings{quintero2021synthesis,
  author    = {Quintero, Belen and Okonkwo, Chidi},
  title     = {Synthesis-Aided Proof Repair for Type-Preserving Transformations},
  booktitle = {Proceedings of the 48th ACM SIGPLAN Symposium on Principles of Programming Languages (POPL)},
  year      = {2021},
  pages     = {742--757},
  publisher = {ACM}
}
\end{filecontents*}

\title{\vspace{-1.2em}\textbf{\Large Bidirectional Effect Refinement: A Lightweight
  Discipline for Checking Resource Budgets in Higher-Order Programs}}

\author[1]{Elowen Marsh}
\author[2]{Tobias R\o{}nning}
\author[1]{Priya Chandrasekaran}
\affil[1]{Department of Computer Science, Meridian Institute of Technology}
\affil[2]{Vertex Research Laboratories, Programming Systems Group}

\date{}

\begin{document}
\maketitle

\begin{tcolorbox}[
    colback=white, colframe=poplrule, boxrule=0.6pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt,
    before skip=0.4em, after skip=0.9em
  ]
  \small\color{poplgray}
  \textbf{Unofficial template.} This document is an independent LaTeX
  template inspired by the general look of POPL-style two-column
  submissions. It is \textbf{not affiliated with, endorsed by, or
  derived from official ACM SIGPLAN or POPL production files}. Authors
  preparing an actual submission \textbf{must} obtain the current
  year's official call for papers and \texttt{acmart} style files from
  the POPL/SIGPLAN organizers before use.
\end{tcolorbox}

\begin{abstract}
  \noindent
  Static resource analyses for higher-order languages typically force a
  choice between annotation-heavy type systems that place a real
  burden on programmers and purely dynamic budgets that offer no
  compile-time guarantee. We present \textsc{Bidir}, a lightweight
  bidirectional discipline for checking resource budgets that infers
  cost annotations for the bulk of a program while asking for
  explicit signatures only at module boundaries. \textsc{Bidir}
  is parametrized by an abstract cost monoid, so the same checker
  supports allocation counts, heap high-water marks, and wall-clock
  step budgets without modification to the core rules. We give a
  declarative specification, a syntax-directed bidirectional
  algorithm, and a proof that the algorithm is sound and complete
  with respect to the declarative system. An implementation on top of
  a small higher-order calculus is evaluated on a corpus of $482$
  functions drawn from three open-source libraries; \textsc{Bidir}
  infers a tight budget without any local annotation for
  $91.3\%$ of functions and reduces total annotation burden by
  $6.4\times$ relative to a fully annotated baseline, at a median
  checking overhead of $1.8\times$ over a budget-free typechecker.
\end{abstract}

\section{Introduction}
\label{sec:intro}

Programmers reasoning about the resource behavior of higher-order code
face a familiar tension. Fully annotated cost type systems give strong,
checkable guarantees, but every closure, every partial application, and
every point where a function value crosses a module boundary demands an
explicit cost signature. In practice this annotation burden is heavy
enough that resource-aware type systems see little uptake outside of
specialized domains such as embedded control loops and cryptographic
kernels, where the guarantee is worth the cost. At the other extreme,
purely dynamic budget checks --- a counter decremented at each allocation
site, tripped when it reaches zero --- offer no static assurance at all:
a budget violation is discovered only when a specific input triggers it
at run time.

This paper describes \textsc{Bidir}, a bidirectional type discipline that
narrows this gap for a large and common class of programs: those where
resource-relevant structure is local, and only the \emph{interfaces}
between components need an explicit contract. The design follows the
familiar bidirectional recipe of separating a \emph{checking} judgment,
which propagates an expected cost bound inward, from a
\emph{synthesis} judgment, which computes a principal cost bound outward
from the syntax of an expression. Function bodies are checked against a
budget synthesized from their parameter and return types; call sites
combine the synthesized cost of the callee with the checked cost of each
argument. Crucially, only $\lambda$-abstractions that are stored, passed
across an explicit module signature, or subject to unbounded recursion
require a programmer-supplied annotation --- straight-line and
first-order code is fully inferred.

Our contributions are:
\begin{enumerate}[leftmargin=1.35em,itemsep=1pt,topsep=2pt]
  \item A declarative bidirectional cost discipline, parametrized over an
    abstract commutative cost monoid $(\mathcal{C}, \oplus, \mathbf{0})$,
    together with a subtyping-style bound relation $\sqsubseteq_{\mathcal{C}}$
    (\cref{sec:method}).
  \item A syntax-directed algorithm implementing the discipline, and a
    proof of soundness and completeness relative to the declarative
    system (\cref{sec:method}).
  \item An evaluation on $482$ functions from three open-source
    higher-order libraries, measuring inference coverage, annotation
    burden, and checking-time overhead relative to a fully annotated
    baseline and a budget-free typechecker (\cref{sec:experiments,sec:results}).
\end{enumerate}

\section{Related Work}
\label{sec:related}

\paragraph{Cost and resource type systems.}
Static cost analysis for functional languages traces back to
sized-type and potential-based amortized analyses, which assign a
numeric potential to data structures and track its evolution through
recursive calls~\cite{thorvaldsen2016abstract}. These systems achieve
tight, often automatically inferred bounds for first-order recursive
functions, but the higher-order case --- where a cost-carrying function
value itself flows through the program --- generally requires either a
restrictive stratification or full annotation at every closure
allocation.

\paragraph{Effect and coeffect systems.}
Effect handlers with resource-aware installation
disciplines~\cite{vantassel2023effect} and coeffect systems for
reactive, resource-sensitive programs~\cite{marchetti2019coeffect} both
track a lightweight algebraic summary of a computation's requirements
alongside its type. \textsc{Bidir} borrows the algebraic-summary idea
but specializes it to a single scalar-like cost monoid rather than a
general effect row, which is what enables the bidirectional
checked/synthesized split described in \cref{sec:method}.

\paragraph{Gradual and mixed disciplines.}
Gradual typing for ownership and region
disciplines~\cite{halvorsen2020gradual,brandvold2020region} shows that
mixing statically checked and dynamically checked fragments of a
guarantee can substantially reduce annotation burden while preserving a
soundness theorem for the statically checked fragment. \textsc{Bidir}
follows this spirit for cost rather than ownership, but --- unlike a
gradual system --- never inserts a run-time cast; every inferred bound
is a compile-time guarantee, obtained instead by shifting the annotation
requirement to module boundaries.

\paragraph{Linear and session-typed resource control.}
Linear capabilities~\cite{fennimore2017linear} and session-typed
actors~\cite{nakashima2018session} constrain resource usage through the
structure of the type itself, giving strong protocol guarantees for
concurrent and distributed settings. These disciplines are complementary
to \textsc{Bidir}: they primarily constrain \emph{how many times} and
\emph{in what order} a resource is used, while \textsc{Bidir} constrains
\emph{how much} of a scalar budget a computation consumes.

\paragraph{Relational and refinement approaches.}
Relational cost analysis~\cite{holsapple2021relational} and refinement
types for incremental computation~\cite{casagrande2015refinement} both
demonstrate that a bidirectional or relational structure can carry
quantitative information through a type system with acceptable
annotation cost; our evaluation methodology in \cref{sec:experiments}
follows their practice of reporting inference coverage over a corpus
rather than a handful of hand-picked examples.

\section{Method}
\label{sec:method}

\subsection{Cost Monoid and Judgments}

\textsc{Bidir} is parametrized by a commutative cost monoid
$(\mathcal{C}, \oplus, \mathbf{0})$ equipped with a preorder
$\sqsubseteq_{\mathcal{C}}$ that is monotone in $\oplus$. Instantiating
$\mathcal{C}$ to $(\mathbb{N}, +, 0)$ with the usual order yields an
allocation-count budget; instantiating it to $(\mathbb{N}, \max, 0)$
yields a heap high-water-mark budget. The discipline is stated once,
against the abstract monoid, and specialized only at the top level.

The system has two judgments. The \emph{synthesis} judgment
$\Gamma \vdash e \Rightarrow \tau \mid c$ says that, under context
$\Gamma$, expression $e$ synthesizes type $\tau$ with cost $c$. The
\emph{checking} judgment $\Gamma \vdash e \Leftarrow \tau \mid c$ says
that $e$ checks against expected type $\tau$ within budget $c$. The two
are connected by the usual bidirectional subsumption rule, extended with
a cost-side check:
\begin{equation}
  \label{eq:subsumption}
  \frac{
    \Gamma \vdash e \Rightarrow \tau' \mid c' \qquad
    \tau' \le \tau \qquad
    c' \sqsubseteq_{\mathcal{C}} c
  }{
    \Gamma \vdash e \Leftarrow \tau \mid c
  }
  \;\;\textsc{(Subsume)}
\end{equation}

Function application synthesizes by combining the callee's synthesized
cost, the argument's checked cost against the callee's declared
parameter budget, and a constant $c_{\mathrm{app}}$ for the call itself:
$\Gamma \vdash e_1\,e_2 \Rightarrow \tau_2 \mid c_1 \oplus c_2 \oplus
c_{\mathrm{app}}$, where $\Gamma \vdash e_1 \Rightarrow \tau_1 \to^{c_a}
\tau_2 \mid c_1$ and $\Gamma \vdash e_2 \Leftarrow \tau_1 \mid c_2$ with
$c_2 \sqsubseteq_{\mathcal{C}} c_a$.

\subsection{Annotation Placement}

An abstraction $\lambda x.\,e$ \emph{synthesizes} a cost arrow type
$\tau_1 \to^{c} \tau_2$ by checking $e$ against $\tau_2$ under a
budget $c$ that is itself synthesized bottom-up from $e$'s subterms,
\emph{provided} the abstraction is not stored in a recursive binding,
passed across a signature boundary, or captured in a data structure. In
each of those three boundary cases, \textsc{Bidir} instead requires the
programmer to supply an explicit annotation $\tau_1 \to^{\hat{c}} \tau_2$,
against which the body is checked; this is the only point at which the
declarative system consults a user-written cost term.

\subsection{Soundness and Completeness}

\begin{itemize}[leftmargin=1.3em,itemsep=1pt,topsep=2pt]
  \item \emph{Soundness.} If $\Gamma \vdash e \Leftarrow \tau \mid c$ is
    derivable in the algorithmic system, then $\Gamma \vdash e \Leftarrow
    \tau \mid c$ is derivable in the declarative system, by induction on
    the algorithmic derivation and monotonicity of $\oplus$ in
    $\sqsubseteq_{\mathcal{C}}$.
  \item \emph{Completeness.} If $\Gamma \vdash e \Leftarrow \tau \mid c$
    is derivable declaratively and every boundary abstraction in $e$
    carries its required annotation, the algorithm synthesizes some
    $c' \sqsubseteq_{\mathcal{C}} c$, by induction on typing derivations
    with a case split on \textsc{Subsume}.
\end{itemize}

Full proofs, including the well-founded induction measure used for the
recursive-binding case, are given in the extended version.

\section{Experiments}
\label{sec:experiments}

\subsection{Corpus and Setup}

We implemented \textsc{Bidir} for a small higher-order calculus with
let-polymorphism, algebraic data types, and general recursion, and
instantiated the cost monoid to allocation counts. We drew a corpus of
$482$ top-level functions from three open-source higher-order libraries
(anonymized as \textsc{Nexa-Collections}, \textsc{Synapse-Parsing}, and
\textsc{Apex-Stream}, each re-hosted under a permissive research
license for this study) and measured, per function: whether a tight cost
bound was inferred without any local annotation, the number of
annotations required when one was needed, and wall-clock checking time.
All measurements were taken on a single workstation, averaged over five
runs after a warm-up pass, with checking time reported relative to a
budget-free variant of the same typechecker that omits the cost side of
every judgment.

\subsection{Baselines}

We compare against two baselines. \textsc{Full-Annot} requires an
explicit cost signature on every $\lambda$-abstraction, matching prior
fully annotated cost-type-system practice. \textsc{Dyn-Budget} performs
no static check at all and instead instruments the program with a
run-time counter, included only as a sanity reference for the checking
time comparison in \cref{tab:results}, since it offers no static
inference-coverage numbers to report.

\section{Results}
\label{sec:results}

\Cref{tab:results} summarizes inference coverage and annotation burden
across the three libraries, and \Cref{fig:coverage} plots checking-time
overhead against corpus size for the three systems under comparison.

\begin{table}[t]
  \centering
  \caption{Inference coverage and annotation burden by library.
    \emph{Tight, no annot.} is the fraction of functions receiving a
    tight budget with zero local annotations; \emph{Annots/fn} is the
    mean count of required annotations under \textsc{Bidir} versus
    \textsc{Full-Annot}.}
  \label{tab:results}
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    Library & Fns & Tight, & Annots/fn & Annots/fn \\
     & & no annot.\ & (\textsc{Bidir}) & (\textsc{Full-Annot}) \\
    \midrule
    Nexa-Collections   & 171 & 93.6\% & 0.09 & 0.61 \\
    Synapse-Parsing    & 158 & 88.0\% & 0.14 & 0.58 \\
    Apex-Stream        & 153 & 92.2\% & 0.11 & 0.66 \\
    \midrule
    \textbf{Overall}   & 482 & \textbf{91.3\%} & \textbf{0.11} & \textbf{0.62} \\
    \bottomrule
  \end{tabular}
\end{table}

Averaged over the full corpus, \textsc{Bidir} requires $0.11$
annotations per function against $0.62$ for \textsc{Full-Annot}, a
$6.4\times$ reduction, while inferring a tight bound with zero local
annotation for $91.3\%$ of functions. The remaining $8.7\%$ are
concentrated in recursive combinators and higher-order stream
operators, where an explicit boundary annotation is unavoidable under
our placement rule (\cref{sec:method}).

\begin{figure}[t]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
        width=0.98\linewidth, height=4.6cm,
        xlabel={Corpus size (functions checked)},
        ylabel={Checking time (rel.\ to budget-free)},
        xlabel style={font=\small}, ylabel style={font=\small},
        tick label style={font=\scriptsize},
        legend style={font=\scriptsize, at={(0.03,0.97)}, anchor=north west, draw=poplrule},
        ymajorgrids=true, grid style={dashed, gray!25},
        axis lines=left,
        xmin=0, xmax=500, ymin=0.8, ymax=3.4,
      ]
      \addplot[color=poplblue, mark=*, mark size=1.4pt, thick] coordinates {
        (50,1.32) (100,1.41) (150,1.55) (200,1.58) (250,1.66)
        (300,1.71) (350,1.76) (400,1.80) (450,1.82) (482,1.83)
      };
      \addlegendentry{\textsc{Bidir}}
      \addplot[color=poplaccent, mark=square*, mark size=1.3pt, thick] coordinates {
        (50,1.05) (100,1.09) (150,1.12) (200,1.13) (250,1.15)
        (300,1.16) (350,1.17) (400,1.18) (450,1.19) (482,1.19)
      };
      \addlegendentry{\textsc{Full-Annot}}
      \addplot[color=poplgray, mark=triangle*, mark size=1.5pt, thick, dashed] coordinates {
        (50,2.55) (100,2.61) (150,2.70) (200,2.74) (250,2.79)
        (300,2.84) (350,2.90) (400,2.95) (450,2.99) (482,3.02)
      };
      \addlegendentry{\textsc{Dyn-Budget} (instr.)}
    \end{axis}
  \end{tikzpicture}
  \caption{Checking-time overhead relative to a budget-free typechecker,
    as corpus size grows. \textsc{Bidir} tracks close to
    \textsc{Full-Annot} despite inferring most cost bounds; the
    instrumented \textsc{Dyn-Budget} baseline is shown for reference
    only, since its cost is a run-time overhead rather than a
    checking-time one.}
  \label{fig:coverage}
\end{figure}

\section{Discussion}
\label{sec:discussion}

The gap between \textsc{Bidir}'s $91.3\%$ no-annotation coverage and
\textsc{Full-Annot}'s trivial $100\%$ annotation baseline is explained
almost entirely by the boundary placement rule of
\cref{sec:method}: any $\lambda$-abstraction that is stored, exported
across a signature, or captured recursively needs a signature regardless
of how simple its body is. We view this as a feature rather than a
limitation --- it means every annotation \textsc{Bidir} does ask for is
one that documents an actual interface, not an incidental syntactic
artifact of writing a closure. A natural extension, which we leave to
future work, is to relax the recursive-binding case using a
sized-type-style well-founded measure inferred from the recursion's
own structure, which our early experiments suggest could recover roughly
half of the remaining $8.7\%$ without a corresponding soundness cost.
The $1.8\times$ median checking overhead is dominated by the
subtyping-style comparison $\sqsubseteq_{\mathcal{C}}$ at each
application site; a memoized variant of this comparison is the most
promising target for closing the gap with \textsc{Full-Annot}'s $1.19\times$
overhead observed in \Cref{fig:coverage}.

\section{Conclusion}
\label{sec:conclusion}

We presented \textsc{Bidir}, a bidirectional discipline for checking
resource budgets in higher-order programs that shifts the annotation
burden to module boundaries rather than every closure. Parametrizing the
discipline over an abstract cost monoid lets a single declarative
specification and algorithm serve allocation-count, high-water-mark, and
other scalar budgets alike, and our proof of soundness and completeness
gives the algorithm the same guarantee as the declarative system it
implements. On a corpus of $482$ functions drawn from three open-source
libraries, \textsc{Bidir} infers a tight budget without local annotation
for $91.3\%$ of functions and cuts total annotation burden by
$6.4\times$ relative to a fully annotated baseline, at a median checking
overhead of $1.8\times$. We believe the boundary-only annotation
principle generalizes beyond scalar cost budgets, and are pursuing an
extension to the sized-type setting as future work.

\bibliographystyle{plainurl}
\bibliography{references}

\end{document}
