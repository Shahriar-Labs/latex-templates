% Packages and colours for this template, on top of what IEEEtran already sets.
%
% IEEEtran sets its own fonts (Times), page geometry, section headings and captions, and
% ISCA's format table depends on them, so this file adds nothing that changes those. In
% particular: no geometry, no font packages, no titlesec and no fancyhdr (the page numbers
% come from \pagestyle{plain} in main.tex, as in ISCA's template).

\usepackage{cite}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage[hyphens]{url}
\usepackage{hyperref}
\hypersetup{hidelinks, pdfauthor={}}

\definecolor{iscablue}{RGB}{16,44,87}
\definecolor{iscagrey}{RGB}{108,117,125}
\definecolor{iscaaccent}{RGB}{186,12,47}
