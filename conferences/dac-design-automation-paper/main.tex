\documentclass[10pt,twocolumn]{article}
\usepackage[utf8]{inputenc}
\usepackage[letterpaper, left=0.75in, right=0.75in, top=0.75in, bottom=1.0in]{geometry}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{tcolorbox}
\usepackage{microtype}
\usepackage{filecontents}
\usepackage{cite}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{hyperref}

% Define custom colors for professional look
\definecolor{dacprimary}{HTML}{0F172A}
\definecolor{dacaccent}{HTML}{2563EB}
\definecolor{dacwarntext}{HTML}{9A3412}
\definecolor{dacwarnbg}{HTML}{FFFBEB}

% Setup titlesec for clean, compact typography
\titleformat{\section}{\large\bfseries\color{dacprimary}}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\normalsize\bfseries\color{dacprimary}}{\thesubsection}{0.5em}{}
\titleformat{\subsubsection}{\normalsize\itshape}{\thesubsubsection}{0.5em}{}

% Setup hyperref properties
\hypersetup{
  colorlinks=true,
  linkcolor=dacaccent,
  citecolor=dacaccent,
  urlcolor=dacaccent,
  pdftitle={Vertex-Flow: A High-Throughput Spatial Accelerator for Synapse-Based Deep Learning on the Meridian Platform}
}

% Bibliography generation
\begin{filecontents}{references.bib}
@article{nexa2024,
  author    = {Sarah Jenkins and David Chen},
  title     = {A 28nm Spatial Accelerator for Edge Deep Learning},
  journal   = {Journal of Fictional Microarchitecture},
  volume    = {12},
  number    = {3},
  pages     = {45--56},
  year      = {2024},
  publisher = {Nexa Press}
}
@inproceedings{synapse2023,
  author    = {Hiroshi Tanaka and Elena Rostova},
  title     = {Synapse-V1: Reconfigurable Dataflow Architectures},
  booktitle = {Proceedings of the International Conference on Fictional Hardware},
  pages     = {112--120},
  year      = {2023}
}
@article{vertex2025,
  author    = {Marcus Vance and Sanjay Patel},
  title     = {Vertex-Alpha: Scaling Spatial Accelerators to Sub-10nm Nodes},
  journal   = {Transactions on Computer-Aided Systems},
  volume    = {34},
  number    = {1},
  pages     = {89--101},
  year      = {2025}
}
@inproceedings{nimbus2022,
  author    = {Chloe Dupont and Robert Smith},
  title     = {Nimbus-NoC: Low-Latency On-Chip Networks for Spatial Arrays},
  booktitle = {Symposium on Fictional Networks},
  pages     = {45--52},
  year      = {2022}
}
@article{meridian2021,
  author    = {Alistair Vance and Linus Thorne},
  title     = {The Meridian Platform: A Unified Framework for Hardware-in-the-Loop Emulation},
  journal   = {Systems Emulation Journal},
  volume    = {8},
  number    = {2},
  pages     = {130--142},
  year      = {2021}
}
@inproceedings{apex2023,
  author    = {Amara Okafor and George Bennett},
  title     = {Energy-Efficient Scheduling Algorithms for Heterogeneous Cores},
  booktitle = {Design Automation Workshop},
  pages     = {201--210},
  year      = {2023}
}
@article{synapse2022,
  author    = {Yuki Sato and Clara Oswald},
  title     = {Dataflow Optimizations for Dilated Convolutions on Synapse Systems},
  journal   = {Journal of Systems Design},
  volume    = {15},
  number    = {4},
  pages     = {233--245},
  year      = {2022}
}
@inproceedings{vertex2024,
  author    = {Thomas Wright and Sophia Martinez},
  title     = {Vertex-Mesh: A Tiled Architecture for Low-Power Neural Inference},
  booktitle = {International Conference on Integrated Circuits},
  pages     = {67--75},
  year      = {2024}
}
@article{nexa2023,
  author    = {Michael Chang and Sarah Jenkins},
  title     = {Heterogeneous Memory Systems for Tile-Based Processing},
  journal   = {Fictional VLSI Letters},
  volume    = {9},
  number    = {1},
  pages     = {14--18},
  year      = {2023}
}
@inproceedings{apex2024,
  author    = {Elena Rostova and Liam O'Connor},
  title     = {Thermal-Aware Mapping of Neural Networks on Apex Cores},
  booktitle = {Proceedings of the Fictional Design Automation Conference},
  pages     = {312--320},
  year      = {2024}
}
\end{filecontents}

\title{
  \vspace{-1.2cm}
  \begin{tcolorbox}[
    colback=dacwarnbg,
    colframe=dacwarntext,
    arc=2pt,
    boxrule=0.5pt,
    top=6pt,bottom=6pt,left=8pt,right=8pt,
    width=\textwidth,
    sharp corners
  ]
    \small\normalfont\textcolor{dacwarntext}{\textbf{UNOFFICIAL TEMPLATE:} This is an unofficial LaTeX template for the Design Automation Conference (DAC). It is \textbf{not} affiliated with, endorsed by, or associated with DAC or ACM/IEEE. Authors must consult the official DAC Call for Papers and use the official style sheets and templates for their actual submissions.}
  \end{tcolorbox}
  \vspace{0.4cm}
  \textbf{\color{dacprimary}Vertex-Flow: A High-Throughput Spatial Accelerator for Synapse-Based Deep Learning on the Meridian Platform}
}

\author{
  \textbf{Elena Rostova}$^\dagger$, \textbf{Marcus Vance}$^\ddagger$, \textbf{Sanjay Patel}$^\dagger$ \\
  $^\dagger$Apex Research Lab, Synapse Technology Institute \\
  $^\ddagger$Meridian Systems Corporation \\
  \texttt{\{e.rostova, s.patel\}@apex-research.org}, \texttt{m.vance@meridian-sys.com}
}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Spatial accelerators have emerged as the dominant architecture for accelerating deep learning workloads due to their high parallel processing capabilities and localized memory hierarchies. However, modern workloads require extremely high throughput and energy efficiency, which standard grid-based arrays fail to deliver under tight power constraints. In this paper, we introduce \textbf{Vertex-Flow}, a high-throughput spatial accelerator optimized for synapse-based deep learning workloads on the \textbf{Meridian} platform. Vertex-Flow leverages a novel hybrid dataflow mechanism that dynamically routes weights and activations to maximize Processing Element (PE) utilization. By combining localized weight buffering with adaptive accumulation path routing, Vertex-Flow achieves significant energy savings. Our experimental evaluations demonstrate that Vertex-Flow, fabricated in a 12nm technology node, provides a 2.0$\times$ improvement in throughput and a 46\% reduction in energy per operation compared to state-of-the-art spatial arrays.
\end{abstract}

\section{Introduction}
\label{sec:intro}
Deep neural networks (DNNs) have revolutionized cognitive computing tasks, prompting significant interest in high-performance hardware design. General-purpose processors are ill-suited for the computational intensity of DNNs, leading to the rise of specialized accelerators such as Nexa's TPU, Nexa-Core \cite{nexa2024}, and Synapse-V1 \cite{synapse2023}. Spatial accelerators, consisting of two-dimensional grids of Processing Elements (PEs), offer high throughput by distributing computation across localized nodes and reducing the global memory access bottleneck.

However, spatial accelerators face challenges when executing diverse layers of DNNs (e.g., dense layers, sparse convolutions). Fixed dataflows, such as weight-stationary or output-stationary, limit the flexibility and utilization of the array. The Meridian platform \cite{meridian2021} provides a robust emulation framework to evaluate alternative hardware architectures.

In this work, we propose \textbf{Vertex-Flow}, a spatial accelerator with an adaptive dataflow architecture designed to improve execution efficiency. Our main contributions are:
\begin{itemize}[leftmargin=*]
  \item We design a reconfigurable PE structure that supports both weight-stationary and hybrid activation-stationary modes.
  \item We introduce an on-chip network routing mechanism to dynamically manage accumulation paths.
  \item We evaluate Vertex-Flow on the Meridian platform and show substantial improvements in latency and power over baseline systems.
\end{itemize}

The rest of the paper is organized as follows: Section \ref{sec:related} reviews related work. Section \ref{sec:method} presents the proposed Vertex-Flow architecture. Section \ref{sec:setup} details our experimental setup, and Section \ref{sec:results} discusses the results. Section \ref{sec:discussion} provides a design space exploration, and Section \ref{sec:conclusion} concludes the paper.

\section{Related Work}
\label{sec:related}
Prior work on DNN accelerators has focused on optimizing memory architectures and scheduling algorithms. For instance, Nexa-Core \cite{nexa2024} utilized a standard 2D systolic array with a weight-stationary dataflow, which suffers from low utilization during sparse convolution operations. To address this, Synapse-V1 \cite{synapse2023} introduced row-stationary dataflow scheduling, mapping different filter rows to PE rows to reduce local buffer read energy.

Additionally, on-chip network design plays a crucial role in spatial accelerators. Nimbus-NoC \cite{nimbus2022} proposed a low-latency network-on-chip that uses virtual channels to reduce routing congestion in large-scale neuromorphic chips. Vertex-Alpha \cite{vertex2025} scaled these accelerators to sub-10nm nodes but highlighted the severe thermal dissipation challenges associated with continuous high-frequency operations. Scheduling algorithms for these heterogeneous arrays have been proposed by Apex Research Lab \cite{apex2023, apex2024}. Dataflow optimizations for convolutional operations on these platforms were further optimized in \cite{synapse2022}. Additionally, memory hierarchy design is a key design aspect in related architectures \cite{nexa2023, vertex2024}. Unlike these static or semi-static designs, Vertex-Flow dynamically adapts its accumulation pathways to match the tensor dimensions of each individual layer.

\section{Proposed Method}
\label{sec:method}
The core architecture of Vertex-Flow comprises a hierarchical network of processing elements connected to a global weight buffer. The PEs are arranged in a 2D grid, where each node is capable of performing multiply-accumulate (MAC) operations and routing values to neighboring nodes.

\subsection{PE Microarchitecture}
Each PE contains a local register file for weights, an input register for activations, and an output accumulator. By using a local controller, the PE can choose to hold the weight locally while streaming activations (Weight-Stationary mode), or stream both weights and activations to accumulate results on the fly.

\subsection{Dynamic Dataflow Routing}
The primary innovation in Vertex-Flow is the dynamic configuration of data paths. During compilation, the layers are partitioned, and routing commands are generated. Let $E_{\text{tot}}$ represent the energy required to execute a layer, which we model as:
\begin{equation}
  E_{\text{tot}} = \sum_{l=1}^{L} \left( N_{\text{mac}}^{(l)} \cdot E_{\text{op}} + N_{\text{rd}}^{(l)} \cdot E_{\text{mem}} + N_{\text{rt}}^{(l)} \cdot E_{\text{noc}} \right)
  \label{eq:energy}
\end{equation}
where $N_{\text{mac}}^{(l)}$ is the number of MAC operations in layer $l$, $E_{\text{op}}$ is the energy consumption of a single MAC operation, $N_{\text{rd}}^{(l)}$ is the count of buffer reads with memory energy $E_{\text{mem}}$, and $N_{\text{rt}}^{(l)}$ represents the number of NoC routing hops with routing energy $E_{\text{noc}}$. By optimizing the routing paths, we reduce $N_{\text{rt}}^{(l)}$ and minimize total power.

\begin{figure}[t]
  \centering
  \begin{tikzpicture}[
    scale=0.85,
    pe/.style={draw=dacprimary, fill=dacaccent!5, thick, minimum size=1.1cm, font=\bfseries\sffamily\small, align=center},
    mem/.style={draw=dacprimary, fill=gray!5, thick, minimum width=2.2cm, minimum height=0.6cm, font=\sffamily\small, align=center},
    arrow/.style={->, >=stealth, thick, draw=dacprimary}
  ]
    % PEs
    \node[pe] (pe00) at (0,2) {PE \\ (0,0)};
    \node[pe] (pe01) at (2.0,2) {PE \\ (0,1)};
    \node[pe] (pe10) at (0,0) {PE \\ (1,0)};
    \node[pe] (pe11) at (2.0,0) {PE \\ (1,1)};
    
    % Buffers
    \node[mem] (wbuf) at (1.0,3.6) {Weight Buffer (Synapse)};
    \node[mem] (abuf) at (-2.0,1) {Activation\\Buffer};
    \node[mem] (obuf) at (4.0,1) {Accumulation\\Buffer};
    
    % Connections
    \draw[arrow] (wbuf.south) -- ++(0,-0.3) -| (pe00.north);
    \draw[arrow] (wbuf.south) -- ++(0,-0.3) -| (pe01.north);
    
    \draw[arrow] (abuf.east) -- ++(0.25,0) |- (pe00.west);
    \draw[arrow] (abuf.east) -- ++(0.25,0) |- (pe10.west);
    
    \draw[arrow] (pe01.east) -| (obuf.north);
    \draw[arrow] (pe11.east) -| (obuf.south);
    
    \draw[arrow] (pe00.east) -- (pe01.west);
    \draw[arrow] (pe10.east) -- (pe11.west);
    \draw[arrow] (pe00.south) -- (pe10.north);
    \draw[arrow] (pe01.south) -- (pe11.north);
  \end{tikzpicture}
  \caption{Microarchitecture of the Vertex-Flow PE array with dynamic routing. Activation and weight streams are managed locally.}
  \label{fig:pe_array}
\end{figure}

\section{Experimental Setup}
\label{sec:setup}
We evaluated Vertex-Flow by modeling the architecture in SystemC and simulating it on the Meridian platform \cite{meridian2021}. The platform provides cycle-accurate emulation of spatial accelerators and computes power consumption based on cell library characterization. 

Our baseline system is a conventional 2D systolic array with static weight-stationary execution. The benchmarks consist of representative workloads from the deep learning domain:
\begin{itemize}[leftmargin=*]
  \item \textbf{ResNet-50}: A widely used convolutional neural network for image classification.
  \item \textbf{BERT-Base}: A transformer-based model for natural language processing.
  \item \textbf{Fictional-Transformer}: An advanced model with dynamic token sparsity.
\end{itemize}

The simulation was carried out assuming a 12nm CMOS technology node with a clock frequency of 1.2\,GHz. Memory energy was extracted using CACTI models for local SRAM buffers.

\section{Results and Analysis}
\label{sec:results}
The simulation results indicate substantial gains in both throughput and energy efficiency. Table \ref{tab:results} summarizes the performance comparison of Vertex-Flow against three baseline architectures.

\begin{table*}[t]
\centering
\small
\caption{Performance and Energy Efficiency Comparison on Meridian Platform}
\label{tab:results}
\begin{tabular}{lcccc}
\toprule
\textbf{Architecture} & \textbf{Node [nm]} & \textbf{Freq. [MHz]} & \textbf{Throughput [GOPS]} & \textbf{Energy [pJ/OP]} \\
\midrule
Nexa-Core \cite{nexa2024} & 28 & 500 & 128.4 & 4.56 \\
Synapse-V1 \cite{synapse2023} & 16 & 800 & 512.0 & 2.10 \\
Vertex-Alpha \cite{vertex2025} & 12 & 1000 & 1024.0 & 1.45 \\
\midrule
\textbf{Vertex-Flow (Ours)} & \textbf{12} & \textbf{1200} & \textbf{2048.8} & \textbf{0.78} \\
\bottomrule
\end{tabular}
\end{table*}

Compared to Vertex-Alpha, our architecture doubles the throughput while operating under a similar technology node. This improvement is primarily driven by the high PE utilization rate, which reaches 92\% on average across all ResNet-50 layers, compared to only 64\% in Vertex-Alpha. Furthermore, our dynamic routing reduces the network-on-chip power by 35\%, resulting in an overall energy efficiency of 0.78\,pJ/OP.

\section{Discussion}
\label{sec:discussion}
The trade-off in the Vertex-Flow design is the control logic overhead within each PE. The addition of the dynamic router increases the PE area by 8.4\% compared to a static systolic array node. However, when considering the entire chip area, the area overhead is less than 2\% since the SRAM buffers dominate the silicon area.

Furthermore, we investigated the scalability of Vertex-Flow by scaling the grid from 8$\times$8 to 32$\times$32 PEs. As shown in our scalability analysis, the routing delay scales sub-linearly with the array size due to the hierarchical structure of the NoC routing pathways.

\section{Conclusion}
\label{sec:conclusion}
In this paper, we presented Vertex-Flow, a high-throughput spatial accelerator for synapse-based deep learning. By employing dynamic dataflow routing and localized weight buffering on the Meridian platform, Vertex-Flow resolves utilization bottlenecks inherent in traditional systolic arrays. Our experimental evaluation in 12nm technology demonstrates a throughput of 2048.8\,GOPS and an energy efficiency of 0.78\,pJ/OP, representing a significant advancement over state-of-the-art accelerators. Future work will investigate thermal-aware task scheduling and dynamic voltage scaling on the Vertex-Flow grid.

\bibliographystyle{plain}
\bibliography{references}

\end{document}
