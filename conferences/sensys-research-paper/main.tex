% Self-contained, anonymous ACM SenSys manuscript.
% Compile with: latexmk -pdf -interaction=nonstopmode main.tex
\documentclass[sigconf,review,anonymous]{acmart}
% Review copies need not balance the final reference columns.
\makeatletter
\@ACM@balancefalse
\makeatother

\usepackage{mathtools}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{microtype}
\usepackage{xspace}
\usepackage{enumitem}
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,calc,fit,positioning,shapes.geometric}
\usepackage{pgfplots}
\usepgfplotslibrary{groupplots}
\pgfplotsset{compat=1.18}

% Review-copy metadata. Replace these fields only after acceptance.
\setcopyright{none}
\settopmatter{printacmref=true,printfolios=true}
\renewcommand\footnotetextcopyrightpermission[1]{}
\acmConference[SenSys '26]{ACM Conference on Embedded Artificial Intelligence and Sensing Systems}{2026}{United States}
\acmBooktitle{ACM Conference on Embedded Artificial Intelligence and Sensing Systems (SenSys '26), 2026, United States}
\acmDOI{}
\acmISBN{}

\definecolor{tsblue}{HTML}{236B8E}
\definecolor{tsorange}{HTML}{D97706}
\definecolor{tsgreen}{HTML}{3B7D44}
\definecolor{tsgray}{HTML}{5F6B73}
\definecolor{tspale}{HTML}{EAF2F5}

\newcommand{\system}{\textsc{TideSense}\xspace}
\newcommand{\eg}{e.g.,\xspace}
\newcommand{\ie}{i.e.,\xspace}
\newcommand{\etal}{et al.\xspace}
\newcommand{\Energy}{E_{\mathrm{day}}}
\newcommand{\Recall}{R_{\mathrm{event}}}
\DeclareMathOperator*{\argmin}{arg\,min}

\title{TideSense: Uncertainty-Aware Energy Budgeting for Long-Lived Edge Sensing}

\author{Anonymous Author(s)}
\affiliation{%
  \institution{Anonymous Institution}
  \country{Anonymous}}

\begin{document}

\begin{abstract}
Battery-powered sensing systems are commonly configured around a fixed sampling
period, even though the value of a measurement varies sharply over time.  A
short period captures transitions but wastes energy during steady state; a long
period extends lifetime but misses brief events.  We present \system, an
uncertainty-aware scheduler that turns sensing into an explicit, online energy
allocation problem.  Each node maintains a compact predictor, converts model
uncertainty, recent surprise, and sample age into an event-risk score, and uses
a token-bucket governor to select the next sensing time while respecting a
multi-month energy budget.  A sparse neighbor digest lets nodes suppress
redundant samples without exchanging raw measurements.

We evaluate the design in a reproducible trace-driven simulator across five
environmental and infrastructure workloads, including abrupt, periodic, and
slow-drift signals.  At matched event recall, \system reduces simulated daily
energy by 47--63\% relative to periodic sampling and by 18--31\% relative to
single-signal adaptive baselines.  Under a 2.4\,kJ/day budget it preserves
96.1\% event recall, bounds the 99th-percentile detection delay to 18.4\,s, and
reduces radio traffic by 39\%.  Ablations show that uncertainty and budget
feedback are complementary: either one alone fails under distribution shift.
These results identify a small, interpretable control surface for making
long-lived sensing responsive without placing a neural model or a continuous
radio link on every node.
\end{abstract}

\begin{CCSXML}
<ccs2012>
 <concept>
  <concept_id>10003033.10003079.10003082</concept_id>
  <concept_desc>Networks~Sensor networks</concept_desc>
  <concept_significance>500</concept_significance>
 </concept>
 <concept>
  <concept_id>10010520.10010553.10010562</concept_id>
  <concept_desc>Computer systems organization~Embedded systems</concept_desc>
  <concept_significance>500</concept_significance>
 </concept>
 <concept>
  <concept_id>10010520.10010575.10010755</concept_id>
  <concept_desc>Computer systems organization~Redundancy</concept_desc>
  <concept_significance>300</concept_significance>
 </concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Networks~Sensor networks}
\ccsdesc[500]{Computer systems organization~Embedded systems}
\ccsdesc[300]{Computer systems organization~Redundancy}

\keywords{adaptive sampling, energy harvesting, embedded sensing, edge intelligence, uncertainty estimation}

\maketitle

% --- Unofficial-template notice (required: this is not an official SenSys file) ---
\noindent\fbox{\parbox{\dimexpr\columnwidth-2\fboxsep-2\fboxrule\relax}{%
  \footnotesize\textbf{Unofficial template.} This layout is provided for convenience and is
  \textbf{not} affiliated with, endorsed by, or sponsored by SenSys or its organisers.
  Always check the official call for papers and use the current year's official style files
  for a real submission.}}
\vspace{0.8em}



\section{Introduction}
\label{sec:introduction}

Long-lived sensor deployments operate under a basic mismatch: the environment
is bursty, but their schedules are static.  A bridge can remain quiescent for
hours before a short vibration transient; indoor air quality changes slowly
until a door opens; and soil moisture drifts until rain produces a step.  A
deployment configured for the transient repeatedly measures predictable steady
state.  One configured for the steady state discovers the event too late.
This tension is especially costly on energy-harvesting and hard-to-service
nodes, where a sampling decision consumes not only sensor energy but also CPU
wake-up and radio-tail energy~\cite{lorincz2004sensor,lin2010energy}.

Adaptive sampling is not new.  Prior systems use prediction error, compressive
sensing, event triggers, or application-specific rules to vary a sampling
rate~\cite{deshpande2004model,raghunathan2006energyscavenging,jain2004duality,lu2010smart}.  Three practical gaps remain.  First, a residual is meaningful only when paired with the model's uncertainty: a
2$^\circ$ error can be routine during a volatile afternoon and alarming at
night.  Second, a locally sensible rate can still exhaust a finite daily energy
budget after an extended event.  Third, correlated nodes often make the same
decision and report redundant evidence.  Existing techniques commonly address
one gap while treating the others as configuration problems.

We ask a narrower systems question: \emph{can a node allocate a fixed energy
budget to the moments at which one additional observation is most likely to
matter?}  \system answers with a closed-loop scheduler whose state fits in 184
bytes and whose update requires 1.7k integer-equivalent operations.  It
combines a constant-velocity predictor with an exponentially weighted error
model.  The resulting risk score controls a continuous sampling interval.  A
token bucket stretches that interval when the node spends energy too quickly,
while hard age and change-point guards retain responsiveness.  Finally, a
four-byte neighbor digest discounts observations that are likely to be
spatially redundant.

The design intentionally avoids a heavyweight learned policy.  This makes the
decision path inspectable, permits safe bounds on sample age, and lets one set
an energy budget without retraining.  The trade-off is that \system is a
scheduler, not a semantic event classifier: it decides \emph{when} to observe,
then invokes the deployment's existing detector.

This paper makes three contributions:

\begin{itemize}[leftmargin=*,itemsep=2pt,topsep=3pt]
  \item We formulate sampling as online, risk-weighted budget allocation and
  derive a bounded interval controller that combines uncertainty, surprise,
  staleness, and spatial redundancy (\S\ref{sec:design}).
  \item We design a budget governor and neighbor protocol that tolerate energy
  droughts, event storms, loss, and cold starts without requiring a gateway in
  the control loop (\S\ref{sec:implementation}).
  \item We provide a trace-driven evaluation spanning five signal regimes and
  report energy, event recall, delay, traffic, sensitivity, and failure modes
  (\S\ref{sec:evaluation}).
\end{itemize}

All results in this manuscript are from deterministic trace replay and the
hardware energy model in Table~\ref{tab:platform}; they are not presented as a
field deployment.  This distinction makes the evidence reproducible and keeps
the conclusions scoped to scheduling behavior.

\section{Motivation and Problem}
\label{sec:motivation}

\subsection{Why one sampling rate is insufficient}

We begin with a seven-day replay of three representative signals.  For each
signal we sweep fixed periods from 2 to 300\,s and run the same event detector
over the resulting observations.  Figure~\ref{fig:motivation} shows that no
period dominates.  Vibration requires dense observations to capture a short
impulse, temperature retains high recall at moderate periods, and soil moisture
is inexpensive until a rain onset lands between samples.  Provisioning every
signal for the worst case spends 2.3--4.1$\times$ the energy needed by an oracle
that knows transition windows in advance.

\begin{figure}[t]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=\columnwidth,height=4.8cm,
      xmode=log,log basis x=10,
      xmin=1.7,xmax=360,ymin=0.45,ymax=1.02,
      xlabel={Fixed sampling period (s)},ylabel={Event recall},
      grid=both,minor grid style={gray!12},major grid style={gray!25},
      legend style={font=\scriptsize,draw=none,fill=none,at={(0.03,0.04)},anchor=south west},
      tick label style={font=\scriptsize},label style={font=\small}]
      \addplot+[color=tsblue,mark=*,thick] coordinates
        {(2,1.00) (5,.99) (10,.97) (20,.91) (30,.84) (60,.69) (120,.55) (300,.48)};
      \addlegendentry{Vibration}
      \addplot+[color=tsorange,mark=square*,thick,dashed] coordinates
        {(2,1.00) (5,1.00) (10,.99) (20,.98) (30,.97) (60,.93) (120,.85) (300,.68)};
      \addlegendentry{Temperature}
      \addplot+[color=tsgreen,mark=triangle*,thick,dashdotted] coordinates
        {(2,1.00) (5,.99) (10,.99) (20,.97) (30,.95) (60,.89) (120,.78) (300,.61)};
      \addlegendentry{Soil moisture}
    \end{axis}
  \end{tikzpicture}
  \caption{Fixed sampling exposes a workload-dependent energy--fidelity
  trade-off.  Recall is measured against events detected in the full-rate
  trace; higher is better.}
  \Description{Line chart of event recall versus fixed sampling period for
  vibration, temperature, and soil moisture. Recall falls fastest for
  vibration as the sampling period increases.}
  \label{fig:motivation}
\end{figure}

Prediction error alone does not close this gap.  Before an event, the most
useful quantity is often uncertainty: error can only be measured \emph{after} a
sample is taken.  Conversely, uncertainty alone can remain low when a biased
model confidently follows a drift.  A robust scheduler needs both signals and a
mechanism that reconciles them with remaining energy.

\subsection{Objective and assumptions}

Node $i$ chooses observation times $\mathcal{T}_i$ over a horizon $H$.  A
sample at time $t$ has energy cost $e_s(t)$, including sensing, computation, and
amortized communication.  Let $m_k\in\{0,1\}$ indicate whether ground-truth
event $k$ is missed, and let $d_k$ be its detection delay.  We seek a causal
policy $\pi$ that approximately solves

\begin{equation}
\label{eq:objective}
  \argmin_{\pi}\; \sum_k \left(\lambda_m m_k+\lambda_d d_k\right)
  \quad \text{s.t.}\quad
  \sum_{t\in\mathcal{T}_i} e_s(t) \leq B_i(H),
\end{equation}

where $B_i(H)$ is energy available from the battery and harvester.  Exact
optimization is impossible online because future events, harvest, and link
quality are unknown.  \system therefore tracks a short-horizon surrogate for
the marginal value of a sample and enforces the constraint through feedback.

We assume each node has a monotonic clock, can estimate the energy of its
operating states, and exposes a scalar feature to schedule.  The feature can be
a raw sensor channel, an FFT band power, or a classifier margin.  Nodes need not
be synchronized tightly: neighbor digests use coarse epochs.  We do not assume
continuous connectivity, a stationary signal, or foreknowledge of events.

\section{Design}
\label{sec:design}

Figure~\ref{fig:architecture} summarizes the control loop.  Every observation
updates a predictor and an error scale.  Their state, sample age, and optional
neighbor evidence form a risk score.  The interval controller proposes the
next deadline; the budget governor then slows or accelerates that deadline
according to accumulated energy debt.  A watchdog enforces hard safety bounds.

\begin{figure*}[t]
  \centering
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      font=\small,
      node distance=7mm and 9mm,
      block/.style={draw,rounded corners=2pt,minimum height=9mm,minimum width=25mm,align=center,fill=tspale},
      guard/.style={draw,diamond,aspect=2.2,inner sep=1.5pt,align=center,fill=orange!12},
      line/.style={-{Latex[length=2mm]},thick},
      feedback/.style={-{Latex[length=2mm]},thick,dashed,color=tsgray}]
    \node[block,fill=blue!9] (sensor) {Sensor / feature\\front end};
    \node[block,right=of sensor] (predict) {Predictor\\$\hat{x},P$};
    \node[block,right=of predict] (risk) {Risk fusion\\$u_t$};
    \node[block,right=of risk] (interval) {Interval map\\$\tau_t^{\star}$};
    \node[block,right=of interval] (budget) {Budget governor\\$b_t,g_t$};
    \node[guard,right=of budget] (watch) {Safety\\guard};
    \node[block,right=of watch,fill=green!10] (timer) {Wake timer\\and radio};
    \draw[line] (sensor) -- node[above,font=\scriptsize]{sample} (predict);
    \draw[line] (predict) -- node[above,font=\scriptsize]{state} (risk);
    \draw[line] (risk) -- (interval);
    \draw[line] (interval) -- (budget);
    \draw[line] (budget) -- (watch);
    \draw[line] (watch) -- (timer);
    \draw[feedback] (timer.south) -- ++(0,-8mm) -| node[pos=.25,below,font=\scriptsize]{energy debit} (budget.south);
    \draw[feedback] (timer.north) -- ++(0,7mm) -| node[pos=.25,above,font=\scriptsize]{next observation} (sensor.north);
    \node[block,below=13mm of risk,minimum width=31mm,fill=yellow!12] (neighbor) {Neighbor digest\\$q_t,a_t$};
    \draw[line] (neighbor) -- (risk);
    \node[draw,dotted,rounded corners,fit=(predict)(risk)(interval),inner sep=4mm,label={[font=\scriptsize]below:184-byte scheduler state}] {};
  \end{tikzpicture}
  }
  \caption{\system's data and control paths.  Solid arrows carry signal state;
  dashed arrows close the timing and energy loops.  The scheduler never blocks
  on neighbor or gateway communication.}
  \Description{Block diagram from sensor front end through predictor, risk
  fusion, interval map, budget governor, safety guard, and wake timer. A
  neighbor digest enters risk fusion, while energy and next-observation arrows
  form feedback loops.}
  \label{fig:architecture}
\end{figure*}

\subsection{Compact uncertainty model}

For a scalar scheduling feature $x_t$, the node uses a constant-velocity state
$z_t=[x_t,\dot{x}_t]^\mathsf{T}$.  Between irregular observations separated by
$\Delta t$, it predicts

\begin{equation}
\hat{z}_{t|t^-}=
\begin{bmatrix}1&\Delta t\\0&1\end{bmatrix}\hat{z}_{t^-},\qquad
P_{t|t^-}=A P_{t^-}A^\mathsf{T}+Q(\Delta t).
\end{equation}

The observation update is the usual two-state Kalman correction, but \system
adapts measurement scale from the clipped residual
$r_t=x_t-\hat{x}_{t|t^-}$.  With forgetting factor $\rho$,

\begin{equation}
\label{eq:residual}
  s_t^2=\rho s_{t^-}^2+(1-\rho)\min(r_t^2,c^2s_{t^-}^2).
\end{equation}

Clipping prevents a single event from inflating uncertainty for hours.  We use
$\rho=0.94$ and $c=4$ by default.  Fixed-point implementations store $P$ and
$s^2$ in Q16.16; positive diagonal floors prevent collapse after long steady
periods.

\subsection{Risk fusion}

At time $t$, four normalized terms estimate the value of sampling:

\begin{align}
U_t &= \min\!\left(1,\sqrt{P_{xx,t}}/\sigma_U\right), &
S_t &= \min\!\left(1,|r_{t^-}|/(c s_{t^-})\right),\\
A_t &= \min\!\left(1,(t-t_{\mathrm{last}})/\tau_{\max}\right), &
D_t &= q_t\exp(-a_t/\tau_n).
\end{align}

$U_t$ measures forecast uncertainty, $S_t$ retains recent surprise, and $A_t$
prevents starvation.  $D_t$ is a redundancy discount: $q_t$ is the similarity
to the freshest neighbor digest and $a_t$ its age.  Missing or stale digests set
$D_t=0$, making coordination fail open.  The fused risk is

\begin{equation}
\label{eq:risk}
u_t=\operatorname{clip}_{[0,1]}
 \left(w_U U_t+w_S S_t+w_A A_t-w_D D_t\right).
\end{equation}

The weights sum to one before the discount.  We use
$w_U=.42$, $w_S=.28$, $w_A=.30$, and $w_D=.20$.  These defaults were selected
on a workload disjoint from evaluation and are held fixed across traces.
Unlike a threshold controller,
the continuous score avoids oscillating between two rates.

\subsection{From risk to an interval}

The unconstrained interval decays exponentially with risk:

\begin{equation}
\label{eq:interval}
\tau_t^{\star}=\tau_{\min}+
(\tau_{\max}-\tau_{\min})\exp(-\kappa u_t),
\end{equation}

with $\kappa=4.6$.  Exponential mapping dedicates most resolution to the
high-risk region, where a small delay matters.  The application specifies
$\tau_{\min}$ from its shortest relevant event and $\tau_{\max}$ from its
maximum tolerable staleness.  We use 2 and 300\,s.

The mapping is intentionally monotone and bounded.  If two histories differ
only in having larger $U_t$, $S_t$, or $A_t$, the one with higher risk cannot be
sampled later.  More importantly, even a wrongly confident model receives an
observation within $\tau_{\max}$.

\subsection{Energy-budget feedback}
\label{sec:budget}

A risk-only controller can spend a day's energy in an hour-long disturbance.
We regulate expenditure with a token bucket $b_t\in[-B_c,B_c]$.  Tokens arrive
at target power $p_b$ and measured or modeled operation energy $e_t$ removes
them:

\begin{equation}
\label{eq:bucket}
b_t=\operatorname{clip}_{[-B_c,B_c]}
\left(b_{t^-}+p_b\Delta t-e_t\right).
\end{equation}

Negative balance creates a multiplicative slowdown
\[
  g_t=\exp\!\left(\eta\max(0,-b_t/B_c)\right).
\]
Positive balance does not speed the node
beyond the risk-selected rate; it is reserve for future events.  The scheduled
interval is

\begin{equation}
\label{eq:finalinterval}
\tau_t=\min\!\left(\tau_{\max},
\max\!\left(\tau_{\min},g_t\tau_t^{\star}\right)\right).
\end{equation}

Two overrides protect fidelity.  A detector trigger pins $\tau_t$ to
$\tau_{\min}$ for a confirmation window $W$, while $A_t=1$ forces a sample even
under energy debt.  Thus the budget is a soft horizon constraint, not a reason
to violate application safety bounds.  If available energy is insufficient
even for one sample per $\tau_{\max}$, the node reports infeasibility rather
than silently degrading.

\subsection{Spatial suppression}

Every $K$th report includes a four-byte digest: a 12-bit quantized feature, a
10-bit slope, a 6-bit uncertainty bin, and a 4-bit epoch.  A node compares the
digest with its own prediction and estimates $q_t$ from normalized feature and
slope distance.  It suppresses only routine reports; event onset and recovery
messages are never suppressed.  Digests are piggybacked on existing frames, so
coordination adds no packet when traffic already exists.

This mechanism differs from cluster-head aggregation~\cite{heinzelman2000energy}
and model-driven acquisition~\cite{deshpande2004model}: there is no elected
leader and no shared global model.  A node can reboot, move, or lose all
neighbors and still run the same local policy.

\begin{algorithm}[t]
\small
\caption{One \system scheduler update}
\label{alg:scheduler}
\DontPrintSemicolon
\KwIn{observation $x_t$, elapsed time $\Delta t$, operation energy $e_t$}
\KwOut{next interval $\tau_t$}
Predict $\hat{z}_{t|t^-}$ and $P_{t|t^-}$ over $\Delta t$\;
$r_t\leftarrow x_t-\hat{x}_{t|t^-}$; update $s_t^2$ using Eq.~\eqref{eq:residual}\;
Correct $\hat{z}_t,P_t$ with $x_t$\;
Read freshest neighbor digest; set $D_t\leftarrow0$ if stale\;
Compute $u_t$ using Eq.~\eqref{eq:risk}\;
$\tau_t^\star\leftarrow\tau_{\min}+(\tau_{\max}-\tau_{\min})e^{-\kappa u_t}$\;
$b_t\leftarrow\operatorname{clip}(b_{t^-}+p_b\Delta t-e_t)$\;
$\tau_t\leftarrow\operatorname{clip}(e^{\eta\max(0,-b_t/B_c)}\tau_t^\star)$\;
\If{event pending or maximum age reached}{$\tau_t\leftarrow\tau_{\min}$}
Arm wake timer for $\tau_t$ and persist scheduler state\;
\end{algorithm}

\section{Implementation}
\label{sec:implementation}

We implement the scheduler as a portable C module with no dynamic allocation.
The platform model corresponds to a Cortex-M4F node with an SHT31-class
environmental sensor, an ADXL362-class accelerometer, and a sub-GHz radio.
Table~\ref{tab:platform} lists the energy values used in replay.  Values include
regulator loss and are deliberately rounded to avoid false precision.

\begin{table}[t]
  \caption{Platform energy model used by trace replay.}
  \label{tab:platform}
  \centering
  \small
  \begin{tabular}{@{}lrr@{}}
    \toprule
    Operation & Duration & Energy \\
    \midrule
    MCU wake + scheduler & 1.8\,ms & 0.041\,mJ \\
    Temperature/humidity sample & 6.5\,ms & 0.29\,mJ \\
    Accelerometer feature window & 250\,ms & 1.84\,mJ \\
    24-byte radio transmit & 5.2\,ms & 0.71\,mJ \\
    24-byte radio receive & 7.0\,ms & 0.53\,mJ \\
    Deep sleep & --- & 38\,$\mu$W \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Timing.}
The wake timer is absolute rather than relative, preventing processing latency
from accumulating as drift.  Sensor completion interrupts place the feature in
a two-entry queue; if the scheduler overruns, the next deadline is measured
from acquisition time.  The implementation uses saturating arithmetic for the
bucket and covariance and commits its 184-byte state every 32 observations.

\paragraph{Budget metering.}
A coulomb counter, when present, supplies measured energy debits.  Otherwise
it uses an operation table like Table~\ref{tab:platform}, updated by the gateway
only when firmware or battery chemistry changes.  A low-pass estimate of
harvested power adjusts $p_b$ no faster than once per hour, avoiding positive
feedback from transient sunlight.

\paragraph{Cold start and faults.}
For the first eight observations the node samples at
$\min(10\,\mathrm{s},\tau_{\max})$ and initializes error scale with the median
absolute successive difference.  Numerical faults reset only the predictor,
not the budget balance.  Three consecutive sensor timeouts exponentially back
off the physical sensor while continuing watchdog beacons.  A stale digest,
gateway outage, or failed radio cannot delay local sampling.

\paragraph{Complexity.}
An update uses 38 scalar multiplies, one exponential lookup, and constant
memory.  A 64-entry lookup table approximates $e^{-x}$ with less than 0.8\%
relative error across the controller's range.  At 48\,MHz the measured code
path is represented in our model as 1.8\,ms, below 0.1\% of a 2\,s minimum
interval.  The event detector and feature extractor are outside this cost and
are charged separately.

\section{Evaluation}
\label{sec:evaluation}

Our evaluation answers four questions:

\begin{enumerate}[leftmargin=*,itemsep=1pt,topsep=3pt]
  \item Does \system reduce energy at matched event recall?
  \item Does its budget hold during event storms and energy droughts?
  \item How much do uncertainty, feedback, and coordination contribute?
  \item Where does the approach fail?
\end{enumerate}

\subsection{Methodology}

\paragraph{Workloads.}
Table~\ref{tab:workloads} describes five 30-day workloads.  Temp and Humidity
use trace-aligned generators calibrated to the Corex Berkeley lab data
set~\cite{madden2004tinydb}.  Soil follows seasonal and rain transitions
reported by SensorScope~\cite{barrenetxea2008sensorscope}.  Vibration embeds
short transients in colored noise, and Air combines slow occupancy drift with
step-like ventilation events.  Generators fix random seeds and emit a full-rate
reference stream.  This setup tests policies under controlled events; it does
not claim to reproduce the end-to-end accuracy of those deployments.

\begin{table}[t]
  \caption{Trace-replay workloads.  Duration is 30 days each.}
  \label{tab:workloads}
  \centering
  \small
  \begin{tabular}{@{}llrr@{}}
    \toprule
    Workload & Dominant behavior & Events & Base rate \\
    \midrule
    Temp & diurnal + occupancy step & 186 & 1\,Hz \\
    Humidity & coupled slow drift & 121 & 1\,Hz \\
    Soil & rain onset + recession & 74 & 0.2\,Hz \\
    Vibration & 4--18\,s transients & 412 & 10\,Hz \\
    Air & drift + ventilation step & 203 & 1\,Hz \\
    \bottomrule
  \end{tabular}
\end{table}

\paragraph{Baselines.}
\textsc{Periodic} uses the fastest fixed period that reaches at least 95\%
event recall on each workload.  \textsc{Residual} halves its interval when
absolute prediction error crosses a threshold and doubles it after four stable
samples.  \textsc{Variance} maps a rolling 16-sample variance to the same
interval bounds.  \textsc{Oracle} samples densely within ground-truth event
windows and at $\tau_{\max}$ otherwise; it is a non-causal lower bound, not a
deployable competitor.  All causal policies use the same event detector,
interval limits, energy table, radio batching, and initial samples.

\paragraph{Metrics.}
An event is recalled if a sampled stream triggers the reference detector before
the event ends plus a 30\,s grace period.  Delay is measured from reference
onset.  Daily energy includes sleep, sensor, MCU, and radio energy.  Reported
values are medians over 20 generator seeds; error bars are 5th--95th
percentiles.  Unless stated otherwise, the target budget is 2.4\,kJ/day,
$B_c=0.2\Energy$, and packets batch up to four observations.

\subsection{Energy--fidelity trade-off}

Figure~\ref{fig:mainresults}a sweeps each policy's aggressiveness across all
workloads.  At 95\% recall, \system uses 1.92\,kJ/day, 54\% less than
\textsc{Periodic}, 26\% less than \textsc{Residual}, and 21\% less than
\textsc{Variance}.  Its gap to the non-causal oracle is 0.36\,kJ/day.  The gain
comes from long steady intervals without committing to them during rising
uncertainty.

At the default operating point, the per-workload reduction over periodic
sampling ranges from 47\% on Vibration to 63\% on Humidity
(Figure~\ref{fig:mainresults}b).  Vibration leaves the least opportunity because
short events force a 2\,s floor both during and after a trigger.  Humidity is
predictable enough to remain near $\tau_{\max}$ for 71\% of replay time.

\begin{figure*}[t]
  \centering
  \begin{tikzpicture}
  \begin{groupplot}[
    group style={group size=2 by 1,horizontal sep=1.35cm},
    width=.47\textwidth,height=5.2cm,
    grid=major,grid style={gray!20},
    tick label style={font=\scriptsize},label style={font=\small},
    title style={font=\small\bfseries},
    legend style={font=\scriptsize,draw=none,fill=none}]
  \nextgroupplot[
    title={(a) Aggregate Pareto frontier},
    xlabel={Daily energy (kJ)},ylabel={Event recall (\%)},
    xmin=1.15,xmax=4.8,ymin=80,ymax=100,
    legend columns=2,legend pos=south east]
    \addplot+[color=black,mark=x,thick] coordinates {(3.05,82.0) (3.48,89.0) (4.16,95.0) (4.62,98.1)};
    \addlegendentry{Periodic}
    \addplot+[color=tsorange,mark=square*,thick,dashed] coordinates {(1.72,82.2) (2.03,89.4) (2.59,95.0) (3.37,98.0)};
    \addlegendentry{Residual}
    \addplot+[color=tsgreen,mark=triangle*,thick,dashdotted] coordinates {(1.65,82.1) (1.94,89.7) (2.43,95.0) (3.15,98.1)};
    \addlegendentry{Variance}
    \addplot+[color=tsblue,mark=*,very thick] coordinates {(1.38,82.4) (1.55,90.1) (1.92,95.0) (2.61,98.0)};
    \addlegendentry{\system}
    \addplot+[color=tsgray,mark=diamond*,thick,dotted] coordinates {(1.21,83.0) (1.34,90.0) (1.56,95.0) (1.98,98.1)};
    \addlegendentry{Oracle}
  \nextgroupplot[
    title={(b) Energy reduction at $\geq$95\% recall},
    ybar,bar width=9pt,
    symbolic x coords={Temp,Humidity,Soil,Vibration,Air},xtick=data,
    x tick label style={rotate=20,anchor=east,font=\scriptsize},
    ylabel={Reduction vs. periodic (\%)},ymin=0,ymax=70,
    nodes near coords,nodes near coords style={font=\scriptsize},
    enlarge x limits=.13]
    \addplot+[fill=tsblue!72,draw=tsblue] coordinates
      {(Temp,55) (Humidity,63) (Soil,58) (Vibration,47) (Air,52)};
  \end{groupplot}
  \end{tikzpicture}
  \caption{Main trace-replay results.  (a) \system shifts the energy--recall
  frontier toward the oracle.  (b) Savings persist across all workloads at a
  matched recall target.}
  \Description{Two charts. The first plots recall against daily energy and
  shows TideSense closest to an oracle. The second shows TideSense reducing
  energy by 47 to 63 percent across five workloads.}
  \label{fig:mainresults}
\end{figure*}

\subsection{Budget adherence and delay}

We stress the controller with a six-hour event storm followed by an 18-hour
quiet period and cut harvested power by 40\% at noon.  Figure~\ref{fig:budget}
shows that a risk-only variant exceeds the day budget by 31\%; \system incurs a
brief energy debt, gradually stretches routine intervals, and finishes within
2.8\% of budget.  It does not immediately jump to $\tau_{\max}$, which would
turn an energy correction into a fidelity cliff.

Across normal workloads, median detection delay is 5.7\,s and the 99th
percentile is 18.4\,s, compared with 4.1 and 13.2\,s for the matched-recall
periodic baseline.  The extra 5.2\,s in the tail buys a 54\% energy reduction.
During the synthetic drought, 99th-percentile delay rises to 24.7\,s but remains
below the 30\,s grace bound.  No workload violates $\tau_{\max}$.

\begin{figure}[t]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=\columnwidth,height=4.9cm,
      xmin=0,xmax=24,ymin=0,ymax=3.45,
      xlabel={Time (hour)},ylabel={Cumulative energy (kJ)},
      grid=major,grid style={gray!20},
      tick label style={font=\scriptsize},label style={font=\small},
      legend style={font=\scriptsize,draw=none,fill=none,at={(.03,.97)},anchor=north west}]
      \addplot+[color=tsblue,very thick,mark=none] coordinates
        {(0,0) (3,.25) (6,.55) (9,1.05) (12,1.52) (15,1.88) (18,2.11) (21,2.28) (24,2.33)};
      \addlegendentry{\system}
      \addplot+[color=tsorange,thick,dashed,mark=none] coordinates
        {(0,0) (3,.25) (6,.56) (9,1.10) (12,1.68) (15,2.17) (18,2.55) (21,2.88) (24,3.14)};
      \addlegendentry{No governor}
      \addplot+[color=black,thin,dotted,domain=0:24,samples=2] {0.1*x};
      \addlegendentry{2.4 kJ budget}
      \draw[gray!60,dash pattern=on 2pt off 2pt] (axis cs:12,0) -- (axis cs:12,3.45)
        node[pos=.92,left,font=\scriptsize,rotate=90]{harvest cut};
    \end{axis}
  \end{tikzpicture}
  \caption{Cumulative energy during an event storm and a 40\% harvest cut.
  Budget feedback repays early debt without an abrupt sampling-rate change.}
  \Description{Cumulative energy chart over 24 hours. TideSense ends near the
  2.4 kilojoule budget, while the version without a governor rises to 3.14
  kilojoules. A vertical marker shows a harvest cut at hour 12.}
  \label{fig:budget}
\end{figure}

\subsection{Ablation and overhead}

Table~\ref{tab:ablation} removes one component at a time at the default budget.
Without uncertainty, recall falls most on Vibration because the controller
cannot anticipate divergence.  Without surprise, confident bias persists
through Soil drift.  Removing the governor improves recall slightly but breaks
the budget.  Removing coordination leaves fidelity unchanged but increases
radio traffic by 39\% in the 12-node spatial replay.  The complete design is
the only configuration that meets all three targets: at least 95\% recall, at
most 2.4\,kJ/day, and no more than 65 packets/node/day.

\begin{table}[t]
  \caption{Component ablation (median across workloads).  Bold values meet the
  corresponding target.}
  \label{tab:ablation}
  \centering
  \small
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Configuration & Recall & Energy & Packets \\
     & (\%) & (kJ/day) & (/node/day) \\
    \midrule
    Complete \system & \textbf{96.1} & \textbf{2.33} & \textbf{61} \\
    No uncertainty & 91.8 & \textbf{2.18} & \textbf{57} \\
    No surprise & 93.2 & \textbf{2.21} & \textbf{59} \\
    No governor & \textbf{97.0} & 3.05 & 78 \\
    No coordination & \textbf{96.2} & 2.47 & 100 \\
    Age guard only & 84.7 & \textbf{1.89} & \textbf{53} \\
    \bottomrule
  \end{tabular}
\end{table}

The scheduler itself accounts for 0.7\% of node energy at the 2\,s floor and
less than 0.05\% at the median 74\,s interval.  Its flash image is 6.8\,KiB,
RAM state is 184 bytes, and the exponential table is 128 bytes.  A digest adds
four bytes every $K=8$ routine reports.  Even if no report is suppressed, this
increases radio energy by 2.1\%; in the spatial replay, suppression more than
offsets that cost after the first hour.

\subsection{Sensitivity and robustness}

We vary each default parameter while holding the rest fixed.  For
$\rho\in[0.88,0.98]$, recall changes by at most 1.6 percentage points and energy
by 7.3\%.  Increasing $\kappa$ from 3 to 7 moves the operating point along the
Pareto frontier rather than causing instability.  Bucket capacities below
0.05$\Energy$ react to individual events and add 11\% interval churn; capacities
above 0.5$\Energy$ respond too slowly to the noon harvest cut.  The chosen
0.2$\Energy$ is not universally optimal, but every value from 0.12 to
0.31$\Energy$ stays within 5\% of the default energy--delay product.

We independently drop 0--60\% of neighbor digests.  Savings decrease smoothly
from 39 to 17\%, while event recall varies by less than 0.3 points because
missing digests remove the redundancy discount.  Clock offsets up to one
digest epoch have the same fail-open behavior.  At 80\% loss, coordination is
nearly ineffective but never worse than the no-coordination ablation.

\section{Discussion and Limitations}
\label{sec:discussion}

\paragraph{What the controller can guarantee.}
Bounded intervals provide a maximum observation age when the node has enough
energy for one sample per $\tau_{\max}$.  They do \emph{not} guarantee event
recall: an event shorter than $\tau_{\min}$, a failed sensor, or an inadequate
feature can still be missed.  The budget governor also cannot create energy.
A deployment must validate that its worst-case survival rate is feasible and
choose the interval bounds from application hazards rather than energy savings.

\paragraph{Multivariate and semantic signals.}
Our implementation schedules from one scalar feature.  Multiple channels can
use the maximum risk or independent schedulers, but both approaches ignore
cross-channel covariance.  A small learned representation could expose a more
useful scalar, echoing embedded inference systems such as
MCUNet~\cite{lin2020mcunet}; doing so would add training, calibration, and model
drift concerns.  We leave joint uncertainty with bounded embedded cost to
future work.

\paragraph{Adversarial conditions.}
A compromised neighbor could advertise false similarity and suppress routine
samples.  Event and age guards limit but do not eliminate this attack.  Message
authentication, trust-weighted digests, or physical plausibility checks are
needed in hostile deployments.  Energy-denial attacks are likewise outside our
model.  \system's interpretable state makes these cases observable, not solved.

\paragraph{From replay to deployment.}
Trace replay isolates scheduling choices and allows matched comparisons, but it
misses sensor warm-up effects, battery voltage dynamics, packet collisions,
seasonal changes longer than 30 days, and maintenance failures.  The energy
table is a model, not direct power-analyzer evidence.  A field evaluation across
seasons is therefore necessary before drawing claims about lifetime.  Our
present conclusion is limited: under the stated workloads and energy model,
uncertainty-aware budget feedback dominates the evaluated causal schedulers.

\section{Related Work}
\label{sec:related}

\paragraph{Adaptive acquisition.}
Model-driven acquisition reduces communication by answering queries from
probabilistic models and contacting sensors only when uncertainty is
high~\cite{deshpande2004model}.  BBQ and Ken build on this principle for
declarative sensor queries~\cite{deshpande2005model}.  Compressive sensing
exploits signal sparsity to reconstruct fewer measurements~\cite{candes2006robust,jain2004duality}, while event-triggered control communicates only when a state
deviates enough to matter~\cite{astrom2008event}.  \system shares their
value-of-information intuition but couples uncertainty to explicit energy debt,
bounded age, and decentralized redundancy suppression.

\paragraph{Energy-aware sensing.}
Energy-harvesting systems traditionally adapt workload to predicted supply,
from energy-neutral operation~\cite{kansal2007power} to utility-maximizing
schedulers~\cite{raghunathan2006energyscavenging}.  Hibernus and Mementos make
intermittent execution safe under power failures~\cite{balsamo2015hibernus,ransford2011mementos}.  These works govern \emph{whether computation can continue}; our
focus is the marginal sensing decision within an available budget.  The token
bucket can consume an external harvest forecast but does not depend on one.

\paragraph{In-network redundancy.}
LEACH amortizes radio cost through cluster heads~\cite{heinzelman2000energy},
directed diffusion names and aggregates data in-network~\cite{intanagonwiwat2000directed}, and TinyDB pushes acquisitional queries into the network~\cite{madden2004tinydb}.  Spatial correlation has also supported adaptive
sampling and suppression~\cite{guestrin2005distributed}.  \system uses only a
loss-tolerant digest, retaining local autonomy and avoiding a cluster repair
protocol.

\paragraph{Embedded learning.}
Modern tiny-ML systems make inference practical on microcontrollers
\cite{banbury2021benchmarking,lin2020mcunet}.  Reinforcement learning could
learn a sampling policy, but constrained behavior under shift is difficult to
inspect.  Our small analytical controller offers a complementary point in the
design space: less expressive, but cheap to port, tune, and audit.

\section{Reproducibility and Ethics}
\label{sec:ethics}

The evaluation uses synthetic or trace-aligned environmental signals and no
human participants, personally identifiable information, or live actuation.
Consequently, it does not require human-subjects review.  A reproducibility
package should include generator seeds, full-rate reference streams, event
labels, policy configurations, simulator code, and scripts that emit every
number in the figures and tables.  For anonymous review, such an artifact must
be hosted without author, institution, or repository metadata.  Field users
should separately assess risks from missed events, environmental sensing, and
radio identifiers; this paper's scheduler is not appropriate as the sole
safeguard in life-critical systems.

\section{Conclusion}

\system treats a sample as an energy investment whose value changes with model
uncertainty, recent surprise, age, and neighboring evidence.  A compact
predictor estimates that value, a bounded interval map converts it to timing,
and a token bucket reconciles responsiveness with a finite energy budget.  In
our deterministic trace replay, the combination reduces modeled daily energy
by 47--63\% over matched-recall periodic sampling while preserving 96.1\%
event recall.  The result is not a claim of deployment lifetime; it is evidence
that uncertainty and energy debt form a useful, interpretable control surface.
The next step is a season-scale, multi-site deployment that measures power and
missed events directly.

\begin{thebibliography}{20}

\bibitem{astrom2008event}
Karl J. \r{A}str\"om.
2008.
Event Based Control.
In \emph{Analysis and Design of Nonlinear Control Systems}.
Springer, 127--147.

\bibitem{balsamo2015hibernus}
Domenico Balsamo, Alex S. Weddell, Anup Das, Alberto Rodriguez Arreola,
Davide Brunelli, Bashir M. Al-Hashimi, Geoff V. Merrett, and Luca Benini.
2015.
Hibernus++: A Self-Calibrating and Adaptive System for Transiently-Powered
Embedded Devices.
\emph{IEEE Transactions on Computer-Aided Design of Integrated Circuits and
Systems} 35, 12 (2015), 1968--1980.

\bibitem{banbury2021benchmarking}
Colby Banbury, Vijay Janapa Reddi, Peter Torelli, Nat Jeffries, Csaba Kiraly,
Jeremy Holleman, Pietro Montino, David Kanter, Sebastian Ahmed, Danilo Pau,
Urish Thakker, Antonio Torrini, Pete Warden, Jay Cordaro, Giuseppe Di Guglielmo,
Javier Duarte, Stephen Gibellini, Videet Parekh, Honson Tran, Nhan Tran, and
Martin Popovich.
2021.
MLPerf Tiny Benchmark.
In \emph{Proceedings of the Neural Information Processing Systems Track on
Datasets and Benchmarks}.

\bibitem{barrenetxea2008sensorscope}
Guillermo Barrenetxea, Fran\c{c}ois Ingelrest, Gunnar Schaefer, and Martin Vetterli.
2008.
The Hitchhiker's Guide to Successful Wireless Sensor Network Deployments.
In \emph{Proceedings of SenSys}. 43--56.

\bibitem{candes2006robust}
Emmanuel J. Cand\`es, Justin Romberg, and Terence Tao.
2006.
Robust Uncertainty Principles: Exact Signal Reconstruction from Highly
Incomplete Frequency Information.
\emph{IEEE Transactions on Information Theory} 52, 2 (2006), 489--509.

\bibitem{deshpande2004model}
Amol Deshpande, Carlos Guestrin, Samuel R. Madden, Joseph M. Hellerstein, and
Wei Hong.
2004.
Model-Driven Data Acquisition in Sensor Networks.
In \emph{Proceedings of VLDB}. 588--599.

\bibitem{deshpande2005model}
Amol Deshpande, Carlos Guestrin, Wei Hong, and Samuel Madden.
2005.
Exploiting Correlated Attributes in Acquisitional Query Processing.
In \emph{Proceedings of ICDE}. 143--154.

\bibitem{guestrin2005distributed}
Carlos Guestrin, Andreas Krause, and Ajit Paul Singh.
2005.
Near-Optimal Sensor Placements in Gaussian Processes.
In \emph{Proceedings of ICML}. 265--272.

\bibitem{heinzelman2000energy}
Wendi Rabiner Heinzelman, Anantha Chandrakasan, and Hari Balakrishnan.
2000.
Energy-Efficient Communication Protocol for Wireless Microsensor Networks.
In \emph{Proceedings of HICSS}. 1--10.

\bibitem{intanagonwiwat2000directed}
Chalermek Intanagonwiwat, Ramesh Govindan, and Deborah Estrin.
2000.
Directed Diffusion: A Scalable and Robust Communication Paradigm for Sensor
Networks.
In \emph{Proceedings of MobiCom}. 56--67.

\bibitem{jain2004duality}
Akshay Jain, Edward Y. Chang, and Yuan-Fang Wang.
2004.
Adaptive Stream Resource Management Using Kalman Filters.
In \emph{Proceedings of SIGMOD}. 11--22.

\bibitem{kansal2007power}
Aman Kansal, Jason Hsu, Sadaf Zahedi, and Mani B. Srivastava.
2007.
Power Management in Energy Harvesting Sensor Networks.
\emph{ACM Transactions on Embedded Computing Systems} 6, 4 (2007), Article 32.

\bibitem{lin2010energy}
Kaisen Lin, Aman Kansal, Dimitrios Lymberopoulos, and Feng Zhao.
2010.
Energy-Accuracy Trade-off for Continuous Mobile Device Location.
In \emph{Proceedings of MobiSys}. 285--298.

\bibitem{lin2020mcunet}
Ji Lin, Wei-Ming Chen, Yujun Lin, John Cohn, Chuang Gan, and Song Han.
2020.
MCUNet: Tiny Deep Learning on IoT Devices.
In \emph{Proceedings of NeurIPS}. 11711--11722.

\bibitem{lorincz2004sensor}
Konrad Lorincz, David J. Malan, Thaddeus R. F. Fulford-Jones, Alan Nawoj,
Antony Clavel, Victor Shnayder, Geoffrey Mainland, Matt Welsh, and Steve Moulton.
2004.
Sensor Networks for Emergency Response: Challenges and Opportunities.
\emph{IEEE Pervasive Computing} 3, 4 (2004), 16--23.

\bibitem{lu2010smart}
Hong Lu, Jun Yang, Zhigang Liu, Nicholas D. Lane, Tanzeem Choudhury, and
Andrew T. Campbell.
2010.
The Jigsaw Continuous Sensing Engine for Mobile Phone Applications.
In \emph{Proceedings of SenSys}. 71--84.

\bibitem{madden2004tinydb}
Samuel R. Madden, Michael J. Franklin, Joseph M. Hellerstein, and Wei Hong.
2005.
TinyDB: An Acquisitional Query Processing System for Sensor Networks.
\emph{ACM Transactions on Database Systems} 30, 1 (2005), 122--173.

\bibitem{raghunathan2006energyscavenging}
Vijay Raghunathan, Aman Kansal, Jason Hsu, Jonathan Friedman, and Mani Srivastava.
2005.
Design Considerations for Solar Energy Harvesting Wireless Embedded Systems.
In \emph{Proceedings of IPSN}. 457--462.

\bibitem{ransford2011mementos}
Benjamin Ransford, Jacob Sorber, and Kevin Fu.
2011.
Mementos: System Support for Long-Running Computation on RFID-Scale Devices.
In \emph{Proceedings of ASPLOS}. 159--170.

\end{thebibliography}

\end{document}
