\section{Reproducibility Checklist}
\label{sec:reproducibility}

An empirical release following ShiftBench should contain the following items:

\begin{enumerate}[leftmargin=*,itemsep=2pt,topsep=3pt]
  \item immutable train, validation, and test manifests, or scripts and hashes
  that reproduce them when data licenses forbid redistribution;
  \item operator configurations, severity grids, invariants, diagnostic outputs,
  and random seeds;
  \item complete candidate-set construction and a clear label for any sampled
  evaluation;
  \item model configurations, equalized tuning budgets, stopping rules, software
  versions, and compute hardware;
  \item per-user metric outputs sufficient for paired confidence intervals, with
  privacy-preserving aggregation where required;
  \item every claim card, including inconclusive comparisons and abstentions; and
  \item wall-clock training and inference time plus peak accelerator memory.
\end{enumerate}

These artifacts make the evaluation inspectable without requiring a reviewer to
trust a single aggregate table.  When proprietary data prevent release, authors
should publish the operators, synthetic fixtures, invariant tests, and an exact
description of the unavailable fields.
