\section{Related Work}

\paragraph{Document-level NMT.} Early document-level approaches concatenate
adjacent sentences into an extended source context
\citep{tanaka2020span}, while later work injects discourse-level memory
via auxiliary encoders \citep{ferreira2019attention}. These methods generally
require document-aligned training data, which is scarcer than sentence-level
bitext for most low-resource pairs \citep{oyelaran2022lowresource}.

\paragraph{Retrieval-augmented translation.} Retrieval augmentation has been
explored for adapting translation to specific domains or terminologies
\citep{alvarez2023retrieval}, and for grounding dialogue generation in
retrieved evidence \citep{hassan2023dialogue}. \textsc{DiscoRerank} differs
in retrieving \emph{within} a single document rather than from an external
corpus, and in using retrieval purely at decoding time.

\paragraph{Reranking and calibration.} Reranking beam candidates by auxiliary
signals is a long-standing technique for improving fluency and adequacy
\citep{kapoor2022evaluation}, and calibrated confidence estimates have proven
useful for combining heterogeneous scoring signals \citep{ibrahim2023calibration}.
We build on this tradition, combining a calibrated discourse score with the
decoder's own likelihood.

\paragraph{Cross-lingual representations.} Our discourse encoder builds on
contrastive cross-lingual sentence embeddings \citep{park2022contrastive} and
parameter-efficient adapter modules \citep{schneider2020adapter} to keep the
encoder small enough to run inline during decoding without a GPU.
