\documentclass[10pt,twocolumn]{article}

% ---------------------------------------------------------------------------
% EMNLP 2026 Submission Paper (UNOFFICIAL template)
% ---------------------------------------------------------------------------
\usepackage[letterpaper,margin=1in,columnsep=0.25in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multirow}
\usepackage{natbib}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usepackage[colorlinks=true,linkcolor=emnlpblue,citecolor=emnlpblue,urlcolor=emnlpblue,breaklinks=true]{hyperref}
\usepackage{enumitem}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage[font=small,labelfont=bf,margin=4pt]{caption}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{microtype}
\usepackage{filecontents}

% ---------------------------------------------------------------------------
% Colour palette
% ---------------------------------------------------------------------------
\definecolor{emnlpblue}{RGB}{31,58,111}
\definecolor{emnlpred}{RGB}{150,30,40}
\definecolor{emnlpgray}{RGB}{90,90,90}
\definecolor{emnlplight}{RGB}{240,242,247}

% ---------------------------------------------------------------------------
% Section formatting
% ---------------------------------------------------------------------------
\titleformat{\section}
  {\normalfont\large\bfseries\color{emnlpblue}}
  {\thesection}{0.6em}{}
\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{emnlpblue!85!black}}
  {\thesubsection}{0.6em}{}
\titlespacing*{\section}{0pt}{10pt}{6pt}
\titlespacing*{\subsection}{0pt}{8pt}{4pt}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[C]{\footnotesize\color{emnlpgray}Unofficial EMNLP-style template --- not affiliated with or endorsed by the conference}
\fancyfoot[C]{\footnotesize\color{emnlpgray}\thepage}

\setlength{\parindent}{1em}
\setlength{\parskip}{0pt}

% ---------------------------------------------------------------------------
% Bibliography source (filecontents)
% ---------------------------------------------------------------------------
\begin{filecontents*}{references.bib}
@inproceedings{alvarez2023retrieval,
  author    = {Alvarez, Mireille and Okonkwo, Daniel},
  title     = {Retrieval-Augmented Decoding for Low-Resource Machine Translation},
  booktitle = {Proceedings of the Conference on Empirical Methods in Natural Language Processing},
  year      = {2023},
  pages     = {4021--4034},
  address   = {Singapore}
}

@inproceedings{park2022contrastive,
  author    = {Park, Soo-Yun and Fenwick, Thomas},
  title     = {Contrastive Pretraining Objectives for Cross-Lingual Sentence Embeddings},
  booktitle = {Proceedings of the Annual Meeting of the Association for Computational Linguistics},
  year      = {2022},
  pages     = {1183--1196}
}

@article{nakamura2021survey,
  author  = {Nakamura, Haruto and Bergstrom, Elin},
  title   = {A Survey of Discourse-Level Coherence Modeling in Neural Text Generation},
  journal = {Transactions of the Association for Computational Linguistics},
  volume  = {9},
  year    = {2021},
  pages   = {612--630}
}

@inproceedings{diallo2023factuality,
  author    = {Diallo, Awa and Kowalski, Piotr},
  title     = {Measuring Factuality Drift in Long-Form Summarization},
  booktitle = {Proceedings of the North American Chapter of the Association for Computational Linguistics},
  year      = {2023},
  pages     = {2210--2225}
}

@inproceedings{tanaka2020span,
  author    = {Tanaka, Ren and Whitfield, Louisa},
  title     = {Span-Based Joint Extraction of Entities and Relations Revisited},
  booktitle = {Proceedings of the Conference on Empirical Methods in Natural Language Processing},
  year      = {2020},
  pages     = {5895--5906}
}

@article{oyelaran2022lowresource,
  author  = {Oyelaran, Femi and Castellano, Jorge},
  title   = {Data-Efficient Fine-Tuning for Morphologically Rich Languages},
  journal = {Computational Linguistics},
  volume  = {48},
  number  = {3},
  year    = {2022},
  pages   = {701--734}
}

@inproceedings{lindqvist2021probing,
  author    = {Lindqvist, Astrid and Mensah, Kwame},
  title     = {Probing Syntactic Generalization in Encoder-Decoder Architectures},
  booktitle = {Proceedings of the Annual Meeting of the Association for Computational Linguistics},
  year      = {2021},
  pages     = {3344--3357}
}

@inproceedings{hassan2023dialogue,
  author    = {Hassan, Yusuf and Novak, Petra},
  title     = {Grounded Dialogue State Tracking with Retrieved Evidence Chains},
  booktitle = {Proceedings of the Conference on Empirical Methods in Natural Language Processing},
  year      = {2023},
  pages     = {9871--9885}
}

@article{ferreira2019attention,
  author  = {Ferreira, Bianca and Solberg, Anders},
  title   = {Attention Head Specialization in Multilingual Transformers},
  journal = {Transactions of the Association for Computational Linguistics},
  volume  = {7},
  year    = {2019},
  pages   = {455--470}
}

@inproceedings{kapoor2022evaluation,
  author    = {Kapoor, Ritvik and Dumont, Celine},
  title     = {Rethinking Automatic Evaluation Metrics for Abstractive Summarization},
  booktitle = {Proceedings of the North American Chapter of the Association for Computational Linguistics},
  year      = {2022},
  pages     = {1450--1465}
}

@inproceedings{schneider2020adapter,
  author    = {Schneider, Lukas and Amaral, Rafaela},
  title     = {Adapter Modules for Efficient Multi-Task Sequence Labeling},
  booktitle = {Proceedings of the Conference on Empirical Methods in Natural Language Processing},
  year      = {2020},
  pages     = {6602--6613}
}

@article{ibrahim2023calibration,
  author  = {Ibrahim, Layla and Petrov, Nikolai},
  title   = {Calibration of Confidence Estimates in Neural Machine Translation},
  journal = {Computational Linguistics},
  volume  = {49},
  number  = {1},
  year    = {2023},
  pages   = {89--117}
}
\end{filecontents*}

\begin{document}

% ---------------------------------------------------------------------------
% Title block
% ---------------------------------------------------------------------------
\newcommand{\PaperTitleBlock}{%
\begin{center}
{\LARGE\bfseries\color{emnlpblue} Discourse-Aware Retrieval Reranking for Low-Resource Neural Machine Translation}\\
\vspace{0.9em}
{\large Mireille Alvarez$^{1}$ \quad Daniel Okonkwo$^{1}$ \quad Soo-Yun Park$^{2}$ \quad Ren Tanaka$^{3}$}\\
\vspace{0.4em}
{\small
$^{1}$Nexa Language Technologies \quad
$^{2}$Vertex Institute for Computational Linguistics \quad
$^{3}$Meridian University, Department of Computer Science}\\
\vspace{0.2em}
{\small\ttfamily\{m.alvarez, d.okonkwo\}@nexa-lt.example}
\end{center}

\vspace{0.6em}
\begin{tcolorbox}[
  colback=emnlplight, colframe=emnlpred!70!black, boxrule=0.6pt,
  arc=2pt, left=8pt, right=8pt, top=5pt, bottom=5pt, breakable
]
\footnotesize\color{black}
\textbf{Unofficial template.} This document is an independently produced LaTeX
template styled after common EMNLP submission formatting conventions. It is
\textbf{not affiliated with, endorsed by, or produced by} the Association for
Computational Linguistics (ACL) or the EMNLP organizing committee. Style
files, page limits, and formatting requirements change from year to year --
\textbf{always download the official style package and consult the current
year's Call for Papers} before preparing a submission.
\end{tcolorbox}
\vspace{0.8em}

\begin{center}
\begin{minipage}{0.92\textwidth}
\small
\textbf{Abstract.} Neural machine translation for low-resource language pairs
suffers disproportionately from discourse-level incoherence: pronouns lose
their antecedents, tense agreement drifts across sentence boundaries, and
document-level terminology becomes inconsistent. We introduce
\textsc{DiscoRerank}, a lightweight reranking module that scores beam
candidates using retrieved discourse contexts from a bitext memory, without
modifying the underlying translation model. \textsc{DiscoRerank} retrieves the
$k$ most discourse-similar sentence pairs already translated within the same
document, encodes them with a frozen bi-encoder, and reranks beam hypotheses
by a weighted combination of translation likelihood and discourse
consistency. On three low-resource pairs (Bengali--English, Swahili--English,
Quechua--Spanish) drawn from a simulated FLORES-style benchmark,
\textsc{DiscoRerank} improves document-level coherence (+3.8 discourse
agreement points) and chrF (+1.6 average) over a strong beam-search baseline,
with negligible latency overhead. We release ablations isolating the
contribution of retrieval depth, encoder choice, and reranking weight.
\end{minipage}
\end{center}
\vspace{1.1em}
}

\twocolumn[\PaperTitleBlock]

% ---------------------------------------------------------------------------
\section{Introduction}

Document-level machine translation has narrowed the gap with human
translators on high-resource pairs, yet low-resource settings remain
stubbornly difficult: limited parallel data forces models toward
sentence-level training objectives, and the resulting systems translate each
sentence in near-isolation. The consequence is a familiar failure mode --
individually fluent sentences that, read together, disagree on pronoun
gender, drift in verb tense, or translate the same named entity two different
ways within a paragraph.

Prior work addresses this either by retraining with document-level context
windows \citep{nakamura2021survey} or by augmenting decoding with auxiliary
discourse features \citep{lindqvist2021probing}. Both approaches require
either large amounts of document-aligned parallel data or architectural
changes to the base model -- both scarce resources precisely in the
low-resource languages where the problem is most severe.

We take a retrieval-based, model-agnostic approach instead. Our method,
\textsc{DiscoRerank}, treats discourse consistency as a reranking signal
rather than a training objective. Given a document already partially
translated, we retrieve prior sentence pairs from the same document that are
lexically and semantically close to the current source sentence, encode them
with a frozen bi-encoder, and use the resulting discourse-similarity score to
rerank the $n$-best list produced by an off-the-shelf beam search decoder.
Because \textsc{DiscoRerank} never touches model weights, it is applicable to
any pretrained translation system with beam-search decoding, including
systems too small or too data-starved to support document-level fine-tuning.

\vspace{2pt}
\noindent Our contributions are threefold:
\begin{itemize}[leftmargin=1.1em,itemsep=1pt,topsep=2pt]
  \item A retrieval-and-rerank formulation of document-level consistency
    that requires no retraining of the base translation model;
  \item A discourse agreement metric, adapted from pronoun and terminology
    consistency literature, for automatic evaluation without human raters;
  \item An empirical study across three low-resource pairs showing
    consistent gains in coherence and chrF, together with ablations
    isolating retrieval depth and encoder choice.
\end{itemize}

\section{Related Work}

\paragraph{Document-level NMT.} Early document-level approaches concatenate
adjacent sentences into an extended source context
\citep{tanaka2020span}, while later work injects discourse-level memory
via auxiliary encoders \citep{ferreira2019attention}. These methods generally
require document-aligned training data, which is scarcer than sentence-level
bitext for most low-resource pairs \citep{oyelaran2022lowresource}.

\paragraph{Retrieval-augmented translation.} Retrieval augmentation has been
explored for adapting translation to specific domains or terminologies
\citep{alvarez2023retrieval}, and for grounding dialogue generation in
retrieved evidence \citep{hassan2023dialogue}. \textsc{DiscoRerank} differs
in retrieving \emph{within} a single document rather than from an external
corpus, and in using retrieval purely at decoding time.

\paragraph{Reranking and calibration.} Reranking beam candidates by auxiliary
signals is a long-standing technique for improving fluency and adequacy
\citep{kapoor2022evaluation}, and calibrated confidence estimates have proven
useful for combining heterogeneous scoring signals \citep{ibrahim2023calibration}.
We build on this tradition, combining a calibrated discourse score with the
decoder's own likelihood.

\paragraph{Cross-lingual representations.} Our discourse encoder builds on
contrastive cross-lingual sentence embeddings \citep{park2022contrastive} and
parameter-efficient adapter modules \citep{schneider2020adapter} to keep the
encoder small enough to run inline during decoding without a GPU.

\section{Method}

\subsection{Problem Setting}
Let a source document $D = (s_1, \dots, s_T)$ be translated sentence by
sentence in order. At step $t$, a base NMT system produces an $n$-best list
$\mathcal{H}_t = \{h_t^{(1)}, \dots, h_t^{(n)}\}$ via beam search, each with
model log-likelihood $\log p_\theta(h_t^{(i)} \mid s_t)$. Standard decoding
selects $h_t^{(1)}$, the top-scoring hypothesis, discarding document context
entirely. \textsc{DiscoRerank} instead selects a hypothesis by combining
translation likelihood with a discourse-consistency score computed against
already-translated sentences $(s_1, h_1), \dots, (s_{t-1}, h_{t-1})$.

\subsection{Discourse Retrieval}
For source sentence $s_t$, we retrieve the top $k$ prior sentence pairs in
the same document under cosine similarity of a frozen bi-encoder embedding
$f(\cdot)$:
\[
  \mathcal{R}_t = \operatorname*{arg\,top\text{-}k}_{j < t}\; \cos\!\big(f(s_t),\, f(s_j)\big).
\]
Retrieval is restricted to the current document, so $\mathcal{R}_t$ is empty
for $t \le k$ and grows in size as the document proceeds -- early sentences
fall back to the base decoder's top hypothesis.

\subsection{Discourse-Consistency Scoring}
Each retrieved pair $(s_j, h_j) \in \mathcal{R}_t$ contributes a
terminology-and-pronoun consistency vote against a candidate hypothesis
$h_t^{(i)}$. We define the discourse score $g(h_t^{(i)})$ as the average
agreement across retrieved pairs on (a) shared named-entity translations and
(b) pronoun-antecedent gender/number agreement, each computed with
rule-based aligners over the source and target sides. The final reranking
score is

\begin{equation}
\label{eq:rerank}
  \operatorname{score}\big(h_t^{(i)}\big) \;=\; \log p_\theta\big(h_t^{(i)} \mid s_t\big) \;+\; \lambda \cdot g\big(h_t^{(i)}\big),
\end{equation}

\noindent where $\lambda \geq 0$ trades off fluency against discourse
consistency. We select $h_t^\star = \arg\max_i \operatorname{score}(h_t^{(i)})$
as the final translation for sentence $t$, and $\lambda$ is tuned once per
language pair on a small development split (Section~\ref{sec:experiments}).

\subsection{Why Reranking, Not Retraining}
Because $g(\cdot)$ operates only on decoded hypotheses and $\lambda$ is a
single scalar, \textsc{DiscoRerank} requires no gradient updates to
$\theta$. The bi-encoder $f(\cdot)$ is pretrained once, off the target
domain, and frozen thereafter. This keeps the method usable in the exact
regime where document-level fine-tuning is least practical: language pairs
with too little document-aligned data to support it.

\section{Experiments}
\label{sec:experiments}

\subsection{Data and Base Systems}
We evaluate on three low-resource pairs -- Bengali--English (bn--en),
Swahili--English (sw--en), and Quechua--Spanish (qu--es) -- using a
simulated FLORES-style document-level benchmark of 180 documents per pair
(average 14.2 sentences/document). Base translation systems are
Transformer-base models pretrained on available sentence-level bitext for
each pair, decoded with beam size 5.

\subsection{Metrics}
We report chrF for surface-level translation quality and a \emph{discourse
agreement} (DA) score, the fraction of retrieved-pair terminology and
pronoun decisions that remain consistent across a document, macro-averaged
over documents. Both metrics are computed automatically; DA is our adaptation
of prior consistency metrics \citep{diallo2023factuality} to
low-resource pairs lacking gold coreference annotation.

\subsection{Baselines}
\textbf{Beam-1} selects the top beam hypothesis with no reranking.
\textbf{Beam-avg} reranks by length-normalized likelihood only ($\lambda=0$).
\textbf{Concat-ctx} concatenates the previous sentence into the source as a
lightweight document-context baseline, following \citet{tanaka2020span}.

\section{Results}

Table~\ref{tab:main} reports chrF and discourse agreement across all three
pairs. \textsc{DiscoRerank} improves discourse agreement over every baseline
on every pair, with the largest gain on Quechua--Spanish (+5.1 DA over
Beam-1), the pair with the smallest bitext and thus the weakest implicit
document modeling in the base system. chrF gains are smaller but consistent,
indicating that discourse-aware reranking does not trade off surface fluency
to obtain coherence.

\begin{table}[t]
\centering
\small
\caption{chrF and discourse agreement (DA, \%) across three low-resource
pairs. Best result per column in \textbf{bold}.}
\label{tab:main}
\begin{tabular}{@{}l l cc@{}}
\toprule
\textbf{Pair} & \textbf{System} & \textbf{chrF} & \textbf{DA} \\
\midrule
\multirow{4}{*}{bn--en}
 & Beam-1        & 41.2 & 68.4 \\
 & Beam-avg      & 41.5 & 69.1 \\
 & Concat-ctx    & 41.9 & 71.8 \\
 & \textsc{DiscoRerank} & \textbf{43.0} & \textbf{75.6} \\
\midrule
\multirow{4}{*}{sw--en}
 & Beam-1        & 44.7 & 70.9 \\
 & Beam-avg      & 44.9 & 71.4 \\
 & Concat-ctx    & 45.3 & 73.5 \\
 & \textsc{DiscoRerank} & \textbf{46.1} & \textbf{76.7} \\
\midrule
\multirow{4}{*}{qu--es}
 & Beam-1        & 33.8 & 61.2 \\
 & Beam-avg      & 34.0 & 61.9 \\
 & Concat-ctx    & 34.4 & 64.7 \\
 & \textsc{DiscoRerank} & \textbf{35.7} & \textbf{66.3} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Ablations}
Figure~\ref{fig:ablation} varies retrieval depth $k$ from 1 to 10 on the
sw--en development split. Discourse agreement rises sharply through $k=4$ and
plateaus thereafter, while chrF is comparatively flat, suggesting that a
handful of nearby discourse anchors captures most of the available
consistency signal and additional retrieved context contributes mostly
redundant votes.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.95\linewidth,
  height=4.6cm,
  xlabel={Retrieval depth $k$},
  ylabel={Score},
  xmin=1, xmax=10,
  ymin=68, ymax=78,
  xtick={1,2,4,6,8,10},
  legend style={font=\scriptsize,at={(0.97,0.05)},anchor=south east,draw=none,fill=none},
  label style={font=\small},
  tick label style={font=\scriptsize},
  axis lines=left,
  grid=major,
  grid style={dashed,gray!25},
]
\addplot[color=emnlpblue,thick,mark=*,mark size=1.4pt] coordinates {
  (1,71.4) (2,73.8) (3,75.5) (4,76.4) (5,76.6) (6,76.7) (8,76.6) (10,76.5)
};
\addlegendentry{Discourse agreement}
\addplot[color=emnlpred,thick,mark=square*,mark size=1.4pt] coordinates {
  (1,44.9) (2,45.4) (3,45.8) (4,46.0) (5,46.1) (6,46.1) (8,46.0) (10,46.0)
};
\addlegendentry{chrF (rescaled $+31$)}
\end{axis}
\end{tikzpicture}
\caption{Effect of retrieval depth $k$ on discourse agreement and chrF
(sw--en development split). Gains saturate around $k=4$--5.}
\label{fig:ablation}
\end{figure}

We further find that encoder choice matters less than retrieval depth: a
distilled multilingual bi-encoder reaches within 0.6 DA points of a larger
encoder at roughly one-eighth the inference cost, making
\textsc{DiscoRerank} practical to run on CPU alongside decoding.

\section{Discussion}

The consistent gains across three typologically distinct low-resource pairs
suggest that document-level incoherence in low-resource NMT is, at least in
part, a decoding-time problem rather than purely a training-data problem --
information sufficient to keep translations consistent is already present in
the document, but standard beam search has no mechanism to use it. This is
encouraging because it means coherence improvements do not have to wait on
scaling document-aligned parallel corpora, which remain scarce for most of
the world's languages.

\textsc{DiscoRerank} is not without limitations. The discourse score
$g(\cdot)$ relies on rule-based named-entity and pronoun aligners, which
degrade on languages with rich morphology or non-Latin scripts lacking
mature NLP tooling -- partly why gains on qu--es, while positive, are smaller
than on bn--en and sw--en. We also assume sequential, single-pass decoding;
extending retrieval to look both backward and forward within a document (at
the cost of a second decoding pass) is a natural next step. Finally, our
discourse agreement metric is itself automatic and correlates with, but does
not replace, human coherence judgments, which we leave to future work given
the annotation cost across three language pairs.

\section{Conclusion}

We presented \textsc{DiscoRerank}, a retrieval-and-rerank method for
improving document-level coherence in low-resource neural machine
translation without retraining the base model. By retrieving
already-translated sentences from the same document and scoring beam
candidates on discourse consistency, \textsc{DiscoRerank} improves both
chrF and discourse agreement across three low-resource pairs, with the
largest relative gains on the most resource-scarce pair. Because the method
adds only a lightweight frozen encoder and a scalar reranking weight, it is
directly applicable to existing deployed translation systems. Future work
includes bidirectional document retrieval, learned (rather than rule-based)
consistency scoring, and a human evaluation of discourse coherence across
all three language pairs.

\section*{Acknowledgments}
We thank the anonymous reviewers for helpful comments. This work was
supported in part by a Nexa Language Technologies research grant and
computing resources provided by the Vertex Institute for Computational
Linguistics.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
