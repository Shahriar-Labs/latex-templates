\section{Method}
Our proposed framework, Synapse-HEA, consists of three main components: feature initialization, relation-aware GNN encoding, and alignment space mapping. The overall workflow is illustrated in Figure~\ref{fig:framework}.

\begin{figure}[t]
  \centering
  \resizebox{\columnwidth}{!}{%
  \begin{tikzpicture}[
    node distance=1.3cm and 1.2cm,
    every node/.style={font=\small, align=center},
    block/.style={rectangle, draw, fill=cikmbg, minimum width=2.0cm, minimum height=0.8cm, rounded corners},
    arrow/.style={-{Latex[scale=0.8]}, thick, draw=cikmgray}
  ]
    % Input Graphs
    \node (g1) [block] {Input Graph $\mathcal{G}_1$ \\ (Vertex DB)};
    \node (g2) [block, below=of g1] {Input Graph $\mathcal{G}_2$ \\ (Nexa Graph)};
    
    % Embeddings
    \node (emb1) [block, right=of g1] {Initial Features \\ $\mathbf{H}^{(0)}_1$};
    \node (emb2) [block, right=of g2] {Initial Features \\ $\mathbf{H}^{(0)}_2$};
    
    % GNN Encoder
    \node (gnn) [block, right=of emb1, yshift=-1.0cm, fill=cikmblue!10, draw=cikmblue, minimum height=1.6cm] {Synapse GNN \\ Encoder};
    
    % Alignment Space
    \node (align) [block, right=of gnn] {Alignment \\ Space $\mathcal{Z}$};
    
    % Flows
    \draw [arrow] (g1) -- (emb1);
    \draw [arrow] (g2) -- (emb2);
    \draw [arrow] (emb1) -- (gnn.north west);
    \draw [arrow] (emb2) -- (gnn.south west);
    \draw [arrow] (gnn) -- (align);
    
    % Loss loop
    \path (align.east) edge [out=30, in=-30, loop, arrow, distance=0.8cm] node[right] {Loss \\ $\mathcal{L}_{\text{align}}$} (align.east);
  \end{tikzpicture}
  }
  \caption{Overview of the Synapse Heterogeneous Entity Alignment (HEA) framework running on Vertex and Nexa database graph inputs.}
  \label{fig:framework}
\end{figure}

\subsection{Relation-Aware Encoding}
Given a heterogeneous graph $\mathcal{G} = (\mathcal{V}, \mathcal{E}, \mathcal{R})$ with entity set $\mathcal{V}$, edge set $\mathcal{E}$, and relation set $\mathcal{R}$, we define the relation-aware graph convolution operation at layer $l$ as follows:
\begin{equation}
  \mathbf{h}_{i}^{(l+1)} = \sigma \left( \sum_{r \in \mathcal{R}} \sum_{j \in \mathcal{N}_r(i)} \frac{1}{c_{i,r}} \mathbf{W}_r^{(l)} \mathbf{h}_{j}^{(l)} + \mathbf{W}_0^{(l)} \mathbf{h}_{i}^{(l)} \right)
  \label{eq:gnn_update}
\end{equation}
where $\mathbf{h}_{i}^{(l)}$ denotes the embedding of entity $v_i$ at layer $l$, $\mathcal{N}_r(i)$ is the set of neighbors of $v_i$ under relation $r$, $\mathbf{W}_r^{(l)}$ is a relation-specific projection matrix, $\mathbf{W}_0^{(l)}$ is a self-loop transformation matrix, and $\sigma(\cdot)$ is the activation function (e.g., ReLU). The normalization constant is set to $c_{i,r} = |\mathcal{N}_r(i)|$.

\subsection{Alignment Space Mapping}
To align entities from two different graphs $\mathcal{G}_1$ and $\mathcal{G}_2$, we map their GNN-encoded representations into a unified alignment space $\mathcal{Z}$. We train the projection network using a margin-based ranking loss over a set of known alignment seeds $\mathcal{S} = \{(e_1, e_2) \mid e_1 \in \mathcal{G}_1, e_2 \in \mathcal{G}_2\}$:
\begin{equation}
  \mathcal{L} = \sum_{(e_1, e_2) \in \mathcal{S}} \sum_{(e_1', e_2') \in \mathcal{S}'} \max \left( 0, \gamma + d(e_1, e_2) - d(e_1', e_2') \right)
  \label{eq:loss}
\end{equation}
where $d(\cdot, \cdot)$ is the Manhattan distance in $\mathcal{Z}$, $\gamma > 0$ is a margin parameter, and $\mathcal{S}'$ is a set of negative alignment pairs generated by corrupting the seeds.
