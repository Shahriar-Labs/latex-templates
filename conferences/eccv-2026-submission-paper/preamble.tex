% Packages, colours and commands for this template, on top of llncs and the eccv package.
%
% ECCV rejects papers that change the font or the format, so this file adds nothing that
% changes llncs's page geometry, fonts, section headings, captions or spacing. In
% particular: no geometry, no caption settings, no enumitem tweaks and no \section
% redefinitions. eccv.sty already loads amsmath, amssymb, xcolor, lineno and cite.

\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,arrows.meta,backgrounds,patterns,fit}

\definecolor{meridianblue}{RGB}{27,53,95}
\definecolor{apexteal}{RGB}{32,116,108}
\definecolor{softgray}{RGB}{95,95,95}

\newcommand{\methodname}{SparseFlow}
