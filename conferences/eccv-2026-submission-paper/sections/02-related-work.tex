\section{Related Work}
\textbf{Memory-based video segmentation.} Space-time correspondence networks store per-frame keys and values and retrieve them with dense attention at every decoding step~\cite{castillo2022stcn}. Follow-up work reduces memory growth by periodically compacting the bank~\cite{adebayo2024xmem} or by associating tracked objects with transformer queries that persist across frames~\cite{nathan2024aot}. Decoupled approaches separate tracking from mask decoding entirely, trading some accuracy for open-world generalization~\cite{reyes2023deva}. All of these methods retain dense attention over the retained memory, which is precisely the cost \methodname{} targets.

\textbf{Sparse and efficient attention.} Sparsity has been used to accelerate transformers in classification and detection by restricting attention to local windows or a fixed stride~\cite{blake2021swin}, and a broader survey catalogs efficient-attention variants for dense vision tasks~\cite{osei2021efficient}. Closest to our setting, learned sparse patterns for dense prediction select keys via a content-dependent gate rather than a fixed geometric pattern~\cite{park2023sparse}, though this line of work considers a single input modality. \methodname{} extends learned sparsity to a joint appearance-motion memory, which is, to our knowledge, not addressed by prior sparse-attention formulations for VOS.

\textbf{Cross-modal fusion.} Referring video segmentation fuses visual and linguistic features through cross-attention blocks that remain dense over the visual tokens~\cite{ferreira2022crossmodal}. Earlier recurrent memory formulations propagate a single fused hidden state across frames rather than an explicit key-value store~\cite{delacroix2020memnet}. \methodname{} differs in fusing modalities \emph{before} the sparsity decision, so the relevance predictor --- not a downstream attention block --- is what reconciles appearance and motion.
