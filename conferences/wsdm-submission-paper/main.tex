\documentclass[10pt,twocolumn]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{letterpaper,top=0.85in,bottom=0.85in,left=0.75in,right=0.75in,columnsep=0.25in}
\usepackage{mathptmx}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{array}
\usepackage[table]{xcolor}
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,decorations.pathmorphing,backgrounds,patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usepackage{tcolorbox}
\usepackage{enumitem}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{filecontents}
\usepackage{natbib}

\definecolor{wsdmblue}{RGB}{22,54,102}
\definecolor{wsdmteal}{RGB}{31,122,140}
\definecolor{wsdmgray}{RGB}{95,95,100}
\definecolor{wsdmaccent}{RGB}{196,132,42}

\usepackage[colorlinks=true,linkcolor=wsdmblue,citecolor=wsdmblue,urlcolor=wsdmblue]{hyperref}

\begin{filecontents*}{wsdmrefs.bib}
@inproceedings{Renner2019Session,
  author    = {Renner, Elias and Whitfield, Sara and Okafor, Chidi},
  title     = {Session Graph Embeddings for Sparse Click Prediction},
  booktitle = {Proceedings of the ACM International Conference on Web Search and Data Mining},
  year      = {2019},
  pages     = {112--121}
}
@inproceedings{Lindqvist2020Contrastive,
  author    = {Lindqvist, Dorothea and Marsh, Elena},
  title     = {Contrastive Pretraining for Query Representation Learning},
  booktitle = {Proceedings of the International ACM SIGIR Conference on Research and Development in Information Retrieval},
  year      = {2020},
  pages     = {305--314}
}
@article{Okonkwo2018LongTail,
  author  = {Okonkwo, Ifeoma and Bauer, Jonas},
  title   = {Modeling the Long Tail: Sparse Query Reformulation in Web Search Logs},
  journal = {ACM Transactions on Information Systems},
  volume  = {36},
  number  = {4},
  pages   = {1--29},
  year    = {2018}
}
@inproceedings{Feld2017ClickModel,
  author    = {Feld, Marcus and Sundqvist, Ingrid},
  title     = {A Cascade Click Model for Position-Biased Search Logs},
  booktitle = {Proceedings of the ACM International Conference on Web Search and Data Mining},
  year      = {2017},
  pages     = {45--54}
}
@inproceedings{Rajan2021GraphRank,
  author    = {Rajan, Kavi and Feldman, Otto and Csik{\'o}s, Anna},
  title     = {GraphRank: Learning to Rank over Heterogeneous Query-Session Graphs},
  booktitle = {Proceedings of the ACM International Conference on Knowledge Discovery and Data Mining},
  year      = {2021},
  pages     = {1420--1430}
}
@inproceedings{Whitfield2016RankNet,
  author    = {Whitfield, Sara and Delacroix, Henri},
  title     = {Revisiting Pairwise Ranking Losses for Sponsored Search Retrieval},
  booktitle = {Proceedings of the International Conference on World Wide Web},
  year      = {2016},
  pages     = {733--742}
}
@inproceedings{Marsh2022BertRetrieval,
  author    = {Marsh, Elena and Okafor, Chidi and Renner, Elias},
  title     = {Dense Retrieval with Transformer Encoders under Query Sparsity},
  booktitle = {Proceedings of the ACM International Conference on Information and Knowledge Management},
  year      = {2022},
  pages     = {2210--2219}
}
@article{Sundqvist2015Debiasing,
  author  = {Sundqvist, Ingrid and Okonkwo, Ifeoma},
  title   = {Debiasing Implicit Feedback in Learning-to-Rank Systems},
  journal = {ACM Transactions on the Web},
  volume  = {9},
  number  = {3},
  pages   = {1--24},
  year    = {2015}
}
@inproceedings{Bauer2019GNN,
  author    = {Bauer, Jonas and Csik{\'o}s, Anna and Lindqvist, Dorothea},
  title     = {Graph Neural Networks for Query Auto-Completion at Scale},
  booktitle = {Proceedings of the ACM International Conference on Web Search and Data Mining},
  year      = {2019},
  pages     = {201--210}
}
@inproceedings{Delacroix2020Evaluation,
  author    = {Delacroix, Henri and Feld, Marcus},
  title     = {Offline Evaluation Pitfalls in Session-Based Recommendation},
  booktitle = {Proceedings of the ACM Conference on Recommender Systems},
  year      = {2020},
  pages     = {88--97}
}
@inproceedings{Csikos2021Reformulation,
  author    = {Csik{\'o}s, Anna and Rajan, Kavi},
  title     = {Neural Query Reformulation with Reinforcement Signals from Click Graphs},
  booktitle = {Proceedings of the International ACM SIGIR Conference on Research and Development in Information Retrieval},
  year      = {2021},
  pages     = {1610--1619}
}
@inproceedings{Feldman2018Embeddings,
  author    = {Feldman, Otto and Whitfield, Sara},
  title     = {Skip-Gram Query Embeddings for Session-Aware Search},
  booktitle = {Proceedings of the ACM International Conference on Web Search and Data Mining},
  year      = {2018},
  pages     = {330--339}
}
\end{filecontents*}

\titleformat{\section}
  {\normalfont\large\bfseries\color{wsdmblue}}
  {\thesection}{0.6em}{}[{\vspace{1pt}\color{wsdmaccent!75!black}\titlerule[0.7pt]}]
\titleformat{\subsection}
  {\normalfont\normalsize\bfseries\color{wsdmblue}}
  {\thesubsection}{0.5em}{}
\titlespacing*{\section}{0pt}{10pt}{5pt}
\titlespacing*{\subsection}{0pt}{7pt}{3pt}

\captionsetup{font=small,labelfont={bf,color=wsdmblue},labelsep=colon}

\begin{document}

\twocolumn[
  \begin{@twocolumnfalse}
  \begin{center}
    {\LARGE\bfseries\color{wsdmblue}
      Contrastive Session Graphs for Long-Tail Query Reformulation in Web Search
    \par}
    \vspace{10pt}
    {\large
      Elena Marsh\textsuperscript{1} \quad
      Kavi Rajan\textsuperscript{2} \quad
      Dorothea Lindqvist\textsuperscript{1} \quad
      Marcus Feld\textsuperscript{3}
    \par}
    \vspace{4pt}
    {\small\color{wsdmgray}
      \textsuperscript{1}Search \& Discovery Group, Nexa Research \quad
      \textsuperscript{2}Department of Computer Science, Vertex University \quad
      \textsuperscript{3}Data Systems Lab, Meridian Institute of Technology
    \par}
    \vspace{2pt}
    {\small\ttfamily\color{wsdmgray}
      \{e.marsh,d.lindqvist\}@nexaresearch.example \quad
      k.rajan@vertex.edu \quad
      m.feld@meridian-tech.example
    \par}
  \end{center}
  \vspace{6pt}

  \noindent
  \begin{center}
  \begin{minipage}{0.96\linewidth}
    \small\raggedright
    {\color{wsdmaccent!75!black}\rule{\linewidth}{0.7pt}}
    \par\vspace{4pt}
    \faFile\ \textbf{Unofficial template.} This document is an independent,
    community-produced \LaTeX{} template inspired by the general style of web search and
    data mining conference submissions. It is \textbf{not affiliated with, endorsed by, or
    derived from official ACM WSDM style files}. Authors preparing a real submission
    \textbf{must} download the current-year official style package from the WSDM call for
    papers and follow its formatting instructions exactly.
    \par\vspace{4pt}
    {\color{wsdmaccent!75!black}\rule{\linewidth}{0.7pt}}
  \end{minipage}
  \end{center}
  \vspace{4pt}

  \begin{abstract}
  \noindent
  Search sessions for long-tail queries are dominated by sparse, noisy click signals,
  which makes learning reliable query reformulation policies difficult for standard
  learning-to-rank pipelines. We propose \textbf{CSG} (Contrastive Session Graphs), a
  framework that represents each search session as a heterogeneous graph over queries,
  clicked results, and reformulation edges, and trains a graph encoder with a
  session-level contrastive objective to pull semantically equivalent reformulations
  together while pushing apart unrelated queries sharing only surface tokens. A
  lightweight scoring head then ranks candidate reformulations conditioned on the
  session encoding. On two web-scale session benchmarks, CSG improves NDCG@10 by
  \textbf{6.1--8.4} points and MRR by \textbf{5.3--7.0} points over strong graph and
  transformer baselines, with the largest gains concentrated on queries observed fewer
  than five times in training. We release ablations isolating the contribution of the
  graph structure, the contrastive objective, and session length, and discuss failure
  modes on adversarially reformulated queries.
  \end{abstract}

  \vspace{2pt}
  \noindent{\small\textbf{Keywords:} query reformulation, session modeling, contrastive
  learning, graph neural networks, long-tail search}

  \vspace{10pt}
  \end{@twocolumnfalse}
]

\section{Introduction}

Web search engines routinely observe queries for which they have little or no
historical click evidence. Head queries benefit from dense click-through data that
lets ranking and reformulation models learn robust relevance signals, but the long
tail --- queries issued a handful of times a month, or once ever --- offers almost
none of that signal. Reformulation systems trained primarily on head-query behavior
tend to propose surface-level rewrites (synonym substitution, spelling correction)
that fail to capture the intent shifts users actually make in sparse-click sessions,
such as narrowing a broad exploratory query into a specific navigational one.

We argue that the session, rather than the individual query, is the right unit of
supervision for long-tail reformulation. Even when a single query has never been seen
before, the \emph{session} it appears in --- the sequence of prior queries, clicked
results, and dwell signals --- is often locally dense enough to support learning. Our
approach, CSG, builds a session graph that connects queries to the documents they
click and to one another via reformulation edges, then learns representations with a
contrastive objective that treats within-session reformulation pairs as positives and
cross-session queries with superficial token overlap as hard negatives.

This paper makes the following contributions:

\begin{itemize}[leftmargin=*,itemsep=1pt,topsep=2pt]
  \item A session graph construction procedure that unifies query, document, and
    reformulation nodes into a single heterogeneous structure amenable to message
    passing (\S\ref{sec:method}).
  \item A contrastive training objective, Eq.~\eqref{eq:contrastive}, that is robust
    to the extreme click sparsity of long-tail sessions, together with a ranking head
    for scoring candidate reformulations, Eq.~\eqref{eq:score}.
  \item An evaluation on two session benchmarks showing consistent gains over
    click-model, learning-to-rank, and transformer-retrieval baselines, with ablations
    isolating which components drive the improvement (\S\ref{sec:experiments},
    \S\ref{sec:results}).
\end{itemize}

The remainder of the paper is organized as follows. \S\ref{sec:related} surveys prior
work on session modeling, click models, and contrastive representation learning for
information retrieval. \S\ref{sec:method} describes the CSG architecture and training
objective. \S\ref{sec:experiments} details our experimental setup, and
\S\ref{sec:results} reports results. \S\ref{sec:discussion} analyzes ablations and
failure cases, and \S\ref{sec:conclusion} concludes.

\section{Related Work}
\label{sec:related}

\paragraph{Click models and position bias.} Cascade-style click models
\citep{Feld2017ClickModel} and their debiased extensions \citep{Sundqvist2015Debiasing}
remain the dominant tool for turning implicit feedback into training signal, but they
are typically fit per-query and degrade sharply when a query is rare or unseen, which
is precisely the regime we target.

\paragraph{Session-based retrieval and reformulation.} Sequence models over query
histories \citep{Feldman2018Embeddings} and reinforcement-signal reformulation
\citep{Csikos2021Reformulation} both use session context, but treat the session as a
flat sequence rather than a graph with explicit click and reformulation edges. Graph
neural approaches to search have mostly targeted auto-completion
\citep{Bauer2019GNN} or general-purpose ranking \citep{Rajan2021GraphRank} rather than
reformulation specifically.

\paragraph{Contrastive representation learning for IR.} Contrastive pretraining has
improved dense query and document representations \citep{Lindqvist2020Contrastive}
and transformer-based retrieval under sparsity \citep{Marsh2022BertRetrieval}, but
these methods operate at the query-pair level and do not exploit the graph structure
of an entire session, which is the gap CSG addresses.

\paragraph{Long-tail modeling and evaluation.} \citet{Okonkwo2018LongTail} document
the extent of long-tail sparsity in production search logs, and
\citet{Delacroix2020Evaluation} caution against offline evaluation protocols that
implicitly favor head-heavy test splits; we adopt their stratified evaluation
recommendation in \S\ref{sec:experiments}. Pairwise ranking losses
\citep{Whitfield2016RankNet} and skip-gram query embeddings
\citep{Feldman2018Embeddings} form two of our baselines.

\section{Method}
\label{sec:method}

\subsection{Session Graph Construction}

For each session $s$, we construct a heterogeneous graph $G_s = (V_s, E_s)$ with two
node types --- query nodes $Q_s$ and document nodes $D_s$ --- and three edge types:
\emph{click} edges between a query and the documents it received clicks on,
\emph{co-session} edges between temporally adjacent queries, and \emph{reformulation}
edges between a query and the immediately following query when the user's intent is
judged unresolved (operationalized as a click followed by a same-session repeat
search within a 90-second window). Query nodes are initialized with subword
embeddings pooled over the query string; document nodes are initialized with title
and snippet embeddings from a frozen bi-encoder.

\subsection{Graph Encoder}

We apply $L{=}3$ layers of relational message passing over $G_s$, producing a
contextual embedding $\mathbf{q}_i \in \mathbb{R}^d$ for every query node $i$ that
aggregates information from its clicked documents and neighboring queries in the
session. Unlike a purely sequential encoder, this lets a query late in the session
influence, and be influenced by, an earlier query connected through a shared clicked
document even if the two are not adjacent in time.

\subsection{Contrastive Objective}

Given an anchor query $i$ and its true reformulation target $i^+$ (the next query in
the same session), we treat all other queries in the training batch as negatives and
minimize the InfoNCE-style loss

\begin{equation}
\mathcal{L}_{\mathrm{sess}} \;=\; -\log
\frac{\exp\!\big(\mathrm{sim}(\mathbf{q}_i, \mathbf{q}_{i^+}) / \tau\big)}
     {\displaystyle\sum_{j=1}^{N} \exp\!\big(\mathrm{sim}(\mathbf{q}_i, \mathbf{q}_j) / \tau\big)},
\label{eq:contrastive}
\end{equation}

where $\mathrm{sim}(\cdot,\cdot)$ is cosine similarity, $\tau$ is a learned
temperature, and $N$ is the batch size. Crucially, negatives are drawn to include
queries that share surface tokens with the anchor but come from unrelated sessions,
which forces the encoder to rely on session-graph structure rather than lexical
overlap alone.

\subsection{Reformulation Scoring}

At inference time, given a partial session ending at query $i$ and a candidate
reformulation $c$ drawn from a retrieved shortlist, we score $c$ as

\begin{equation}
\mathrm{score}(i, c) \;=\; \sigma\!\Big(\mathbf{w}^{\top}
  \big[\mathbf{q}_i \,;\, \mathbf{q}_c \,;\, \mathbf{q}_i \odot \mathbf{q}_c\big]
  + b\Big),
\label{eq:score}
\end{equation}

with $\sigma$ the logistic function, $\mathbf{w}$ and $b$ learned parameters, and
$\odot$ the elementwise product. Candidates are ranked by $\mathrm{score}(i,\cdot)$
and the top-$k$ are returned as suggested reformulations.

\section{Experiments}
\label{sec:experiments}

\subsection{Datasets}

We evaluate on two session benchmarks. \textbf{NexaQL} is a sample of 2.4M sessions
from a general web search log, stratified so that 40\% of test-set queries are
long-tail (fewer than five historical occurrences). \textbf{MeridianSessions} is a
smaller, denser benchmark of 310K sessions drawn from a vertical shopping search
engine, used to test transfer to a narrower domain. Both are split temporally into
train/validation/test with a two-week gap to avoid session leakage, following the
protocol of \citet{Delacroix2020Evaluation}.

\subsection{Baselines}

We compare against \textbf{BM25} query expansion, \textbf{RankNet-Session}
\citep{Whitfield2016RankNet} adapted to session features, \textbf{GraphRank}
\citep{Rajan2021GraphRank}, and \textbf{LongTail-BERT}, a transformer bi-encoder
fine-tuned with the contrastive pretraining recipe of
\citet{Lindqvist2020Contrastive}. All baselines are retuned on our splits with the
same candidate shortlist as CSG for a fair comparison.

\subsection{Implementation Details}

The graph encoder uses hidden dimension $d{=}256$, $L{=}3$ relational graph
convolution layers, and dropout $0.1$. We train with Adam at learning rate
$3\times10^{-4}$, batch size $512$ sessions, and temperature $\tau$ initialized at
$0.07$ and learned. Training runs for $20$ epochs with early stopping on validation
NDCG@10; all reported results are averaged over five seeds.

\section{Results}
\label{sec:results}

Table~\ref{tab:results} reports ranking quality on both benchmarks. CSG outperforms
every baseline on both datasets, with the margin widening on the long-tail stratum
of NexaQL, consistent with our hypothesis that session-graph structure is most
valuable exactly where per-query click history is thinnest.

\begin{table*}[t]
\centering
\small
\caption{Reformulation ranking quality on the full test set of each benchmark. Best
result per column in bold; CSG is our method.}
\label{tab:results}
\begin{tabular}{lcccccc}
\toprule
& \multicolumn{3}{c}{\textbf{NexaQL}} & \multicolumn{3}{c}{\textbf{MeridianSessions}} \\
\cmidrule(lr){2-4} \cmidrule(lr){5-7}
\textbf{Method} & NDCG@10 & MRR & Recall@20 & NDCG@10 & MRR & Recall@20 \\
\midrule
BM25 expansion              & 0.312 & 0.284 & 0.441 & 0.356 & 0.318 & 0.489 \\
RankNet-Session \citep{Whitfield2016RankNet} & 0.358 & 0.331 & 0.487 & 0.402 & 0.365 & 0.531 \\
GraphRank \citep{Rajan2021GraphRank}         & 0.391 & 0.360 & 0.512 & 0.428 & 0.389 & 0.556 \\
LongTail-BERT \citep{Lindqvist2020Contrastive} & 0.407 & 0.374 & 0.529 & 0.441 & 0.401 & 0.568 \\
\midrule
\rowcolor{wsdmblue!8}
\textbf{CSG (ours)} & \textbf{0.468} & \textbf{0.427} & \textbf{0.583} & \textbf{0.487} & \textbf{0.444} & \textbf{0.611} \\
\bottomrule
\end{tabular}
\end{table*}

Figure~\ref{fig:bars} breaks down NDCG@10 on NexaQL by query frequency stratum. The
gap between CSG and the strongest baseline, LongTail-BERT, is largest in the sparsest
bucket ($<5$ historical occurrences), where CSG improves NDCG@10 by 8.4 points versus
a 3.1-point gap in the head-query bucket.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  ybar,
  bar width=9pt,
  width=\linewidth,
  height=4.4cm,
  ymin=0,ymax=0.6,
  ylabel={NDCG@10},
  symbolic x coords={$<$5,5--50,$>$50},
  xtick=data,
  xlabel={Historical query frequency},
  enlarge x limits=0.28,
  legend style={at={(0.5,1.18)},anchor=north,legend columns=-1,font=\scriptsize,draw=none},
  ylabel style={font=\scriptsize},
  xlabel style={font=\scriptsize},
  tick label style={font=\scriptsize},
  axis line style={wsdmgray},
  ]
\addplot[fill=wsdmgray!55,draw=wsdmgray] coordinates {($<$5,0.352) (5--50,0.421) ($>$50,0.489)};
\addplot[fill=wsdmaccent!85,draw=wsdmaccent!60!black] coordinates {($<$5,0.436) (5--50,0.464) ($>$50,0.502)};
\legend{LongTail-BERT,CSG (ours)}
\end{axis}
\end{tikzpicture}
\caption{NDCG@10 on NexaQL by query frequency stratum. CSG's advantage grows as
click history becomes sparser.}
\label{fig:bars}
\end{figure}

\section{Discussion}
\label{sec:discussion}

\subsection{Ablations}

Removing the reformulation edges from the session graph (keeping only click and
co-session edges) drops NDCG@10 on NexaQL from $0.468$ to $0.429$, confirming that
explicit reformulation structure carries signal beyond temporal adjacency. Replacing
the contrastive objective in Eq.~\eqref{eq:contrastive} with a standard
cross-entropy classification loss over the shortlist drops NDCG@10 to $0.417$,
roughly halving CSG's margin over LongTail-BERT; hard negatives with lexical overlap
appear to be the main source of this gain, matching the intuition in
\citet{Csikos2021Reformulation} that surface-level negatives are otherwise easy for
the model to ignore.

\subsection{Failure Modes}

CSG underperforms BM25 expansion on a small subset ($\approx$3\% of test sessions)
where the true reformulation is a near-exact spelling correction with no semantic
shift; the contrastive objective, tuned to separate semantically distinct queries,
occasionally over-separates queries that differ by a single character. We view
combining CSG with a lightweight edit-distance prior as promising future work.

\subsection{Limitations}

Our session graphs are built from a 90-second reformulation window, which is a
reasonable default but was not tuned per-vertical; MeridianSessions, with generally
shorter dwell times, may benefit from a tighter window. We also do not model
cross-device sessions, which could recover additional long-tail signal at the cost of
substantially more complex identity resolution.

\section{Conclusion}
\label{sec:conclusion}

We presented CSG, a session-graph contrastive framework for long-tail query
reformulation, and showed that modeling the full session as a heterogeneous graph,
combined with a hard-negative contrastive objective, yields consistent gains over
click-model, learning-to-rank, and transformer-retrieval baselines, with the largest
improvements exactly where prior click history is thinnest. Future work includes
extending the graph to cross-device sessions and combining CSG with lightweight
lexical priors to close the small gap observed on near-exact spelling corrections.

\section*{Acknowledgments}
{\small We thank the Nexa Research search infrastructure team for log access and
tooling support, and our anonymous reviewers for feedback that improved the
long-tail evaluation protocol used in \S\ref{sec:experiments}.}

\bibliographystyle{plainnat}
\bibliography{wsdmrefs}

\end{document}
