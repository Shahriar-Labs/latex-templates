% Packages, colours and custom commands for this template.
% ─── Core packages ───────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\usepackage{lmodern}
\usepackage{microtype}

% ─── Page geometry ───────────────────────────────────────────────────────────
\usepackage[
  top=25mm, bottom=25mm,
  left=25mm, right=25mm,
  headheight=14pt
]{geometry}

% ─── Colors ──────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{dfgblue}{RGB}{0,62,116}
\definecolor{dfgaccent}{RGB}{0,130,186}
\definecolor{dfggold}{RGB}{186,143,0}
\definecolor{dfglightgray}{RGB}{245,247,250}
\definecolor{dfgmidgray}{RGB}{180,188,198}
\definecolor{dfgdarkgray}{RGB}{70,80,92}
\definecolor{dfgsuccess}{RGB}{30,130,80}

% ─── Math ────────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

% ─── Tables ──────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{array}
\usepackage{multirow}

% ─── Graphics & TikZ ─────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,decorations.pathmorphing}

% ─── tcolorbox ───────────────────────────────────────────────────────────────
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable,theorems}

% ─── Typography & layout ─────────────────────────────────────────────────────
\usepackage{parskip}
\usepackage{setspace}
\usepackage{enumitem}
\usepackage{ragged2e}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{fontawesome5}
\usepackage{mdframed}
\usepackage{calc}
\usepackage{etoolbox}

% ─── Hyperref (last) ─────────────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=dfgblue,
  urlcolor=dfgaccent,
  citecolor=dfgblue,
  pdftitle={DFG Project Description -- Adaptive Quantum Sensing Networks},
  pdfauthor={Dr. Elena Hartmann},
  pdfsubject={DFG Research Grant Application}
]{hyperref}

% ─── Section formatting ──────────────────────────────────────────────────────
\titleformat{\section}
  {\color{dfgblue}\large\bfseries}
  {\thesection.}{0.6em}{}
  [\vspace{-4pt}\color{dfgaccent}\rule{\linewidth}{1.2pt}\vspace{2pt}]

\titleformat{\subsection}
  {\color{dfgdarkgray}\normalsize\bfseries}
  {\thesubsection}{0.5em}{}

\titleformat{\subsubsection}
  {\color{dfgdarkgray}\normalsize\itshape}
  {\thesubsubsection}{0.5em}{}

\titlespacing*{\section}{0pt}{18pt}{8pt}
\titlespacing*{\subsection}{0pt}{12pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{8pt}{2pt}

% ─── Header / footer ─────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \small\color{dfgdarkgray}%
  \textbf{DFG Project Description} \textbar{} Adaptive Quantum Sensing Networks%
}
\fancyhead[R]{%
  \small\color{dfgdarkgray}Dr.\ Elena Hartmann%
}
\fancyfoot[C]{%
  \small\color{dfgmidgray}Page \thepage\ of \pageref{LastPage}%
}

% ─── Custom tcolorbox styles ─────────────────────────────────────────────────
\tcbset{
  dfgbox/.style={
    enhanced,
    colback=dfglightgray,
    colframe=dfgblue,
    fonttitle=\bfseries\color{white},
    colbacktitle=dfgblue,
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={colframe=dfgblue, sharp corners},
    sharp corners,
    top=4mm, bottom=4mm, left=4mm, right=4mm,
    breakable
  },
  accentbox/.style={
    enhanced,
    colback=white,
    colframe=dfgaccent,
    left=4mm, right=4mm, top=3mm, bottom=3mm,
    borderline west={3pt}{0pt}{dfgaccent},
    sharp corners,
    breakable
  },
  goldbox/.style={
    enhanced,
    colback=white,
    colframe=dfggold,
    left=4mm, right=4mm, top=3mm, bottom=3mm,
    borderline west={3pt}{0pt}{dfggold},
    sharp corners,
    breakable
  }
}

% ─── Utility commands ────────────────────────────────────────────────────────
\newcommand{\sectionicon}[1]{\textcolor{dfgaccent}{#1}\hspace{4pt}}
\newcommand{\keyword}[1]{\textbf{\textcolor{dfgblue}{#1}}}
\newcommand{\dfgsep}{\vspace{6pt}\noindent\color{dfgmidgray}\rule{\linewidth}{0.4pt}\vspace{6pt}}

% ─────────────────────────────────────────────────────────────────────────────
\usepackage{eurosym}
