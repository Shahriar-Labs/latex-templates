% Packages, colours and custom commands for this template.
%% ─── Core packages ──────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[UKenglish]{babel}
\usepackage{lmodern}
\usepackage{microtype}

%% ─── Page geometry ───────────────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=25mm, bottom=25mm,
  left=25mm, right=25mm,
  headheight=14pt
]{geometry}

%% ─── Colour ──────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{ukriblue}{HTML}{002147}
\definecolor{ukripink}{HTML}{C8097A}
\definecolor{ukricyan}{HTML}{00AEEF}
\definecolor{ukrigray}{HTML}{F4F5F7}
\definecolor{textgray}{HTML}{4A4A4A}
\definecolor{rulegray}{HTML}{CCCCCC}

%% ─── Typography ──────────────────────────────────────────────────────────────
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{setspace}
\setstretch{1.15}

%% ─── Math ────────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

%% ─── Tables ──────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{colortbl}
\usepackage{multirow}
\usepackage{longtable}

%% ─── Lists ───────────────────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.4em, topsep=3pt, itemsep=2pt, parsep=0pt}
\setlist[enumerate]{leftmargin=1.6em, topsep=3pt, itemsep=2pt, parsep=0pt}

%% ─── Graphics & TikZ ─────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning, calc, shapes.geometric, decorations.pathmorphing, backgrounds, patterns}

%% ─── tcolorbox ───────────────────────────────────────────────────────────────
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, most}

%% ─── Headers / footers ───────────────────────────────────────────────────────
\usepackage{fancyhdr}
\usepackage{lastpage}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \textcolor{ukriblue}{\footnotesize\textbf{UKRI Grant Proposal}} \textcolor{rulegray}{|}
  \textcolor{textgray}{\footnotesize\ Synapse Biomedical Technologies Ltd}
}
\fancyhead[R]{%
  \textcolor{textgray}{\footnotesize Application Ref:\ \textbf{MR/X0042187/1}}
}
\fancyfoot[C]{%
  \textcolor{rulegray}{\small\thepage\ of \pageref{LastPage}}
}

%% ─── Hyperref ────────────────────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=ukriblue,
  urlcolor=ukripink,
  citecolor=ukricyan,
  pdftitle={UKRI Grant Proposal -- Neural Scaffold Regeneration},
  pdfauthor={Dr Eleanor Hartley},
  pdfsubject={MRC Biomedical Research},
  pdfkeywords={neural regeneration, biomaterials, UKRI, MRC}
]{hyperref}

%% ─── Misc ────────────────────────────────────────────────────────────────────
\usepackage{parskip}
\setlength{\parskip}{6pt}
\usepackage{calc}
\usepackage{ifthen}
\usepackage{float}
\usepackage{caption}
\captionsetup{font=small, labelfont={bf,color=ukriblue}, textfont={color=textgray}}

%% ─── Custom tcolorbox styles ─────────────────────────────────────────────────
\tcbset{
  sectionbox/.style={
    enhanced,
    colback=ukrigray,
    colframe=ukriblue,
    fonttitle=\bfseries\color{white},
    coltitle=white,
    attach boxed title to top left={xshift=8mm, yshift=-3mm},
    boxed title style={
      colback=ukriblue,
      colframe=ukriblue,
      sharp corners,
      left=4pt, right=4pt, top=2pt, bottom=2pt
    },
    sharp corners,
    left=8pt, right=8pt, top=10pt, bottom=8pt,
    boxrule=0.5pt,
    breakable
  },
  infobox/.style={
    enhanced,
    colback=white,
    colframe=ukripink,
    left=6pt, right=6pt, top=6pt, bottom=6pt,
    boxrule=1.5pt,
    sharp corners,
    breakable
  },
  highlightbox/.style={
    enhanced,
    colback=ukriblue!8,
    colframe=ukricyan,
    left=6pt, right=6pt, top=6pt, bottom=6pt,
    boxrule=1pt,
    sharp corners,
    breakable
  }
}

%% ─── Section formatting ──────────────────────────────────────────────────────
\usepackage[explicit]{titlesec}
\titleformat{\section}
  {\color{ukriblue}\large\bfseries}
  {}
  {0pt}
  {\colorbox{ukriblue}{\parbox{\dimexpr\linewidth-2\fboxsep\relax}%
    {\color{white}\thesection\quad #1}}}
  [\vspace{2pt}]

\titleformat{\subsection}
  {\color{ukriblue}\normalsize\bfseries}
  {\color{ukripink}\thesubsection.}
  {0.5em}
  {}
  [\color{ukripink}\hrule height 0.8pt\vspace{2pt}]

\titleformat{\subsubsection}
  {\color{textgray}\normalsize\bfseries\itshape}
  {}
  {0pt}
  {}

\titlespacing*{\section}{0pt}{14pt}{6pt}
\titlespacing*{\subsection}{0pt}{10pt}{4pt}
\titlespacing*{\subsubsection}{0pt}{8pt}{2pt}

%% ─── Convenience macros ──────────────────────────────────────────────────────
\newcommand{\pinkbullet}{\textcolor{ukripink}{\textbullet}\enspace}
\newcommand{\bluebullet}{\textcolor{ukricyan}{\textbullet}\enspace}
\newcommand{\fieldlabel}[1]{\textcolor{ukriblue}{\textbf{#1}}}
\newcommand{\pinktag}[1]{%
  \begin{tikzpicture}[baseline=-0.5ex]
    \node[fill=ukripink, text=white, font=\scriptsize\bfseries,
          inner xsep=4pt, inner ysep=2pt, rounded corners=2pt] {#1};
  \end{tikzpicture}%
}
\newcommand{\cyantag}[1]{%
  \begin{tikzpicture}[baseline=-0.5ex]
    \node[fill=ukricyan, text=white, font=\scriptsize\bfseries,
          inner xsep=4pt, inner ysep=2pt, rounded corners=2pt] {#1};
  \end{tikzpicture}%
}

%% ═══════════════════════════════════════════════════════════════════════════
