% Packages, colours and custom commands for this template.
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[scaled=0.95]{helvet}
\renewcommand{\familydefault}{\sfdefault}

% Geometry with standard Horizon Europe MSCA margins
\usepackage[margin=1.5cm,includeheadfoot]{geometry}

% Color configuration
\usepackage[table,svgnames]{xcolor}
\definecolor{MSCABlue}{HTML}{1A365D}
\definecolor{MSCAGold}{HTML}{B8860B}
\definecolor{MSCALightGray}{HTML}{F8FAFC}
\definecolor{MSCADarkGray}{HTML}{334155}
\definecolor{MSCABorder}{HTML}{E2E8F0}

% Standard mathematical packages
\usepackage{amsmath,amssymb}

% Table packages
\usepackage{booktabs}
\usepackage{tabularx}

% Header and footer configuration
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\fancyhead[L]{\fontsize{9}{11}\selectfont \color{MSCADarkGray} HORIZON-MSCA-PF-2026 -- Part B1}
\fancyhead[R]{\fontsize{9}{11}\selectfont \color{MSCADarkGray} Acronym: SYNAPSE-NET}
\fancyfoot[C]{\fontsize{9}{11}\selectfont \color{MSCADarkGray} Page \thepage}

% Headings styling
\usepackage{titlesec}
\titleformat{\section}
  {\color{MSCABlue}\normalfont\large\bfseries}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\color{MSCAGold}\normalfont\normalsize\bfseries}
  {\thesubsection}{1em}{}
\titleformat{\subsubsection}
  {\color{MSCADarkGray}\normalfont\normalsize\bfseries\itshape}
  {\thesubsubsection}{1em}{}

% Lists formatting
\usepackage{enumitem}
\setlist[itemize]{noitemsep, topsep=2pt}
\setlist[enumerate]{noitemsep, topsep=2pt}

% TikZ configuration
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,decorations.pathmorphing,backgrounds,patterns}

% tcolorbox configuration
\usepackage[most]{tcolorbox}
\newtcolorbox{mscabox}[2][]{
  colback=MSCALightGray,
  colframe=MSCABlue,
  coltitle=white,
  fonttitle=\bfseries\color{white},
  title=#2,
  arc=2mm,
  boxrule=1pt,
  left=3mm, right=3mm, top=2mm, bottom=2mm,
  enhanced,
  attach boxed title to top left={yshift=-2.5mm, xshift=4mm},
  boxed title style={colback=MSCAGold, colframe=MSCAGold, arc=1mm},
  #1
}

% Fontawesome5 for icons
\usepackage{fontawesome5}

% Hyperref (should be loaded last)
\usepackage[colorlinks=true, linkcolor=MSCABlue, citecolor=MSCABlue, urlcolor=MSCAGold]{hyperref}
