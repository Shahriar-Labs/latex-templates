\section{Positioning of This Work}

ATS is positioned deliberately between the two extremes surveyed above: it
retains a forecasting signal, but restricts it to an exponential-smoothing
estimator cheap enough to run on coordinator-tier hardware, and pairs it
with a bounded feedback controller designed explicitly to damp oscillation
under correlated node failures.
