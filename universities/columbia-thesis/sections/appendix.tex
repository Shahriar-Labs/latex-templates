\chapter{Log Compaction}
\label{app:a}

The replicated task table is compacted by snapshotting it every 10,000 entries, which bounds
recovery time for a restarted consensus member to under two seconds in our deployment.
