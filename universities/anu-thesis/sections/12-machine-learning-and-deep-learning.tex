\section{Machine Learning and Deep Learning}

The past decade has seen an explosion of machine-learning applications in
ecology, driven by the availability of large, remotely sensed datasets.
Ensemble methods such as random forests and extreme gradient boosting (XGBoost)
consistently achieve high predictive accuracy in species distribution modelling
benchmarks~\cite{elith2009}, and convolutional neural networks have been
applied to classify vegetation types from aerial imagery with accuracy
exceeding 90\%~\cite{hoffman2014}.

However, these methods have well-documented limitations for ecological
inference. They are typically \enquote{black boxes} that do not produce
interpretable parameter estimates or rigorous uncertainty intervals.
Extrapolation beyond the range of training data is unreliable, and the
complex interactions learned by deep networks may not correspond to
biologically meaningful processes.

\begin{table}[htbp]
\centering
\caption{Summary of key statistical methods for ecological monitoring,
organised by paradigm.}
\label{tab:studies}
\begin{tabularx}{\textwidth}{@{}>{\color{anucrimson}\bfseries}l
  @{\hspace{6mm}}l@{\hspace{6mm}}>{\raggedright\arraybackslash}X@{}}
\toprule
Method & Complexity & Key Limitation \\
\midrule
GLM / GAM    & Low    & Assumes conditional independence;
                        linear (or smooth) covariate effects \\
MaxEnt       & Medium & Presence-only; sensitive to background sampling \\
Random Forest& Medium & No coherent uncertainty; extrapolation unreliable \\
INLA         & High   & Approximate inference; limited to latent Gaussian models \\
HMC (Stan)   & High   & Computationally intensive; requires explicit model coding \\
\bottomrule
\end{tabularx}
\end{table}
