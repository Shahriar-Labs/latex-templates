\chapter{Geometry-Aware Pre-Training}
\label{ch:method}

Our messages include the angles between adjacent bonds and a flag for ring membership, and the
network is pre-trained to reconstruct masked substructures before it is fine-tuned on each
property.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[node distance=5mm and 7mm,
      box/.style={draw=accent, rounded corners=2pt, minimum height=9mm, align=center, font=\small}]
    \node[box] (u) {Unlabelled\\molecules};
    \node[box, right=of u] (p) {Masked-substructure\\pre-training};
    \node[box, right=of p] (f) {Fine-tuning on\\a property};
    \node[box, right=of f] (c) {Calibrated\\prediction};
    \draw[-{Stealth}] (u) -- (p); \draw[-{Stealth}] (p) -- (f); \draw[-{Stealth}] (f) -- (c);
  \end{tikzpicture}
  \caption{Pre-training on unlabelled molecules before property fine-tuning.}
  \label{fig:pretrain}
\end{figure}
