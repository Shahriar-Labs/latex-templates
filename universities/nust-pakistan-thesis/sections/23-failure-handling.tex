\section{Failure Handling}
If an intent frame is lost, the gateway falls back to a conservative default listen window
sized to $T_{\max}$, guaranteeing eventual reception at the cost of latency rather than
missed data. Section~\ref{sec:robustness} evaluates this fallback under induced packet loss.

% =====================================================
% CHAPTER 4: EXPERIMENTAL SETUP
% =====================================================
