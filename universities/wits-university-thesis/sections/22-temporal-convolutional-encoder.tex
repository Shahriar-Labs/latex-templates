\section{Temporal Convolutional Encoder}
The encoder stacks dilated causal convolutions with residual connections, following the
general TCN design, using dilation factors $\{1, 2, 4, 8, 16\}$ across five residual blocks
with 64 channels each.
