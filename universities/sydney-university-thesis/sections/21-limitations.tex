\section{Limitations}
The dataset, while substantial, is drawn from a single industrial partner; cross-domain
validation is needed before broader claims of generality can be supported. The adaptive
threshold controller was tuned for load variation on the timescale of shifts and days;
its behaviour under abrupt, large-magnitude distribution shift (e.g., a change of product
line) was not evaluated.
