% preamble.tex
% Define custom colors using a high-contrast engineering review theme
\definecolor{primary}{HTML}{0F172A}      % Dark Slate 900 (Deep background, headers)
\definecolor{secondary}{HTML}{0EA5E9}    % Sky Blue 500 (Primary accent, highlights)
\definecolor{accent}{HTML}{6366F1}       % Indigo 500 (Secondary accent)
\definecolor{neutral-light}{HTML}{F8FAFC}% Slate 50 (Slide background)
\definecolor{neutral-dark}{HTML}{334155} % Slate 700 (Body text)
\definecolor{bg-light}{HTML}{FFFFFF}     % White (Card backgrounds)
\definecolor{danger}{HTML}{EF4444}       % Red 500 (Risk high status)
\definecolor{warning}{HTML}{F59E0B}      % Amber 500 (Risk medium status)
\definecolor{success}{HTML}{10B981}      % Emerald 500 (Mitigated status)

% Standard packages
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc, shapes.geometric, backgrounds}
\usepackage{tcolorbox}
\tcbuselibrary{skins}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{fontawesome5}
\usepackage{microtype}

% Configure Beamer theme settings
\setbeamertemplate{navigation symbols}{}

% Typography styling
\usefonttheme{professionalfonts}
\setbeamerfont{title}{size=\huge, series=\bfseries}
\setbeamerfont{frametitle}{size=\Large, series=\bfseries}
\setbeamerfont{framesubtitle}{size=\small, shape=\itshape}

% Beamer color mappings
\setbeamercolor{background canvas}{bg=neutral-light}
\setbeamercolor{normal text}{fg=neutral-dark}
\setbeamercolor{titlelike}{fg=primary}
\setbeamercolor{frametitle}{fg=primary}
\setbeamercolor{item}{fg=secondary}

% Custom modern footer
\setbeamertemplate{footline}{
  \leavevmode%
  \hbox{%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,left]{author in head/foot}%
      \usebeamerfont{author in head/foot}\hspace*{2ex}\textcolor{neutral-dark!70}{Apex Systems -- Tech Review}
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,center]{title in head/foot}%
      \usebeamerfont{title in head/foot}\textcolor{neutral-dark!70}{Nexa Cloud Gateway}
    \end{beamercolorbox}%
    \begin{beamercolorbox}[wd=.333333\paperwidth,ht=2.25ex,dp=1ex,right]{date in head/foot}%
      \usebeamerfont{date in head/foot}\textcolor{neutral-dark!70}{\insertframenumber{} / \inserttotalframenumber}\hspace*{2ex}
    \end{beamercolorbox}%
  }%
  \vskip0pt%
}

% BibLaTeX setup
\usepackage[backend=biber,style=authoryear]{biblatex}
\addbibresource{references.bib}

% Custom modern elements
\newtcolorbox{designcard}[2][]{
  enhanced,
  colback=bg-light,
  colframe=primary!10,
  coltitle=primary,
  fonttitle=\bfseries\large,
  title={#2},
  arc=3mm,
  boxrule=1pt,
  shadow={2pt}{-2pt}{0pt}{black!5},
  left=10pt, right=10pt, top=10pt, bottom=10pt,
  #1
}

% Custom severity badge command for tabularx
\newcommand{\severitybadge}[2]{%
  \tcbox[on line,
         boxrule=0.5pt,
         arc=0.8mm,
         colback=#1!10,
         colframe=#1,
         coltext=#1!80!black,
         left=3pt, right=3pt, top=1.5pt, bottom=1.5pt,
         fontupper=\bfseries\tiny]%
         {#2}%
}
