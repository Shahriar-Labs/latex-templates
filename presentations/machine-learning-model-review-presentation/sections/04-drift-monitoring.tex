\section{Drift \& Production}
\subsection{Out-of-Distribution Checks}

\begin{frame}{Production Drift \& Performance Monitoring}{Simulated Stress Testing on Shifted Distributions}
  We evaluated model robustness against semantic and domain drift using out-of-distribution datasets~\cite{vance2026drift}.
  
  \vspace{0.3cm}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{driftbox}{Drift Risk Assessment}
        \begin{itemize}
          \item \textbf{Domain Shift:} User query distributions are monitored daily.
          \item \textbf{Alert Threshold:} Population Stability Index (PSI) set to 0.25.
          \item \textbf{Current PSI:} Stable at 0.11 under standard user workloads.
        \end{itemize}
      \end{driftbox}
    \end{column}
    \hfill
    \begin{column}{0.48\textwidth}
      \textbf{Mitigation \& Retraining Plan:}
      \begin{itemize}
        \item \textbf{Active Learning:} Dynamic data routing when uncertainty entropy exceeds 0.85.
        \item \textbf{Shadow Deployment:} Parallel pipeline running on 5\% traffic.
        \item \textbf{Fallbacks:} Instant routing to Meridian-I if model latency P99 exceeds 150ms.
      \end{itemize}
    \end{column}
  \end{columns}
\end{frame}
