\begin{frame}{Findings}
  \begin{console}
  \crits{HIGH}   one line each, with the concrete failure\\
  \warns{MEDIUM} what an attacker gets, not what is theoretically possible\\
  \ok{LOW}    kept, because a report of only high findings hides the pattern
  \end{console}
\end{frame}

\begin{frame}{The one that matters}
  \bignumber{1 high}{with a working proof, which is the only category that gets a date on this
  slide. Everything else is scheduled, not promised.}
\end{frame}
