\sectionslide{Migrating}

\begin{frame}{From the old endpoints}
  \begin{console}
  \warns{deprecated} /jobs/:id      \ok{use} /v1/compile/:job\\
  \warns{changed}    error shape    \ok{now} \{ error, code \}\\
  \ok{unchanged}    auth            bearer token, as before
  \end{console}
\end{frame}

\begin{frame}{How long the old one lives}
  \bignumber{6 months}{of overlap, announced with a date rather than "soon". A deprecation
  without a date is one nobody plans around.}
\end{frame}
