\begin{frame}{The surface}
  \begin{console}
  POST /v1/projects        create\\
  GET  /v1/projects/:id    read\\
  POST /v1/compile         build, returns a job\\
  GET  /v1/compile/:job    poll
  \end{console}
\end{frame}

\begin{frame}{What it is for}
  \begin{console}
  \ok{yes} automating something you do by hand today\\
  \ok{yes} wiring the product into a pipeline you already run\\
  \crits{no} a replacement for the editor. Different job.
  \end{console}
\end{frame}
