\begin{frame}{Phases}
  \begin{console}
  \ok{1. shadow}    new path receives copies, serves nothing\\
  \warns{2. partial}   a slice of real traffic, by account\\
  \warns{3. cutover}   everything, with the old path warm\\
  \ok{4. teardown}  only after a quiet week
  \end{console}
\end{frame}

\begin{frame}{Where we are now}
  \bignumber{43\%}{of requests on the new path, with error rates within noise of the old one.
  That comparison is the gate for phase three.}
\end{frame}
