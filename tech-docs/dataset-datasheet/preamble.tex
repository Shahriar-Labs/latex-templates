% Packages, colours and custom commands for this template.
% Dataset Datasheet — LaTeX Template
% Edit, customize, and compile online at https://letx.app
% Compile with: latexmk -pdf main.tex (pdflatex)

% ── Geometry ──
\usepackage[margin=1in,top=0.85in,bottom=0.85in,headheight=14pt]{geometry}
\raggedbottom

% ── Encoding & Font ──
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[default]{sourcesanspro}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{microtype}

% ── Math ──
\usepackage{amsmath}
\usepackage{amssymb}

% ── Colors ──
\usepackage{xcolor}
\definecolor{accent}{HTML}{2563EB}
\definecolor{accentlight}{HTML}{DBEAFE}
\definecolor{dark}{HTML}{1F2937}
\definecolor{midgray}{HTML}{6B7280}
\definecolor{softgray}{HTML}{F9FAFB}
\definecolor{border}{HTML}{E5E7EB}
\definecolor{green}{HTML}{059669}
\definecolor{orange}{HTML}{D97706}
\definecolor{red}{HTML}{DC2626}
\definecolor{purple}{HTML}{7C3AED}

% ── Spacing ──
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\usepackage{ragged2e}

% ── Section Titles ──
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {}
  {0pt}
  {}
  [\vspace{-0.3em}\textcolor{border}{\rule{\linewidth}{1pt}}\vspace{0.6em}]
\titleformat{\subsection}
  {\large\bfseries\color{dark}}
  {}
  {0pt}
  {}
\titlespacing{\section}{0pt}{14pt}{6pt}
\titlespacing{\subsection}{0pt}{12pt}{4pt}

% ── Tables ──
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\renewcommand{\arraystretch}{1.25}
\setlength{\tabcolsep}{6pt}

% ── Lists ──
\usepackage{enumitem}
\setlist{nosep,leftmargin=*,itemsep=1pt,topsep=4pt,partopsep=0pt}

% ── Callout Boxes ──
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\newtcolorbox{motivationbox}{
  enhanced,
  colback=accentlight,
  colframe=accent!40,
  left=5mm,
  right=5mm,
  top=4mm,
  bottom=4mm,
  arc=2mm,
  boxrule=0.6pt,
  before skip=10pt,
  after skip=10pt,
}
\newtcolorbox{warnbox}{
  enhanced,
  colback=orange!8,
  colframe=orange!30,
  left=5mm,
  right=5mm,
  top=3mm,
  bottom=3mm,
  arc=2mm,
  boxrule=0.5pt,
  before skip=8pt,
  after skip=8pt,
}

% ── Code Listings ──
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize\color{dark},
  backgroundcolor=\color{softgray},
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{border},
  framesep=8pt,
  xleftmargin=2pt,
  xrightmargin=2pt,
  breaklines=true,
  aboveskip=0pt,
  belowskip=0pt,
  showstringspaces=false,
  columns=flexible,
  language=Python,
  keywordstyle=\color{accent}\bfseries,
  stringstyle=\color{green},
  commentstyle=\color{midgray}\itshape,
  numbers=none,
}

% ── Icons ──
\usepackage{fontawesome5}

% ── TikZ ──
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, backgrounds}

% ── Captions ──
\usepackage{caption}
\captionsetup{font={small,color=midgray},skip=4pt,justification=raggedright,singlelinecheck=false}

% ── Float control ──
\usepackage{float}

% ── Header/Footer ──
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\textcolor{midgray}{MultiLingua-100K \textbar\ Dataset Datasheet v1.0}}
\fancyhead[R]{\footnotesize\textcolor{midgray}{\thepage}}
\renewcommand{\headrulewidth}{0.4pt}
\fancyfoot{}
\setlength{\headheight}{14pt}

% ── Hyperlinks ──
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  citecolor=purple,
  pdfauthor={Marchetti, Tanaka, Okafor},
  pdftitle={MultiLingua-100K Dataset Datasheet},
}
