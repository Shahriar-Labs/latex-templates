\section*{\color{primaryNavy}6. UAT Process Flow}
\sectionline

\begin{center}
\begin{tikzpicture}[
  node distance=1.6cm and 1.4cm,
  box/.style={
    rectangle,
    draw=accentBlue,
    fill=bgLight,
    line width=1pt,
    minimum width=2.6cm,
    minimum height=1.1cm,
    align=center,
    font=\footnotesize\bfseries\color{primaryNavy},
    rounded corners=4pt
  },
  arrow/.style={
    ->,
    >=stealth,
    line width=1.2pt,
    color=accentBlue
  }
]
  \node[box] (plan) {Plan\\UAT};
  \node[box, right=of plan] (execute) {Execute\\Tests};
  \node[box, right=of execute] (review) {Review\\Results};
  \node[box, right=of review] (signoff) {Sign-Off\\\& Deploy};

  \draw[arrow] (plan) -- (execute);
  \draw[arrow] (execute) -- (review);
  \draw[arrow] (review) -- (signoff);
\end{tikzpicture}
\end{center}

\vspace{14pt}
