% Packages, colours and custom commands for this template.
% Model Card Template — elegant, modern, compile-clean
% Edit and compile online at https://letx.app
% Compiles with: latexmk -pdf main.tex

\usepackage{amsmath,colortbl}

% ── Encoding & Fonts ──
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{microtype}

% ── Page Layout ──
\usepackage[margin=1.8cm,top=1.5cm,bottom=1.5cm,headheight=14pt,a4paper]{geometry}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\raggedbottom

% ── Colors ──
\usepackage[table,svgnames]{xcolor}
\definecolor{accent}{HTML}{2563EB}
\definecolor{accentdark}{HTML}{1D4ED8}
\definecolor{accentlight}{HTML}{DBEAFE}
\definecolor{dark}{HTML}{111827}
\definecolor{medium}{HTML}{6B7280}
\definecolor{lightbg}{HTML}{F9FAFB}
\definecolor{border}{HTML}{E5E7EB}
\definecolor{warn}{HTML}{D97706}
\definecolor{warnlight}{HTML}{FEF3C7}
\definecolor{success}{HTML}{059669}
\definecolor{successlight}{HTML}{D1FAE5}
\definecolor{danger}{HTML}{DC2626}
\definecolor{dangerlight}{HTML}{FEE2E2}

% ── Graphics & Boxes ──
\usepackage{tikz}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}

% ── Tables ──
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{multicol}

% ── Lists ──
\usepackage{enumitem}
\setlist{nosep,leftmargin=*,itemsep=2pt,topsep=4pt}

% ── Section Titles ──
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {}
  {0pt}
  {}[\vspace{-2pt}{\color{accent!25}\rule{\linewidth}{0.6pt}}\vspace{6pt}]
\titlespacing{\section}{0pt}{14pt}{6pt}

\titleformat{\subsection}
  {\large\bfseries\color{dark}}
  {}
  {0pt}
  {}
\titlespacing{\subsection}{0pt}{10pt}{4pt}

% ── Code Listings ──
\usepackage{listings}
\lstset{
  basicstyle=\small\ttfamily,
  breaklines=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{border},
  backgroundcolor=\color{lightbg},
  xleftmargin=6pt,
  xrightmargin=6pt,
  aboveskip=8pt,
  belowskip=8pt,
  showstringspaces=false,
  columns=flexible,
  numbers=none,
  keywordstyle=\color{accent}\bfseries,
  commentstyle=\color{medium}\itshape,
  stringstyle=\color{success},
}

% ── Icons ──
\usepackage{fontawesome5}

% ── Links ──
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accentdark,
  citecolor=accentdark,
  pdfborder={0 0 0},
}

% ══════════════════════════════════════
% Custom Commands
% ══════════════════════════════════════

% -- Evaluation heatmap cell macros (5-level) --
\newcommand{\heathi}[1]{\cellcolor{success!30}\textbf{#1}}
\newcommand{\heatmh}[1]{\cellcolor{success!15}\textbf{#1}}
\newcommand{\heatmed}[1]{\cellcolor{warn!20}\textbf{#1}}
\newcommand{\heatml}[1]{\cellcolor{warn!35}\textbf{#1}}
\newcommand{\heatlo}[1]{\cellcolor{danger!20}\textbf{#1}}

% -- Detail-row macro for model info grid --
\newcommand{\detailrow}[2]{%
  \textcolor{accent}{\bfseries\small #1} & {\small #2} \\[3pt]}

% -- Horizontal bar for fairness charts --
\newcommand{\fairbar}[1]{%
  \begin{tikzpicture}[baseline=-2.5pt]
    \fill[accent] (0,0) rectangle (#1*0.036, 0.22);
    \draw[accent!12,thin] (0,0) rectangle (3.6, 0.22);
  \end{tikzpicture}%
}

% -- tcolorbox: caveats callout --
\newtcolorbox{caveatbox}{
  colback=warnlight!60,
  colframe=warn!40,
  arc=3mm,
  boxrule=0.8pt,
  left=5mm,
  right=5mm,
  top=4mm,
  bottom=4mm,
  fontupper=\small,
  before skip=10pt,
  after skip=8pt,
}

% -- tcolorbox: info callout --
\newtcolorbox{infobox}{
  colback=accentlight!50,
  colframe=accent!30,
  arc=3mm,
  boxrule=0.8pt,
  left=5mm,
  right=5mm,
  top=4mm,
  bottom=4mm,
  fontupper=\small,
  before skip=10pt,
  after skip=8pt,
}

% -- Inline suitability badges --
\newcommand{\suithigh}{\textcolor{success}{\faCheckCircle}~~High}
\newcommand{\suitmed}{\textcolor{warn}{\faExclamationTriangle}~~Medium}
\newcommand{\suitlimited}{\textcolor{warn!80}{\faMinusCircle}~~Limited}
\newcommand{\suitnot}{\textcolor{danger}{\faTimesCircle}~~Not Recommended}

% -- Bullet label --
\newcommand{\bulletlabel}[1]{\textcolor{accent}{\textbf{#1}}}

% ══════════════════════════════════════
% Document
% ══════════════════════════════════════
