% Packages, colours and custom commands for this template.
% Ablation Study Report — elegant, modern LaTeX template
% Edit and compile online at https://letx.app
% Compiles with: latexmk -pdf main.tex (pdflatex, TeX Live full)


% ── Packages ──────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[margin=2.2cm,a4paper]{geometry}
\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{shadows,arrows.meta,positioning,calc,fit,backgrounds,decorations.pathreplacing}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{fontawesome5}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{listings}
\usepackage{caption}
\usepackage{multicol}
\usepackage{hyperref}

% ── Colors ────────────────────────────────────────────────────
\definecolor{accent}{HTML}{2563EB}
\definecolor{accentdark}{HTML}{1E40AF}
\definecolor{slate}{HTML}{1E293B}
\definecolor{slatelight}{HTML}{64748B}
\definecolor{slatebg}{HTML}{F8FAFC}
\definecolor{green}{HTML}{16A34A}
\definecolor{greenlight}{HTML}{DCFCE7}
\definecolor{amber}{HTML}{D97706}
\definecolor{red}{HTML}{DC2626}
\definecolor{redlight}{HTML}{FEE2E2}
\definecolor{barstart}{HTML}{3B82F6}
\definecolor{barmid}{HTML}{6366F1}
\definecolor{barend}{HTML}{DC2626}

% ── Page style ────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\footnotesize\color{slatelight}EXP-2026-07-ABL-042}
\fancyhead[R]{\footnotesize\color{slatelight}Ablation Study Report}
\fancyfoot[C]{\footnotesize\color{slatelight}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\setlength{\headheight}{14.5pt}

% ── Section formatting ────────────────────────────────────────
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {\thesection}{1em}{}
  [{\color{accent!25}\titlerule[0.6pt]\vspace{4pt}}]
\titleformat{\subsection}
  {\large\bfseries\color{slate}}
  {\thesubsection}{1em}{}

% ── Paragraph setup ───────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\raggedbottom

% ── Listings style ────────────────────────────────────────────
\lstset{
  basicstyle=\ttfamily\footnotesize\color{slate},
  breaklines=true,
  backgroundcolor=\color{slatebg},
  frame=none,
  xleftmargin=14pt,
  xrightmargin=14pt,
  aboveskip=10pt,
  belowskip=10pt,
  numbers=none,
  showstringspaces=false,
  columns=flexible,
  keepspaces=true,
}

% ── tcolorbox styles ─────────────────────────────────────────
\newtcolorbox{calloutbox}{
  enhanced,
  colback=accent!4,
  colframe=accent!30,
  arc=4pt,
  boxrule=0.6pt,
  left=12pt,
  right=12pt,
  top=8pt,
  bottom=8pt,
  fontupper=\small,
  before skip=12pt,
  after skip=12pt,
}

\newtcolorbox{findingbox}{
  enhanced,
  colback=greenlight!50,
  colframe=green!30,
  arc=4pt,
  boxrule=0.6pt,
  left=14pt,
  right=14pt,
  top=9pt,
  bottom=9pt,
  fontupper=\small,
  before skip=12pt,
  after skip=12pt,
}

% ── Helper macros ─────────────────────────────────────────────
\newcommand{\chaptername}{Ablation Study}
\newcommand{\modelname}{EGNN-X}
\newcommand{\dataset}{QM9}
\newcommand{\metric}{MAE}
\newcommand{\metricunit}{kcal/mol}
\newcommand{\pcheck}{\textcolor{green}{\textbf{\(\bullet\)}}}
\newcommand{\pcross}{\textcolor{slatelight!50}{\textbf{\(\circ\)}}}
\newcommand{\pna}{\textcolor{slatelight!30}{---}}

% ── Document ──────────────────────────────────────────────────
\emergencystretch=3em\relax
