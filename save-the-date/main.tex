% Save the Date Card — elegant, photo-centric design
% Edit and compile online at https://letx.app
% Compile with: latexmk -pdf main.tex
\documentclass[11pt]{article}
\usepackage{amsmath}

% ── Geometry ──────────────────────────────────────────────────
\usepackage[paperwidth=7in,paperheight=5in,margin=0.45in,noheadfoot]{geometry}

% ── Colors ────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{B76E5C}       % warm rose-terracotta
\definecolor{dark}{HTML}{2D2D2D}         % near-black
\definecolor{softbg}{HTML}{F9F5F0}        % warm cream
\definecolor{gold}{HTML}{C9A96E}          % subtle gold
\definecolor{lightgray}{HTML}{E8E3DD}    % frame tone
\pagecolor{softbg}

% ── Fonts ─────────────────────────────────────────────────────
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[T1]{fontenc}

% ── Packages ──────────────────────────────────────────────────
\usepackage{fontawesome5}
\usepackage{tikz}
\usetikzlibrary{calc,positioning}
\usepackage{tcolorbox}
\tcbuselibrary{skins}
\usepackage{tabularx}
\usepackage{hyperref}
\usepackage{enumitem}

% ── Typography tweaks ─────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\frenchspacing

% ── Helper macros ─────────────────────────────────────────────
\newcommand{\couplenames}[1]{%
  {\fontsize{18}{22}\selectfont\color{dark}\bfseries\MakeUppercase{#1}\par}%
}
\newcommand{\savedate}[1]{%
  {\fontsize{36}{42}\selectfont\color{accent}\bfseries#1\par}%
}
\newcommand{\divider}{%
  \vspace{2pt}\par
  {\color{gold}\rule{0.38\linewidth}{0.6pt}}\par
  \vspace{2pt}%
}
\newcommand{\detailrow}[2]{%
  \begin{tabularx}{\linewidth}{@{}>{\color{accent}}c@{\hspace{5pt}}>{\raggedright\arraybackslash}X@{}}
  \scriptsize\faIcon{#1} & \footnotesize\color{dark}#2
  \end{tabularx}\par\vspace{1pt}%
}

% ── Document ──────────────────────────────────────────────────
\begin{document}
\thispagestyle{empty}

% Decorative frame
\begin{tikzpicture}[remember picture,overlay]
  \draw[lightgray,line width=1.2pt]
    ($(current page.north west)+(0.28in,-0.28in)$) rectangle
    ($(current page.south east)+(-0.28in,0.28in)$);
  \draw[gold,line width=0.4pt]
    ($(current page.north west)+(0.32in,-0.32in)$) rectangle
    ($(current page.south east)+(-0.32in,0.32in)$);
\end{tikzpicture}

% ── Top section: couple names ─────────────────────────────────
\begin{center}
  \couplenames{Sophia \& Alexander}
  \vspace{4pt}
  {\footnotesize\color{gold}\textbullet\ \textbullet\ \textbullet}
  \vspace{6pt}

  {\color{dark}\footnotesize\itshape are getting married}
\end{center}

\vspace{8pt}

% ── Photo placeholder ─────────────────────────────────────────
\begin{tcolorbox}[
  enhanced,
  boxrule=0pt,
  frame hidden,
  colback=lightgray,
  width=0.42\linewidth,
  height=0.42\linewidth,
  halign=center,
  valign=center,
  before={\centering},
  after={\par},
  borderline={0.8pt}{0pt}{gold},
  sharp corners,
]
  {\color{white!60!dark}\faIcon{camera-retro}}\\[2pt]
  {\color{white!60!dark}\tiny\textit{photo}}
\end{tcolorbox}

\vspace{10pt}

% ── Date: the hero element ────────────────────────────────────
\begin{center}
  \savedate{14 \ {\large JUNE} \ 2026}
  \divider
  {\color{dark}\footnotesize\scshape Saturday \textbullet\ Four o'clock in the afternoon}
\end{center}

\vspace{10pt}

% ── Venue preview ─────────────────────────────────────────────
\begin{center}
\begin{minipage}{0.82\linewidth}
  \centering
  {\color{accent}\footnotesize\bfseries\scshape The Willow Barn}\\[1pt]
  {\color{dark}\footnotesize 27 Orchard Lane, Sonoma Valley, California}

  \vspace{6pt}
  {\color{gold}\rule{0.44\linewidth}{0.4pt}}
  \vspace{6pt}

  {\color{dark}\tiny\itshape
    Join us for an evening among the vineyards as we begin our next chapter.\\
    Formal invitation to follow.}
\end{minipage}
\end{center}

\vspace{4pt}

% ── Footer ────────────────────────────────────────────────────
\begin{center}
  {\color{dark!60}\tiny\scshape
   Kindly RSVP at \hspace{1pt} \href{https://sophiaandalexander.com}{sophiaandalexander.com}
   \hspace{8pt}\textbullet\hspace{8pt}
   \faIcon{envelope}\hspace{2pt}\texttt{hello@sophiaandalexander.com}}
\end{center}

\end{document}
