\section{Peer Evaluation Workflow}

The peer evaluation process is integrated into the group project timeline in three distinct phases. Early evaluations (formative) occur at the midpoint of the semester, while the final evaluation (summative) takes place immediately following the project submission.

Figure~\ref{fig:workflow} illustrates the standard cycle of project execution and evaluation at Nexa University.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=1.6cm and 1.8cm,
  box/.style={
    rectangle,
    draw=nexanavy,
    thick,
    fill=nexabg,
    text=nexacharcoal,
    align=center,
    rounded corners=3pt,
    minimum width=3.8cm,
    minimum height=1.2cm,
    font=\small\bfseries
  },
  arrow/.style={
    -{Latex[length=2.5mm]},
    thick,
    nexateal
  }
]
  % Nodes
  \node[box] (step1) {\faUsers\\[3pt] 1. Team Formation \& \\ Project Planning};
  \node[box, right=of step1] (step2) {\faLightbulb\\[3pt] 2. Project Midpoint \\ (Formative Feedback)};
  \node[box, below=of step2] (step3) {\faFile\\[3pt] 3. Project Submission \\ (Summative Review)};
  \node[box, left=of step3] (step4) {\faCalculator\\[3pt] 4. Instructor Review \\ \& Grade Adjustment};

  % Connecting lines
  \draw[arrow] (step1) -- (step2) node[midway, above, font=\scriptsize\itshape, text=nexateal] {Work starts};
  \draw[arrow] (step2) -- (step3) node[midway, right, font=\scriptsize\itshape, text=nexateal] {Adjust course};
  \draw[arrow] (step3) -- (step4) node[midway, below, font=\scriptsize\itshape, text=nexateal] {Submit rubric};
  \draw[arrow] (step4) -- (step1) node[midway, left, font=\scriptsize\itshape, text=nexateal] {Future cycles};

  % Highlight container
  \begin{scope}[on background layer]
    \node[
      fill=nexaamber!10,
      draw=nexaamber!40,
      dashed,
      inner sep=10pt,
      fit=(step2) (step3),
      label={[text=nexaamber!80!black, font=\footnotesize\bfseries]above:Evaluation Windows}
    ] {};
  \end{scope}

\end{tikzpicture}
\caption{The standard Nexa University peer evaluation and grading workflow.}
\label{fig:workflow}
\end{figure}

The formative midpoint check is key; it allows teams to address communication gaps or work distribution issues before grades are negatively affected. The final peer evaluation rubric score is confidential and submitted individually by each student to the instructor.
