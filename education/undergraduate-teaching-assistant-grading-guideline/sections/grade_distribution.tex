\section{Target Grade Distribution}
While we grade strictly according to the rubric, we monitor the overall class distribution to ensure the exam difficulty and grading standards are aligned with student performance.

\subsection{Historical Distribution}
Figure~\ref{fig:grade_dist} shows the historical and target grade distribution for CS 201 over the last three semesters at the Vertex Institute of Technology.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            enlargelimits=0.15,
            legend style={at={(0.5,-0.15)}, anchor=north, legend columns=-1},
            ylabel={Percentage of Students (\%)},
            symbolic x coords={A, B, C, D, F},
            xtick=data,
            nodes near coords,
            nodes near coords align={vertical},
            width=0.85\textwidth,
            height=6.5cm,
            bar width=15pt,
            axis x line=bottom,
            axis y line=left,
            ymajorgrids=true,
            grid style={dashed, gray!30},
            cycle list={
                {fill=primary, draw=primary},
                {fill=secondary, draw=secondary}
            }
        ]
            % Target Distribution
            \addplot coordinates {(A,25) (B,40) (C,25) (D,8) (F,2)};
            % Actual Fall 2025
            \addplot coordinates {(A,28) (B,38) (C,22) (D,9) (F,3)};
            \legend{Target Distribution, Fall 2025 Actual}
        \end{axis}
    \end{tikzpicture}
    \caption{Target vs. Actual Grade Distribution for CS 201.}
    \label{fig:grade_dist}
\end{figure}

\subsection{Grade Inflation and Calibration}
If you notice that more than 60\% of students in your assigned grading bucket are receiving an ``A'' on a manual component, pause your grading and contact the Head TA. We may need to recalibrate the manual criteria to prevent grade inflation. Conversely, if more than 20\% of students are failing a component, we will schedule an emergency review session.
