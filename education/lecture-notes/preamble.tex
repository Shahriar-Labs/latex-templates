\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% Page Geometry (Wide right margin for notes)
\usepackage[
    a4paper,
    left=2.5cm,
    right=6.0cm,
    top=2.8cm,
    bottom=2.8cm,
    marginparwidth=4.5cm,
    marginparsep=0.6cm,
    headheight=15pt
]{geometry}

% Color Definitions (Tasteful Slate-Teal-Amber Palette)
\usepackage{xcolor}
\definecolor{primary}{HTML}{0F172A}      % Slate 900
\definecolor{secondary}{HTML}{0D9488}    % Teal 600
\definecolor{accent}{HTML}{B45309}       % Amber 700
\definecolor{bglight}{HTML}{F8FAFC}      % Slate 50
\definecolor{slate600}{HTML}{475569}     % Slate 600 (for secondary text/notes)
\definecolor{defbg}{HTML}{F0FDF4}        % Emerald 50 (Soft Green)
\definecolor{defborder}{HTML}{10B981}    % Emerald 500
\definecolor{thmbg}{HTML}{EFF6FF}        % Blue 50 (Soft Blue)
\definecolor{thmborder}{HTML}{3B82F6}    % Blue 500
\definecolor{proofborder}{HTML}{6B7280}  % Gray 500

% Typography
\usepackage{mathpazo}                    % Palatino font
\usepackage[scaled=0.95]{helvet}         % Helvetica for sans-serif
\usepackage{courier}                     % Courier for monospace
\usepackage{microtype}
\usepackage{parskip}                     % Modern spacing instead of indentation

% Core Packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{tikz}
\usepackage{marginnote}
\usepackage{fontawesome5}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{tcolorbox}

% TikZ Configuration
\usetikzlibrary{positioning, calc, shapes.geometric}

% Header and Footer Customization
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\sffamily\small\color{slate600}\leftmark}
\fancyhead[R]{\sffamily\small\color{slate600}PHYS-4028 Lecture Notes}
\fancyfoot[C]{\sffamily\small\color{slate600}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% Title Formatting for Chapters (Lectures)
\titleformat{\chapter}[display]
  {\normalfont\sffamily\huge\bfseries\color{primary}}
  {\large\sffamily\bfseries\color{accent}\faAtom\ LECTURE \thechapter}
  {2pt}
  {\vspace{2pt}}
  [\vspace{5pt}{\color{secondary}\hrule height 1.5pt}]
\titlespacing*{\chapter}{0pt}{-20pt}{20pt}

\titleformat{\section}
  {\normalfont\sffamily\Large\bfseries\color{primary}}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{15pt}{8pt}

\titleformat{\subsection}
  {\normalfont\sffamily\large\bfseries\color{secondary}}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{12pt}{6pt}

% Custom Margin Notes
\renewcommand*{\marginfont}{\footnotesize\sffamily\color{slate600}}
\newcommand{\remark}[2][0pt]{%
  \marginnote{\vspace{#1}\raggedright #2}%
}

% Definition, Theorem, and Proof Boxes
\newtcolorbox{definitionbox}[2][]{
  colback=defbg,
  colframe=defborder,
  coltitle=defborder!40!black,
  fonttitle=\bfseries\sffamily,
  title={Definition: #2},
  arc=3pt,
  outer arc=3pt,
  boxrule=0.5pt,
  leftrule=3.5pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  #1
}

\newtcolorbox{theorembox}[2][]{
  colback=thmbg,
  colframe=thmborder,
  coltitle=thmborder!40!black,
  fonttitle=\bfseries\sffamily,
  title={Theorem: #2},
  arc=3pt,
  outer arc=3pt,
  boxrule=0.5pt,
  leftrule=3.5pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  #1
}

\newtcolorbox{proofbox}[1][]{
  colback=white,
  colframe=proofborder,
  coltitle=proofborder!40!black,
  fonttitle=\itshape,
  title={Proof},
  arc=0pt,
  outer arc=0pt,
  boxrule=0pt,
  leftrule=2pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  bottomtitle=2pt,
  toptitle=2pt,
  after={\par\hfill\(\square\)\medskip},
  #1
}

% Clean List Formatting
\setlist[itemize]{leftmargin=18pt, label=\textcolor{secondary}{\(\bullet\)}, itemsep=3pt, parsep=0pt}
\setlist[enumerate]{leftmargin=18pt, label=\textcolor{primary}{\arabic*.}, itemsep=3pt, parsep=0pt}
