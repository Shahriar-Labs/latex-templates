% ============================================================
%  ELECTRIC NIGHT — Event Flyer Template
%  Edit and compile online at https://letx.app
%  Engine : pdflatex (TeX Live full)
%  Output : A4 portrait, single page
% ============================================================

\documentclass[12pt]{article}
\usepackage{amsmath}

% ── Geometry ─────────────────────────────────────────────────
\usepackage[
  a4paper,
  left=0mm, right=0mm, top=0mm, bottom=0mm,
  noheadfoot, nomarginpar
]{geometry}

% ── Core packages ────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

\usepackage[table]{xcolor}
\usepackage{tikz}
\usetikzlibrary{calc, positioning, fadings, shadings}
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, raster}
\usepackage{tabularx}
\usepackage{array}
\usepackage{fontawesome5}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{qrcode}
\usepackage{hyperref}

\hypersetup{
  hidelinks,
  pdfauthor={LetX},
  pdftitle={Electric Night — Event Flyer}
}

% ── Lengths / spacing ────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ── Palette ──────────────────────────────────────────────────
\definecolor{bgdark}{HTML}{0A0A12}       % near-black page bg
\definecolor{bgmid}{HTML}{12121E}        % card bg
\definecolor{accent}{HTML}{C45AFF}       % electric violet
\definecolor{accentB}{HTML}{FF5AC4}      % hot pink
\definecolor{gold}{HTML}{FFD166}         % gold highlight
\definecolor{textlight}{HTML}{F0ECF8}    % near-white text
\definecolor{textmuted}{HTML}{9B8FB0}    % muted text
\definecolor{rulecol}{HTML}{2E2040}      % thin rule color
\definecolor{cardone}{HTML}{140E22}      % slot-card bg 1
\definecolor{cardtwo}{HTML}{0E1422}      % slot-card bg 2
\definecolor{dividerline}{HTML}{3A2760}

% ── Helper macros ────────────────────────────────────────────
% Accent pill tag
\newcommand{\pill}[2][accent]{%
  \tikz[baseline=(n.base)]\node[
    fill=#1, text=bgdark, rounded corners=3pt,
    inner xsep=5pt, inner ysep=2pt,
    font=\bfseries\fontsize{7}{7}\selectfont
  ](n){#2};%
}

% Section heading rule
\newcommand{\sectionrule}[1]{%
  {\color{accent}\rule[0.4ex]{18pt}{1.2pt}}%
  \hspace{6pt}%
  {\color{textmuted}\bfseries\fontsize{7.5}{9}\selectfont\MakeUppercase{#1}}%
  \hspace{6pt}%
  {\color{rulecol}\leaders\hrule height 0.6pt\hfill}\par
}

% Artist card
\newcommand{\artistcard}[4][cardone]{%
  % #1=bg colour  #2=name  #3=genre  #4=time
  \begin{tcolorbox}[
    colback=#1, colframe=dividerline,
    boxrule=0.6pt, arc=4pt,
    top=5pt, bottom=5pt, left=7pt, right=7pt,
    nobeforeafter
  ]
    {\color{accent}\bfseries\fontsize{11}{13}\selectfont #2}\par
    \vspace{2pt}
    {\color{textmuted}\fontsize{7.5}{9}\selectfont #3}\par
    \vspace{3pt}
    {\color{gold}\small\faIcon{clock}\ }%
    {\color{textlight}\fontsize{8}{9}\selectfont #4}
  \end{tcolorbox}%
}

% Info row (icon + text)
\newcommand{\inforow}[2]{%
  {\color{accent}#1}\ {\color{textlight}\small #2}\par\vspace{3pt}%
}

% ── PAGE BACKGROUND ──────────────────────────────────────────
\pagecolor{bgdark}

% ═══════════════════════════════════════════════════════════
\begin{document}
\thispagestyle{empty}

\begin{tikzpicture}[remember picture, overlay]
  % Subtle radial glow behind headline
  \shade[inner color=accent!18!bgdark, outer color=bgdark, shading=radial]
    (current page.north) ++(0,-55mm)
    ellipse (80mm and 40mm);
  % Bottom glow
  \shade[inner color=accentB!12!bgdark, outer color=bgdark, shading=radial]
    (current page.south) ++(0,40mm)
    ellipse (70mm and 35mm);
\end{tikzpicture}

% ── OUTER PADDING FRAME ──────────────────────────────────────
\begin{tikzpicture}[remember picture, overlay]
  \draw[color=dividerline, line width=0.5pt]
    ($(current page.north west)+(5mm,-5mm)$)
    rectangle
    ($(current page.south east)+(-5mm,5mm)$);
\end{tikzpicture}

\vspace*{6mm}

% ──────────────────────────────────────────────────────────────
%  TOP BAR — presented by
% ──────────────────────────────────────────────────────────────
\begin{center}
  {\color{textmuted}\fontsize{7.5}{9}\selectfont\MakeUppercase{%
    Neon Republic Presents}}
\end{center}

\vspace{2mm}
{\color{dividerline}\hrule height 0.4pt}
\vspace{4mm}

% ──────────────────────────────────────────────────────────────
%  HERO HEADLINE
% ──────────────────────────────────────────────────────────────
\begin{center}
  {\color{textlight}\fontsize{52}{54}\bfseries\selectfont
    ELECTRIC}\\[-2mm]
  {\color{accent}\fontsize{62}{64}\bfseries\selectfont
    NIGHT}\\[1mm]
  {\color{textmuted}\fontsize{10}{12}\selectfont
    \MakeUppercase{The Ultimate Underground Rave Experience}}
\end{center}

\vspace{4mm}
{\color{accent!40!bgdark}\hrule height 0.8pt}
\vspace{5mm}

% ──────────────────────────────────────────────────────────────
%  DATE / TIME / VENUE BLOCK
% ──────────────────────────────────────────────────────────────
\begin{center}
\begin{tabularx}{148mm}{
  @{}
  >{\centering\arraybackslash}X
  @{\hspace{3mm}}
  >{\color{dividerline}\centering\arraybackslash}p{0.4pt}
  @{\hspace{3mm}}
  >{\centering\arraybackslash}X
  @{\hspace{3mm}}
  >{\color{dividerline}\centering\arraybackslash}p{0.4pt}
  @{\hspace{3mm}}
  >{\centering\arraybackslash}X
  @{}
}
  % Date
  {{\color{textmuted}\fontsize{7}{8}\selectfont\MakeUppercase{Date}}\par
   \vspace{2pt}
   {\color{textlight}\fontsize{16}{18}\bfseries\selectfont 12}\par
   {\color{accent}\fontsize{10}{12}\bfseries\selectfont JULY 2025}}
  &
  {\color{dividerline}\rule{0.4pt}{22mm}}
  &
  % Time
  {{\color{textmuted}\fontsize{7}{8}\selectfont\MakeUppercase{Doors Open}}\par
   \vspace{2pt}
   {\color{textlight}\fontsize{16}{18}\bfseries\selectfont 10\,PM}\par
   {\color{accent}\fontsize{9}{11}\selectfont Till Sunrise}}
  &
  {\color{dividerline}\rule{0.4pt}{22mm}}
  &
  % Venue
  {{\color{textmuted}\fontsize{7}{8}\selectfont\MakeUppercase{Venue}}\par
   \vspace{2pt}
   {\color{textlight}\fontsize{11}{13}\bfseries\selectfont The Warehouse}\par
   {\color{textmuted}\fontsize{8}{10}\selectfont 48 Brickyard Lane, Berlin}}
\end{tabularx}
\end{center}

\vspace{5mm}
{\color{dividerline}\hrule height 0.4pt}
\vspace{5mm}

% ──────────────────────────────────────────────────────────────
%  LINEUP SECTION
% ──────────────────────────────────────────────────────────────
\hspace*{7mm}%
\begin{minipage}{\dimexpr\linewidth-14mm}
  \sectionrule{Lineup}
  \vspace{4mm}

  \begin{tcbraster}[
    raster columns=3,
    raster column skip=3mm,
    raster row skip=3mm,
    raster equal height=rows,
    nobeforeafter
  ]
    \artistcard[cardone]{SOLARIS}%
      {Techno / Hard Groove}{10:00 PM -- 11:30 PM}
    \artistcard[cardtwo]{NOVA PULSE}%
      {Electronic / Ambient}{11:30 PM -- 01:00 AM}
    \artistcard[cardone]{DJ KRATOS}%
      {Drum \& Bass}{01:00 AM -- 02:30 AM}
    \artistcard[cardtwo]{ARIA SKYE}%
      {Deep House / Melodic}{02:30 AM -- 04:00 AM}
    \artistcard[cardone]{ZEROA}%
      {Industrial / EBM}{04:00 AM -- 05:30 AM}
    \artistcard[cardtwo]{MIRA VOSS}%
      {Closing Set -- Sunrise}{05:30 AM -- 07:00 AM}
  \end{tcbraster}
\end{minipage}

\vspace{5mm}
{\color{dividerline}\hrule height 0.4pt}
\vspace{5mm}

% ──────────────────────────────────────────────────────────────
%  BOTTOM ROW — Info + Tickets + QR
% ──────────────────────────────────────────────────────────────
\hspace*{7mm}%
\begin{minipage}{\dimexpr\linewidth-14mm}
\begin{tabularx}{\linewidth}{
  @{}
  p{58mm}
  @{\hspace{4mm}}
  X
  @{\hspace{4mm}}
  p{38mm}
  @{}
}

  % ── Left: event info ────────────────────────────────────
  {%
    \sectionrule{Event Info}\vspace{3mm}
    \inforow{\faIcon{map-marker-alt}}{48 Brickyard Lane, Mitte, Berlin}
    \inforow{\faIcon{calendar-alt}}{Saturday, 12 July 2025}
    \inforow{\faIcon{clock}}{10:00 PM -- Sunrise (07:00 AM)}
    \inforow{\faIcon{users}}{Capacity: 800 --- 18+ only}
    \inforow{\faIcon{ban}}{No re-entry after 2:00 AM}
    \inforow{\faIcon{shield-alt}}{ID required at door}
  }

  &

  % ── Centre: tickets ─────────────────────────────────────
  {%
    \sectionrule{Tickets}\vspace{3mm}
    \begin{tabularx}{\linewidth}{
      @{}
      >{\color{accent}\bfseries}l
      @{\hspace{6mm}}
      >{\raggedright\arraybackslash}X
      @{}
    }
      Early Bird  & EUR 12 \\[3pt]
      General     & EUR 20 \\[3pt]
      VIP Lounge  & EUR 45 \\[3pt]
      Door        & EUR 28 \\
    \end{tabularx}
    \vspace{4mm}
    \pill[accent]{Limited Early Bird Available}
    \vspace{3mm}\par
    {\color{textmuted}\fontsize{7.5}{9}\selectfont
      All tickets include cloakroom access.\par
      VIP includes 2 drinks tokens + priority entry.}
  }

  &

  % ── Right: QR code ──────────────────────────────────────
  {%
    \sectionrule{Tickets \& Info}\vspace{4mm}
    \begin{center}
      \begin{tcolorbox}[
        colback=textlight, colframe=textlight,
        boxrule=0pt, arc=4pt,
        top=4pt, bottom=4pt, left=4pt, right=4pt,
        width=32mm, nobeforeafter
      ]
        \centering
        \qrcode[height=24mm]{https://letx.app/electric-night-2025}
      \end{tcolorbox}
      \vspace{3pt}
      {\color{textmuted}\fontsize{6.5}{8}\selectfont Scan to buy tickets}
    \end{center}
  }

\end{tabularx}
\end{minipage}

\vspace{4mm}
{\color{dividerline}\hrule height 0.4pt}

% ──────────────────────────────────────────────────────────────
%  FOOTER
% ──────────────────────────────────────────────────────────────
\vspace{3mm}
\begin{center}
  \begin{tabularx}{148mm}{
    @{}
    >{\raggedright\arraybackslash}X
    @{}
    >{\centering\arraybackslash}X
    @{}
    >{\raggedleft\arraybackslash}X
    @{}
  }
    {\color{textmuted}\fontsize{7}{8}\selectfont
      \faIcon{instagram}\ @neonrepublic \quad
      \faIcon{facebook}\ /NeonRepublic}
    &
    {\color{textmuted}\fontsize{6.5}{8}\selectfont
      Neon Republic Events \textbar{} Berlin, Germany\par
      \copyright\ 2025 --- All rights reserved}
    &
    {\color{textmuted}\fontsize{7}{8}\selectfont
      \faIcon{globe}\ neonrepublic.de\par
      \faIcon{envelope}\ hello@neonrepublic.de}
  \end{tabularx}
\end{center}
\vspace{4mm}

\end{document}
