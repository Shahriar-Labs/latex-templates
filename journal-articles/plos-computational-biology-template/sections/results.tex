\section{Results}
We simulated the SDM architecture using a network of $N = 1000$ Meridian neurons subjected to random poissonian input.
Our results show that when homeostatic scaling is enabled, the system converges to a self-organized critical state within $t = 500\text{ ms}$ of simulation time.
In contrast, disabling the scaling parameter $\gamma$ results in runaway network activity, leading to epileptiform oscillations and saturation.

\subsection{Comparison of Network Parameters}
We compared the SDM architecture against standard static recurrent network configurations.
Table~\ref{tab:results} summarizes the performance metrics across different trial runs using fictional datasets designed to emulate multi-electrode array recordings.

\begin{table}[htbp]
\centering
\caption{\textbf{Performance comparison of static versus Synapse-Dynamic models.}}
\begin{tabular}{lcccc}
\toprule
\textbf{Model Configuration} & \textbf{Sparsity (\%)} & \textbf{Convergence (ms)} & \textbf{Entropy (bits)} & \textbf{Accuracy (\%)} \\
\midrule
Static Base Network & 10 & N/A & 2.45 & 78.4 \\
Dynamic (No Scaling) & 15 & 1240 & 1.88 & 62.1 \\
\rowcolor{TableLight} SDM (Proposed) & 12 & 345 & 4.12 & 94.6 \\
SDM + Nimbus Filter & 8 & 290 & 4.56 & 96.8 \\
\bottomrule
\end{tabular}
\label{tab:results}
\end{table}

As indicated in Table~\ref{tab:results}, our proposed model (SDM + Nimbus Filter) achieves an accuracy of 96.8\% in temporal pattern classification, while maintaining a high entropy level of 4.56 bits.
This represents a significant improvement over the static baseline networks, which struggle to adapt to temporal shifts in input patterns.

\subsection{Spectral Power Analysis}
To investigate the frequency profile of the network, we computed the power spectral density (PSD) of the aggregate network activity.
We observed distinct peak frequencies in the gamma band ($30$--$80\text{ Hz}$), which is consistent with experimental recordings of cortical circuits in behaving primates.
The peak frequency is highly sensitive to the learning rate $\eta$, indicating that synaptic plasticity directly shapes the network's macroscopic oscillations.
