\section{Methodology}
\label{sec:methodology}

\subsection{Sensor Node Hardware Architecture}
The Synapse Acoustic Monitor (SAM) hardware architecture consists of four primary subsystems: the transducer front-end, the processing unit, the power manager, and the wireless communication module. Figure \ref{fig:node_arch} illustrates the interconnection of these components.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=1.5cm and 2cm,
    box/.style={draw, rectangle, rounded corners, minimum width=2.5cm, minimum height=1.2cm, align=center, thick, fill=mdpiBg, draw=mdpiPrimary},
    header/.style={font=\sffamily\bfseries\small\color{mdpiSecondary}},
    arrow/.style={-Latex, thick, draw=mdpiPrimary}
]
    % Nodes
    \node[box] (sensor) {\textbf{Transducer}\\\small (Piezo-ceramic)};
    \node[box, right=of sensor] (afe) {\textbf{Analog Front-End}\\\small (LNA \& Bandpass)};
    \node[box, right=of afe] (processor) {\textbf{Processor}\\\small (Apex MCU)};
    \node[box, below=of processor] (transceiver) {\textbf{Transceiver}\\\small (Meridian RF)};
    \node[box, below=of afe] (pmic) {\textbf{Power Management}\\\small (Nexa Harvester)};

    % Arrows
    \draw[arrow] (sensor) -- (afe);
    \draw[arrow] (afe) -- (processor);
    \draw[arrow] (processor) -- (transceiver);
    \draw[arrow] (pmic) |- (processor);
    \draw[arrow] (pmic) -- (afe);
    \draw[arrow] (pmic) -- (transceiver);
    
    % Boundary label
    \begin{scope}[on background layer]
        \node[draw, dashed, borderCol, fill=black!2, inner sep=15pt, fit=(sensor) (processor) (transceiver) (pmic)] (boundary) {};
    \end{scope}
    \node[anchor=south east] at (boundary.south east) {\sffamily\scriptsize SAM Node Architecture};
\end{tikzpicture}
\caption{System block diagram of the Synapse Acoustic Monitor (SAM) sensor node, showcasing the integrated Nexa power harvesting interface and Meridian RF connectivity.}
\label{fig:node_arch}
\end{figure}

The acoustic transduction is performed by a custom-fabricated lead zirconate titanate (PZT-5H) piezo-ceramic element optimized for ultrasonic emission detection in concrete. The analog signal is routed to a Low Noise Amplifier (LNA) designed by Nexa Microelectronics, featuring a gain of 40 dB in the 100 kHz--1 MHz frequency band.

\subsection{Wave Propagation and Localization}
Acoustic emission waves within concrete structures propagate primarily as Lamb waves. The localization of an acoustic source (damage event) is determined using a Time Difference of Arrival (TDOA) algorithm. Let $\mathbf{x}_s = (x_s, y_s)$ be the unknown source location, and $\mathbf{x}_i = (x_i, y_i)$ be the position of the $i$-th sensor node. The arrival time $t_i$ at node $i$ is modeled as:
\begin{equation}
    t_i = t_0 + \frac{\|\mathbf{x}_s - \mathbf{x}_i\|}{v_p} + \eta_i ,
    \label{eq:tdoa}
\end{equation}
where $t_0$ is the emission occurrence time, $v_p$ is the group velocity of the wave in concrete (experimentally determined to be $v_p = 3850$ m/s), and $\eta_i$ represents measurement noise.

To solve the system of non-linear equations generated by Equation \eqref{eq:tdoa}, we employ a Levenberg-Marquardt optimization solver implemented directly in the Apex MCU firmware.
