\section{Review Methodology}
\label{sec:methodology}

\subsection{Search strategy}

We queried four indices (ACM Digital Library, IEEE Xplore, DBLP, and Google Scholar) with the boolean
string \texttt{("edge cach*" OR "edge-cach*") AND (consist* OR coheren*) AND (protocol OR
replication)}, restricted to venues in distributed systems, networking, and mobile computing, published
between January 2014 and December 2024. The initial pull returned 311 records after de-duplication.

\subsection{Inclusion and exclusion criteria}

\begin{themebox}{Inclusion criteria}
\begin{itemize}
  \item Proposes or formally analyses a consistency protocol, model, or guarantee for cached data
        at the edge (base station, CDN edge PoP, on-device, or peer cache).
  \item Reports at least one quantitative evaluation dimension (latency, staleness bound, fault
        recovery time, or overhead).
  \item Peer-reviewed venue or a preprint with $\geq 20$ citations at time of screening.
\end{itemize}
\end{themebox}

\begin{themebox}{Exclusion criteria}
\begin{itemize}
  \item Pure eviction/admission policy papers with no consistency contribution.
  \item Consistency work scoped only to data-centre or cloud tiers (no edge component).
  \item Workshop abstracts and posters without a full evaluation section.
\end{itemize}
\end{themebox}

Two reviewers independently screened titles and abstracts; disagreements ($n=17$) were resolved by
full-text read and discussion. Figure~\ref{fig:prisma} summarises the funnel from initial search to
final corpus.

\begin{figure}[h!]
\centering
\begin{tikzpicture}[
  stage/.style={draw, rounded corners=2pt, fill=nx@mist, minimum width=6.4cm, minimum height=0.9cm,
                align=center, font=\small},
  arr/.style={-{Stealth[length=2.2mm]}, nx@slate, thick}
]
\node[stage] (a) {311 records identified across 4 databases};
\node[stage, below=0.55cm of a] (b) {248 records after de-duplication};
\node[stage, below=0.55cm of b] (c) {96 records after title/abstract screening};
\node[stage, below=0.55cm of c] (d) {58 full texts assessed for eligibility};
\node[stage, below=0.55cm of d, fill=nx@teal, text=white] (e) {42 studies in final corpus};

\draw[arr] (a) -- (b);
\draw[arr] (b) -- (c);
\draw[arr] (c) -- (d);
\draw[arr] (d) -- (e);
\end{tikzpicture}
\caption{Screening funnel from initial search to the 42-study corpus used in this review.}
\label{fig:prisma}
\end{figure}

\subsection{Data extraction}

For each included study we extracted: consistency model claimed, representative system name, target
edge layer, staleness bound (if any), reported latency overhead relative to a no-consistency baseline,
fault-tolerance mechanism, and publication year. These fields underpin the thematic clustering in
Section~\ref{sec:thematic} and the comparison table in Section~\ref{sec:comparison}.
