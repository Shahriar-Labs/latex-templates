\section{Analysis Plan and Power Calculation}
\label{sec:analysis}

\subsection{Confirmatory Statistical Analysis}
The following confirmatory statistical tests will be executed sequentially. All tests will use a two-tailed alpha level of $\alpha = 0.05$ unless otherwise specified.

\subsubsection{Test 1: Subjective Cognitive Load (H1)}
To determine if the Adaptive UI reduces subjective cognitive load, we will conduct an independent-samples $t$-test comparing the global NASA Task Load Index (NASA-TLX) score between the two groups. The primary variable is the overall NASA-TLX score (computed as the unweighted mean of its six subscales, ranging from 0 to 100).
\begin{equation}
t = \frac{\bar{X}_1 - \bar{X}_2}{s_p \sqrt{\frac{2}{n}}}
\end{equation}
where $\bar{X}_1$ and $\bar{X}_2$ represent the mean NASA-TLX scores for the Static and Adaptive UI groups respectively, $s_p$ is the pooled standard deviation, and $n = 132$ is the sample size per group.

\subsubsection{Test 2: Decision Response Time (H2)}
We will analyze the Decision Response Time (DRT) using a $2\times 2$ mixed-factorial Analysis of Variance (ANOVA). The between-subjects factor is UI Type (Adaptive vs. Static) and the within-subjects factor is Task Load (Low vs. High). 
The primary hypothesis of interest is the interaction term:
\begin{equation}
F_{\text{Interaction}}(1, 262) = \frac{\text{MS}_{\text{UI} \times \text{Load}}}{\text{MS}_{\text{Error}}}
\end{equation}
We expect a statistically significant interaction effect, showing that the increase in response time under high workload is significantly buffered by the Adaptive UI.

\subsubsection{Test 3: Decision Accuracy (H3)}
To test if decision accuracy is maintained or improved under the Adaptive UI, we will analyze the proportion of correct decisions across all experimental trials. We will use a Chi-Square test of independence to compare the overall accuracy rates:
\begin{equation}
\chi^2 = \sum \frac{(O - E)^2}{E}
\end{equation}
where $O$ and $E$ represent the observed and expected counts of correct and incorrect decisions.

\subsection{Power Calculation and Sensitivity Analysis}
A formal power analysis was conducted using G*Power 3.1.9.7. Figure~\ref{fig:power_curve} plots the power curve for the primary independent-samples $t$-test as a function of sample size, assuming a Cohen's $d = 0.40$ and $\alpha = 0.05$.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=0.85\textwidth,
    height=6cm,
    grid=both,
    grid style={line width=.1pt, color=tableborder},
    xlabel={Total Sample Size ($N$)},
    ylabel={Statistical Power ($1-\beta$)},
    xmin=50, xmax=350,
    ymin=0.4, ymax=1.0,
    xtick={50, 100, 150, 200, 250, 264, 300, 350},
    ytick={0.4, 0.5, 0.6, 0.7, 0.8, 0.9, 0.95, 1.0},
    line width=1.2pt,
    legend pos=south east,
    font=\sffamily\small
]
\addplot[color=secondary, mark=none, thick] coordinates {
    (50, 0.47) (100, 0.68) (150, 0.81) (200, 0.89) 
    (250, 0.94) (264, 0.95) (300, 0.97) (350, 0.99)
};
\addlegendentry{Power ($d = 0.40$)}

% Highlight point N=264
\draw[dashed, color=alert, thick] (axis cs:264,0.4) -- (axis cs:264,0.95);
\draw[dashed, color=alert, thick] (axis cs:50,0.95) -- (axis cs:264,0.95);
\node[circle, fill=alert, inner sep=2pt] at (axis cs:264,0.95) {};
\node[right, color=alert, font=\sffamily\scriptsize] at (axis cs:265,0.92) {Target $N=264$ (Power $= 0.95$)};

\end{axis}
\end{tikzpicture}
\caption{Power curve for the primary outcome $t$-test (two-tailed, $\alpha = 0.05$, $d = 0.40$).}
\label{fig:power_curve}
\end{figure}

The analysis indicates that a sample size of $N=264$ actually yields a statistical power of approximately $95\%$ to detect a medium effect size of $d=0.40$. This provides a substantial safeguard against false negatives and ensures robust empirical conclusions.
