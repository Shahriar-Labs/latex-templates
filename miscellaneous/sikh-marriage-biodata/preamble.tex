% Packages, colours and custom commands for this template.
% Sikh Marriage Biodata Template — LaTeX
% Edit and compile online at: https://letx.app
% Local compile: latexmk -pdf main.tex (pdflatex, TeX Live full)
%
% Design: warm maroon + gold on cream, Khanda motif (TikZ), clean two-column layout.
% All content is realistic sample data; replace with your own.

\usepackage{amsmath}

% ── Encoding & Font ──
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% ── Geometry ──
\usepackage{geometry}
\geometry{a4paper, margin=15mm, top=13mm, bottom=13mm, footskip=6mm}

% ── Colors ──
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{8B1D2F}
\definecolor{gold}{HTML}{C4A44A}
\definecolor{cream}{HTML}{FDFBF5}
\definecolor{dark}{HTML}{2D2D2D}
\definecolor{gray}{HTML}{777777}
\definecolor{lighter}{HTML}{EAE5DB}

\pagecolor{cream}
\color{dark}

% ── Tables ──
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{colortbl}

% ── Graphics ──
\usepackage{tikz}

% ── Icons ──
\usepackage{fontawesome5}

% ── Section titles ──
\usepackage{titlesec}
\titleformat{\section}
  {\normalsize\bfseries\color{accent}}
  {}
  {0pt}
  {}
  [\vspace{-0.3ex}{\color{gold}\rule{\linewidth}{0.5pt}}]
\titlespacing{\section}{0pt}{9pt plus 1pt}{5pt}

% ── Hyperlinks ──
\usepackage[hidelinks]{hyperref}

% ── Paragraphs ──
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ── Tighten lists ──
\usepackage{enumitem}
\setlist{nosep, leftmargin=*}

% ── Custom macros ──

% Draw a stylised Khanda () in the accent colour
\newcommand{\drawkhanda}{%
  \begin{tikzpicture}[scale=0.35, line cap=round, line join=round]
    % Chakkar (circle)
    \draw[accent, line width=1.2pt] (0,0.5) circle (0.75);
    % Central double-edged sword shaft
    \draw[accent, line width=1.3pt] (0,-0.85) -- (0,2.1);
    % Sword tip
    \fill[accent] (-0.13,2.0) -- (0,2.1) -- (0.13,2.0) -- cycle;
    % Handle crossbar
    \draw[accent, line width=1.6pt] (-0.45,-0.85) -- (0.45,-0.85);
    % Handle grip below
    \draw[accent, line width=0.9pt] (0,-1.15) -- (0,-0.85);
    \draw[accent, line width=0.9pt] (-0.06,-1.2) -- (0.06,-1.2);
    % Left kirpan (curved blade)
    \draw[accent, line width=1.0pt] (-0.55,0.95)
      .. controls (-0.5,0.65) and (-0.35,0.45) .. (-0.35,0.1) -- (-0.25,0.2)
      .. controls (-0.38,0.5) and (-0.42,0.65) .. (-0.45,0.8) -- cycle;
    % Right kirpan (mirrored)
    \draw[accent, line width=1.0pt] (0.55,0.95)
      .. controls (0.5,0.65) and (0.35,0.45) .. (0.35,0.1) -- (0.25,0.2)
      .. controls (0.38,0.5) and (0.42,0.65) .. (0.45,0.8) -- cycle;
  \end{tikzpicture}%
}

% Key–value row inside a tabularx
% #1 = key (bold, accent), #2 = value
\newcommand{\kvrow}[2]{%
  \textbf{\color{accent}#1} & #2%
}
