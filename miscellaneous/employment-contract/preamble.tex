% Packages, colours and custom commands for this template.
% Employment Contract Template
% Can be edited and compiled online at https://letx.app
% Compiles with pdflatex: latexmk -pdf main.tex

\usepackage{amsmath}

% --- Encoding and Fonts ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% --- Page Geometry ---
\usepackage[letterpaper, margin=1in, top=0.8in, bottom=0.9in, headheight=14pt]{geometry}

% --- Colors ---
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1A5276}
\definecolor{accentlight}{HTML}{D4E6F1}
\definecolor{accentlighter}{HTML}{EBF5FB}
\definecolor{dark}{HTML}{2C3E50}
\definecolor{medium}{HTML}{7F8C8D}
\definecolor{softgray}{HTML}{F2F3F4}

% --- Essential Packages ---
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usepackage{fontawesome5}
\usepackage{fancyhdr}
\usepackage[hidelinks]{hyperref}

% --- Paragraph Spacing ---
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\newcommand{\parsp}{\vspace{5pt}}

% --- Table Setup ---
\renewcommand{\arraystretch}{1.2}

% --- Header / Footer ---
\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
  \fancyfoot[C]{\footnotesize\color{medium}\thepage}
}
\pagestyle{plain}

% --- Section Formatting ---
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {\thesection}
  {1em}
  {}
  [\vspace{-0.3em}{\color{accent}\titlerule[0.5pt]}\vspace{0.5em}]

\titlespacing*{\section}{0pt}{14pt}{6pt}

% --- List Setup ---
\setlist[itemize]{leftmargin=*,nosep,label=\textbullet,itemsep=2pt}
\setlist[enumerate]{leftmargin=*,nosep,itemsep=2pt}

% --- Custom Boxes ---
\newtcolorbox{commencebox}{
  colback=accentlight,
  colframe=accent,
  arc=4pt,
  boxrule=0.7pt,
  left=12pt,
  right=12pt,
  top=10pt,
  bottom=10pt,
  before skip=6pt,
  after skip=10pt,
}

\newtcolorbox{atwillbox}{
  colback=accentlighter,
  colframe=accent,
  arc=4pt,
  boxrule=1pt,
  left=14pt,
  right=14pt,
  top=12pt,
  bottom=12pt,
  before skip=10pt,
  after skip=6pt,
  borderline west={3pt}{0pt}{accent},
  breakable,
}

% --- Key-Value Table Column ---
\newcolumntype{V}{>{\raggedright\arraybackslash}X}

% --- Signature Block Helper ---
\newcommand{\sigline}[3]{%
  \begin{minipage}[t]{0.46\linewidth}
    \vspace{22pt}
    \rule{\linewidth}{0.5pt}\\[3pt]
    {\footnotesize\color{medium}#1}\\[2pt]
    {\bfseries#2}\\[1pt]
    {\small#3}
  \end{minipage}
}
