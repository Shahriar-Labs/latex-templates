% Extended Abstract Template — edit & compile online at https://letx.app
% Compiles with: latexmk -pdf main.tex  (pdflatex)
% Two-page, two-column conference extended abstract with pgfplots figure.

\documentclass[10pt,twocolumn]{article}

% ─── Geometry ───────────────────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=18mm, bottom=18mm,
  left=15mm, right=15mm,
  columnsep=6mm
]{geometry}

% ─── Fonts & encoding ───────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{tgpagella}          % Palatino-style serif (elegant, classic)
\usepackage{microtype}

% ─── Colour ─────────────────────────────────────────────────────────────────
\usepackage{xcolor}
\definecolor{accent}{HTML}{1A4E8C}   % steel-blue accent
\definecolor{muted}{HTML}{555555}    % body-text neutral
\definecolor{rule}{HTML}{C0C8D8}     % light rule

% ─── Core packages ──────────────────────────────────────────────────────────
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{titlesec}
\usepackage{parskip}
\usepackage{hyperref}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ─── Spacing fix (required when parskip is loaded) ──────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{3.5pt plus 1pt minus 1pt}

% ─── Section headings ────────────────────────────────────────────────────────
\titleformat{\section}
  {\normalfont\normalsize\bfseries\color{accent}}
  {}
  {0pt}
  {\MakeUppercase}
  [\vspace{-2pt}\color{rule}\hrule height 0.6pt\vspace{2pt}]

\titlespacing*{\section}{0pt}{7pt}{3pt}

% ─── Caption style ───────────────────────────────────────────────────────────
\captionsetup{
  font={small,color=muted},
  labelfont={bf,color=accent},
  skip=4pt
}

% ─── Hyperref ────────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  urlcolor=accent,
  citecolor=accent,
  linkcolor=accent,
  pdftitle={Adaptive Gradient Pruning for Energy-Efficient Neural Inference},
  pdfauthor={Elena Vasquez; Dmitri Orlov; Jun-Yi Chen}
}

% ─── Title macros ────────────────────────────────────────────────────────────
\makeatletter
\newcommand{\papertitle}[1]{\def\@papertitle{#1}}
\newcommand{\paperauthors}[1]{\def\@paperauthors{#1}}
\newcommand{\paperaffil}[1]{\def\@paperaffil{#1}}
\newcommand{\paperemail}[1]{\def\@paperemail{#1}}

\newcommand{\maketitleblock}{%
  \twocolumn[{%
    \begin{center}
      {\LARGE\bfseries\color{accent} \@papertitle\par}
      \vspace{6pt}
      {\normalsize \@paperauthors\par}
      \vspace{2pt}
      {\small\color{muted} \@paperaffil\par}
      \vspace{2pt}
      {\small\color{muted} \texttt{\@paperemail}\par}
    \end{center}
    \vspace{2pt}
    \color{rule}\hrule height 1pt
    \vspace{8pt}
  }]
}
\makeatother

% ─── Abstract environment ─────────────────────────────────────────────────────
\renewenvironment{abstract}{%
  \small\color{muted}\itshape
  \textbf{\color{accent}Abstract.}\ \ignorespaces
}{%
  \par
}

% ─── Keywords macro ───────────────────────────────────────────────────────────
\newcommand{\keywords}[1]{%
  \vspace{2pt}
  {\small\color{muted}\textbf{Keywords:} #1\par}
  \vspace{6pt}
  \color{rule}\hrule height 0.4pt
  \vspace{6pt}
}

% ─── References style ────────────────────────────────────────────────────────
\usepackage[numbers,sort&compress]{natbib}
\setlength{\bibsep}{2pt}
\renewcommand{\bibfont}{\small}

% ════════════════════════════════════════════════════════════════════════════
\begin{document}

\papertitle{Adaptive Gradient Pruning for\\Energy-Efficient Neural Inference}
\paperauthors{%
  Elena Vasquez$^{1}$\quad Dmitri Orlov$^{2}$\quad Jun-Yi Chen$^{1,3}$%
}
\paperaffil{%
  $^{1}$Department of Electrical Engineering, ETH Zurich, Switzerland\quad
  $^{2}$Samsung AI Center, Cambridge, UK\quad
  $^{3}$MIT CSAIL, Cambridge, MA, USA%
}
\paperemail{vasquez@ee.ethz.ch}

\maketitleblock

% ─── Abstract ────────────────────────────────────────────────────────────────
\begin{abstract}
We present \textit{AdaGrad-Prune}, a training-time method that co-optimises
sparsity and gradient flow in deep neural networks, targeting sub-milliwatt
inference on microcontroller-class hardware. By coupling a differentiable
threshold gate with a magnitude-sensitive gradient rescaling rule, our method
achieves up to 87\% weight sparsity while matching dense-model accuracy within
0.6\% top-1 on ImageNet. Across five edge platforms, AdaGrad-Prune reduces
arithmetic operations by a factor of $5.2\times$ and energy consumption by
$4.7\times$ versus the uncompressed baseline with no custom silicon required.
\end{abstract}

\keywords{neural network pruning, edge inference, energy efficiency,
          gradient-based optimisation, model compression}

% ─── 1. Introduction ─────────────────────────────────────────────────────────
\section{Introduction}

Deploying large neural networks on resource-constrained sensors and wearables
demands aggressive model compression without sacrificing task accuracy.
Structured pruning \cite{han2015deep} and quantisation \cite{jacob2018quantization}
are well-studied, yet few approaches simultaneously control gradient dynamics
during pruning, leading to premature weight death or unstable fine-tuning.

Recent lottery-ticket analyses \cite{frankle2019lottery} suggest that sparse
subnetworks capable of matching full-model accuracy exist within randomly
initialised networks; however, reliably identifying these subnetworks during
training remains an open problem, especially under strict energy budgets.

\textbf{Contributions.}
\begin{itemize}[leftmargin=1.2em,topsep=2pt,itemsep=1pt]
  \item A differentiable threshold gate $\sigma_\tau$ that learns per-layer
        sparsity targets end-to-end.
  \item A gradient rescaling rule that prevents gradient vanishing in pruned
        channels while preserving sparsity pressure.
  \item State-of-the-art accuracy–energy trade-off on five MCU platforms.
\end{itemize}

% ─── 2. Method ───────────────────────────────────────────────────────────────
\section{Method / Approach}

\textbf{Threshold gate.}\ Let $\mathbf{w} \in \mathbb{R}^d$ be a weight
vector and $\tau \in \mathbb{R}_{>0}$ a learnable threshold.  We define a
soft binary mask:
\begin{equation}
  m_i = \sigma\!\left(\beta\bigl(|w_i| - \tau\bigr)\right), \quad
  \hat{w}_i = m_i \cdot w_i,
  \label{eq:gate}
\end{equation}
where $\sigma$ is the sigmoid and $\beta{=}10$ controls gate sharpness.
During the forward pass, $\hat{\mathbf{w}}$ replaces $\mathbf{w}$; at
inference, $m_i < 0.5$ entries are hard-zeroed.

\textbf{Gradient rescaling.}\ For pruned weights ($m_i < 0.5$), the
upstream gradient $\partial\mathcal{L}/\partial w_i$ is multiplied by a
decay factor $\gamma = 0.05$, ensuring that weights near the threshold
receive informative—but attenuated—updates.  This prevents gradient
vanishing in deep layers while sustaining sparsity pressure throughout
training.

\textbf{Energy objective.}\ A differentiable FLOPs proxy $\Phi$ (based on
$\ell_1$ norm of masks) is added to the task loss:
\begin{equation}
  \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{CE}}
    + \lambda_\Phi \sum_{\ell} \Phi^{(\ell)},
  \label{eq:loss}
\end{equation}
where $\lambda_\Phi$ is annealed from 0 to $10^{-4}$ over the first
30\% of training epochs.

% ─── 3. Results ──────────────────────────────────────────────────────────────
\section{Results}

\textbf{Accuracy vs.\ sparsity} (ResNet-50, ImageNet-1K, 90 epochs).
Table~\ref{tab:accuracy} compares AdaGrad-Prune against three baselines at
matched sparsity.  AdaGrad-Prune closes the accuracy gap to the dense model
by $1.4\times$ compared to the next-best method.

\begin{table}[h]
\centering
\small
\caption{Top-1 accuracy at 80\% sparsity (ResNet-50, ImageNet).}
\label{tab:accuracy}
\begin{tabularx}{\linewidth}{@{}>{\raggedright\arraybackslash}Xcc@{}}
\toprule
\textbf{Method} & \textbf{Top-1 (\%)} & \textbf{FLOPs$\downarrow$} \\
\midrule
Dense baseline  & 76.8 & $1.0\times$ \\
Magnitude (one-shot) \cite{han2015deep} & 73.4 & $4.1\times$ \\
GMP \cite{zhu2018prune}          & 74.9 & $4.4\times$ \\
Lottery-ticket \cite{frankle2019lottery} & 75.2 & $4.6\times$ \\
\textbf{AdaGrad-Prune (ours)}    & \textbf{76.2} & \textbf{$5.2\times$} \\
\bottomrule
\end{tabularx}
\end{table}

\textbf{Energy measurements} were taken on an STM32H7 MCU (Cortex-M7,
480\,MHz) running single-image inference.
Figure~\ref{fig:energy} shows mean energy per inference across four
sparsity levels.  AdaGrad-Prune achieves $4.7\times$ reduction at 87\%
sparsity, reaching 0.31\,mJ per image.

\begin{figure}[h]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=\linewidth,
  height=4.4cm,
  xlabel={\small Sparsity (\%)},
  ylabel={\small Energy (mJ)},
  xlabel style={color=muted},
  ylabel style={color=muted},
  tick label style={font=\small, color=muted},
  xtick={0,50,70,80,87},
  ytick={0.3,0.6,0.9,1.2,1.5},
  ymin=0.2, ymax=1.6,
  xmin=0,  xmax=92,
  grid=major,
  grid style={line width=0.3pt, color=rule},
  axis line style={color=rule},
  legend style={
    font=\footnotesize,
    fill=white,
    draw=rule,
    at={(0.98,0.98)},
    anchor=north east
  },
  cycle list name=color list,
]
% Dense baseline (flat line for reference)
\addplot[color=muted, dashed, thick, mark=none]
  coordinates {(0,1.46)(50,1.46)(70,1.46)(80,1.46)(87,1.46)};
\addlegendentry{Dense}

% GMP
\addplot[color=accent!60, thick, mark=square*, mark size=1.8pt]
  coordinates {(50,1.10)(70,0.82)(80,0.65)(87,0.55)};
\addlegendentry{GMP}

% AdaGrad-Prune
\addplot[color=accent, thick, mark=*, mark size=2pt]
  coordinates {(50,1.02)(70,0.71)(80,0.51)(87,0.31)};
\addlegendentry{AdaGrad-Prune}

\end{axis}
\end{tikzpicture}
\caption{Energy per inference on STM32H7 vs.\ sparsity level.}
\label{fig:energy}
\end{figure}

% ─── 4. Conclusion ───────────────────────────────────────────────────────────
\section{Conclusion}

AdaGrad-Prune demonstrates that jointly learning sparsity thresholds and
rescaling gradients during training yields networks that are both
accurate and highly energy-efficient on commodity microcontrollers.
The method requires no post-training fine-tuning step and is compatible
with standard training pipelines.  Future work will extend the framework
to structured (channel-level) pruning and hardware-aware energy proxies
derived from lookup-table profiling.

% ─── References ──────────────────────────────────────────────────────────────
\section{References}
\begingroup
\renewcommand{\section}[2]{}   % suppress bibsection heading (already typeset)
\begin{thebibliography}{9}

\bibitem{han2015deep}
S.~Han, J.~Pool, J.~Tran, and W.~Dally,
\textit{Learning both weights and connections for efficient neural networks},
in \textit{Advances in Neural Information Processing Systems (NeurIPS)},
pp.~1135--1143, 2015.

\bibitem{jacob2018quantization}
B.~Jacob \textit{et al.},
\textit{Quantization and training of neural networks for efficient
integer-arithmetic-only inference},
in \textit{Proc.\ IEEE/CVF CVPR}, pp.~2704--2713, 2018.

\bibitem{frankle2019lottery}
J.~Frankle and M.~Carlin,
\textit{The lottery ticket hypothesis: Finding sparse, trainable neural networks},
in \textit{ICLR}, 2019.

\bibitem{zhu2018prune}
M.~Zhu and S.~Gupta,
\textit{To prune, or not to prune: Exploring the efficacy of pruning for
model compression},
in \textit{ICLR Workshop}, 2018.

\end{thebibliography}
\endgroup

\end{document}
