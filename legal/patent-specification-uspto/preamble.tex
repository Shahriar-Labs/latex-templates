% Packages, colours and custom commands for this template.
% ============================================================
%  USPTO Patent Specification — LaTeX Template
%  Editable and compilable online at https://letx.app
%  Engine : pdflatex  |  Build : latexmk -pdf main.tex
% ============================================================


% ── Geometry ────────────────────────────────────────────────
\usepackage[
  top=1in, bottom=1in,
  left=1in, right=1in,
  headheight=14pt
]{geometry}

% ── Core Packages ───────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

% ── Serif Body Font (Palatino style) ────────────────────────
\usepackage{mathpazo}

% ── Colour ──────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1A3A5C}      % deep navy
\definecolor{rulecolor}{HTML}{2B5F8A}   % medium blue
\definecolor{lightgray}{HTML}{F5F5F5}   % table row tint
\definecolor{midgray}{HTML}{6C6C6C}     % secondary text
\definecolor{pagetint}{HTML}{FAFAF8}    % subtle warm page

% ── Page background tint ────────────────────────────────────
\usepackage{pagecolor}
\pagecolor{pagetint}

% ── Typography & Layout ─────────────────────────────────────
\usepackage{setspace}
\setstretch{1.15}
\usepackage{ragged2e}
\usepackage{parskip}
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{array}
\usepackage{booktabs}
\usepackage{multirow}

% ── Rules & Decorations ─────────────────────────────────────
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{lastpage}

% ── Hyperref ────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=accent,
  urlcolor=rulecolor,
  pdftitle={Patent Application: Adaptive Neural Compression Engine},
  pdfauthor={Meridian IP Group LLP},
  pdfsubject={USPTO Utility Patent Application},
  pdfkeywords={patent application, neural compression, USPTO, AI}
]{hyperref}

% ── Drawing-sheet TikZ support ──────────────────────────────
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, fit, backgrounds}

% ── tcolorbox for callout blocks ────────────────────────────
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, fitting}

% ── FontAwesome5 for icons ──────────────────────────────────
\usepackage{fontawesome5}

% ── amsmath (for \text, align etc.) ─────────────────────────
\usepackage{amsmath}

% ============================================================
%  SECTION FORMATTING
% ============================================================
\newcommand{\sectionrule}{\noindent{\color{rulecolor}\rule{\linewidth}{0.6pt}}}

\titleformat{\section}
  {\color{accent}\large\bfseries\scshape}
  {}
  {0pt}
  {}
  [\sectionrule]

\titlespacing*{\section}{0pt}{14pt}{6pt}

\titleformat{\subsection}
  {\color{accent}\normalsize\bfseries}
  {}
  {0pt}
  {}

\titlespacing*{\subsection}{0pt}{10pt}{3pt}

% ============================================================
%  HEADER / FOOTER
% ============================================================
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{rulecolor}\leaders\hrule height \headrulewidth\hfill}}
\fancyhead[L]{\small\color{midgray}\textsc{US Patent Application}}
\fancyhead[C]{\small\color{accent}\textbf{Adaptive Neural Compression Engine}}
\fancyhead[R]{\small\color{midgray}App.\ No.\ 18/\textbf{472,\!839}}
\fancyfoot[L]{\small\color{midgray}\textit{Confidential — Attorney Work Product}}
\fancyfoot[C]{\small\color{midgray}Page \thepage\ of \pageref{LastPage}}
\fancyfoot[R]{\small\color{midgray}\textit{Meridian IP Group LLP}}

\fancypagestyle{firstpage}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyfoot[C]{\small\color{midgray}Page \thepage\ of \pageref{LastPage}}
  \fancyfoot[R]{\small\color{midgray}\textit{Meridian IP Group LLP}}
}

% ============================================================
%  CUSTOM MACROS
% ============================================================

% --- Bold reference numeral for drawings --------------------
\newcommand{\refnum}[1]{\textbf{(#1)}}

% --- Claim counter ------------------------------------------
\newcounter{claimcnt}
\newcommand{\claim}[1]{%
  \stepcounter{claimcnt}%
  \vspace{4pt}%
  \noindent\textbf{\theclaimcnt.}\enspace #1\par%
}

% --- Defined term -------------------------------------------
\newcommand{\defterm}[1]{\textit{#1}}

% --- Section label used in cross-references -----------------
\newcommand{\secref}[1]{\S\,\ref{#1}}

% --- Key–value metadata row (safe tabularx layout) ----------
\newcommand{\metarow}[2]{%
  {\color{accent}\bfseries #1} & #2 \\[2pt]%
}

% --- Clause box (tcolorbox) for claim highlights ------------
\tcbset{
  claimbox/.style={
    enhanced,
    breakable,
    colback=lightgray,
    colframe=accent,
    boxrule=0.5pt,
    arc=3pt,
    left=6pt, right=6pt, top=4pt, bottom=4pt,
    fonttitle=\bfseries\small\color{white},
    attach boxed title to top left={yshift=-2mm,xshift=6pt},
    boxed title style={colback=accent, arc=2pt, boxrule=0pt},
  }
}

% ============================================================
%  DOCUMENT BEGIN
% ============================================================
