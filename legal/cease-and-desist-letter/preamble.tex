% ============================================================
% preamble.tex — packages, colours, geometry, custom commands
% ============================================================

% --- Page geometry ---
\usepackage[
  top=2.4cm,
  bottom=2.8cm,
  left=2.6cm,
  right=2.6cm,
  headheight=16pt
]{geometry}

% --- Core packages ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}
\usepackage{lmodern}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{ragged2e}
\usepackage{emptypage}

% --- Math ---
\usepackage{amsmath}
\usepackage{amssymb}

% --- Colour ---
\usepackage[table,dvipsnames]{xcolor}
\definecolor{NexaNavy}{HTML}{0D1F3C}
\definecolor{NexaGold}{HTML}{B8953A}
\definecolor{NexaSlate}{HTML}{4A5568}
\definecolor{NexaFrost}{HTML}{EEF2F8}
\definecolor{NexaRed}{HTML}{9B2335}
\definecolor{NexaLine}{HTML}{D0D7E3}

% --- Headers / Footers ---
\usepackage{fancyhdr}
\usepackage{lastpage}

% --- Tables ---
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{multirow}
\usepackage{colortbl}

% --- Graphics & TikZ ---
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,decorations.pathmorphing}

% --- tcolorbox ---
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable,fitting}

% --- Lists ---
\usepackage{enumitem}

% --- Fonts & Icons ---
\usepackage{fontawesome5}

% --- Hyperref (load last) ---
\usepackage[
  colorlinks=true,
  linkcolor=NexaNavy,
  urlcolor=NexaNavy,
  citecolor=NexaNavy,
  pdftitle={Cease and Desist Letter — Nexa Corporation},
  pdfauthor={Meridian Legal Group LLP},
  pdfsubject={Intellectual Property Infringement Notice},
  pdfkeywords={cease and desist, copyright, trademark, legal notice}
]{hyperref}

% ============================================================
% TYPOGRAPHY
% ============================================================
\usepackage{titlesec}

\titleformat{\section}
  {\color{NexaNavy}\large\bfseries\scshape}
  {}
  {0pt}
  {}
  [\vspace{2pt}\color{NexaGold}\hrule height 0.8pt\vspace{4pt}]

\titlespacing*{\section}{0pt}{18pt}{8pt}

% ============================================================
% PAGE STYLE
% ============================================================
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \color{NexaSlate}\footnotesize\scshape
  Cease \& Desist Notice \textperiodcentered\ Nexa Corp.\ v.\ Vertex Systems
}
\fancyhead[R]{%
  \color{NexaSlate}\footnotesize
  \textbf{PRIVILEGED \& CONFIDENTIAL}
}
\fancyfoot[C]{%
  \color{NexaSlate}\footnotesize
  Page \thepage\ of \pageref{LastPage}
}
\fancyfoot[R]{%
  \color{NexaSlate}\footnotesize
  Ref: MLG-2024-CD-0047
}

% ============================================================
% CUSTOM COMMANDS
% ============================================================

% Demand item
\newcommand{\demanditem}[2]{%
  \item[\textcolor{NexaGold}{\faBullseye}\;]
  \textbf{#1.}\quad #2
}

% Exhibit label
\newcommand{\exhibitref}[1]{\textbf{Exhibit~#1}}

% Defined term
\newcommand{\defterm}[1]{\textbf{``#1''}}

% Warning box
\newtcolorbox{warningbox}{
  enhanced,
  breakable,
  colback=NexaRed!6!white,
  colframe=NexaRed,
  leftrule=4pt,
  toprule=0pt,
  bottomrule=0pt,
  rightrule=0pt,
  arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  fontupper=\small,
}

% Notice box
\newtcolorbox{noticebox}{
  enhanced,
  breakable,
  colback=NexaFrost,
  colframe=NexaNavy,
  leftrule=4pt,
  toprule=0pt,
  bottomrule=0pt,
  rightrule=0pt,
  arc=2pt,
  left=10pt, right=10pt, top=8pt, bottom=8pt,
  fontupper=\small,
}

% Signature line
\newcommand{\sigline}[2]{%
  \vspace{18pt}
  \noindent
  \begin{minipage}[t]{0.45\textwidth}
    \rule{7cm}{0.5pt}\\[2pt]
    \textbf{#1}\\
    {\small\color{NexaSlate}#2}
  \end{minipage}%
}
