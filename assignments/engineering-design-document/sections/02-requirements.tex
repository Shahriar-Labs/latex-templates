\section{Requirements}

\subsection{Functional}

\begin{tabularx}{\linewidth}{@{}lX@{}}
  \toprule
  \textbf{ID} & \textbf{Requirement} \\
  \midrule
  F-01 & Accept events via REST (\texttt{/v1/ingest}) and gRPC (\texttt{EventIngester.Publish}). \\[2pt]
  F-02 & Validate event envelopes against a registered Protobuf or JSON Schema. \\[2pt]
  F-03 & Route events to one or more Kafka topics based on configurable filter expressions. \\[2pt]
  F-04 & Apply transformation plugins (masking, enrichment, deduplication) in declared order. \\[2pt]
  F-05 & Expose a web UI for pipeline authoring and live event-trace debugging. \\[2pt]
  F-06 & Publish routing metrics (throughput, error rate, lag) to Prometheus. \\
  \bottomrule
\end{tabularx}

\subsection{Non-Functional}

\begin{tabularx}{\linewidth}{@{}llX@{}}
  \toprule
  \textbf{ID} & \textbf{Category} & \textbf{Requirement} \\
  \midrule
  NF-01 & Performance   & $\leq 10\,\mathrm{ms}$ p99 ingest latency at 500\,k\,ev/s. \\[2pt]
  NF-02 & Availability  & 99.95\% monthly uptime; graceful degradation under backpressure. \\[2pt]
  NF-03 & Scalability   & Horizontal autoscale 2--64 pods; stateless design. \\[2pt]
  NF-04 & Security      & mTLS between all services; PII fields masked before Kafka write. \\[2pt]
  NF-05 & Observability & Structured JSON logs; distributed traces (OpenTelemetry). \\[2pt]
  NF-06 & Compliance    & GDPR: event TTL $\leq 90$ days enforced at Kafka topic level. \\
  \bottomrule
\end{tabularx}

%% ─────────────────────────────────────────────────────────────────────────────
