% Packages, colours and custom commands for this template.
% Engineering Lab Report Template
% Edit and compile online at https://letx.app
% Compile: latexmk -pdf main.tex


% ── Geometry ──
\usepackage[margin=2.2cm,top=2.5cm,bottom=2.5cm,headheight=14pt]{geometry}

% ── Fonts ──
\usepackage{sourcesanspro}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[T1]{fontenc}

% ── Colors ──
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1B5E96}
\definecolor{accentlight}{HTML}{D6E9F8}
\definecolor{graydark}{HTML}{4A4A4A}
\definecolor{graylight}{HTML}{F4F5F6}
\definecolor{pagetint}{HTML}{FBFCFD}
\pagecolor{pagetint}

% ── Math ──
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{siunitx}
\sisetup{detect-all,per-mode=symbol}

% ── Tables ──
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}

% ── Graphics & Diagrams ──
\usepackage{tikz}
\usepackage{circuitikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{shapes,arrows.meta,positioning,calc,fit,backgrounds}

% ── Code Listings ──
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{graydark!40},
  backgroundcolor=\color{graylight},
  numbers=left,
  numberstyle=\tiny\color{graydark},
  xleftmargin=2em,
  framexleftmargin=1.5em,
  aboveskip=1em,
  belowskip=1em,
  tabsize=2,
}

% ── Section Headings ──
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {\thesection}{1em}{}
  [\vspace{-0.5em}{\color{accent!40}\hrule}]
\titleformat{\subsection}
  {\large\bfseries\color{accent!85!black}}
  {\thesubsection}{1em}{}

% ── Headers / Footers ──
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{graydark}\textit{ECE~3204 --- Lab Report~3}}
\fancyhead[R]{\small\color{graydark}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{accent!40}\leaders\hrule height \headrulewidth\hfill}}

% ── Misc ──
\usepackage{enumitem}
\setlist{nosep,leftmargin=*}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=accent,urlcolor=accent!70!black}
\usepackage{fontawesome5}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}

% ── Paragraph defaults ──
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ── Custom Environments ──
\newtcolorbox{resultbox}{
  colback=accentlight!35,
  colframe=accent,
  boxrule=0.5pt,
  arc=3pt,
  left=10pt,right=10pt,top=8pt,bottom=8pt,
  before skip=12pt,after skip=12pt,
}

\newtcolorbox{infobox}{
  colback=graylight,
  colframe=graydark!25,
  boxrule=0.5pt,
  arc=3pt,
  left=10pt,right=10pt,top=8pt,bottom=8pt,
  before skip=12pt,after skip=12pt,
}

\DeclareSIUnit{\AWG}{AWG}
