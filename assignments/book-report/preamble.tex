% Packages, colours and custom commands for this template.
% Book Report Template — edit, customize, and compile online at https://letx.app
% Designed for middle-grade students and teachers
% Compiles with: latexmk -pdf main.tex
\usepackage{amsmath}

% === Encoding and Font ===
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% === Page Layout ===
\usepackage[margin=0.75in, top=0.7in, bottom=0.65in]{geometry}
\raggedbottom

% === Colors ===
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{2D5F6E}
\definecolor{accentlight}{HTML}{7BA7B5}
\definecolor{pagebg}{HTML}{FBF9F6}
\definecolor{textgray}{HTML}{3A3A3A}

\pagecolor{pagebg}
\color{textgray}

% === Graphics and Boxes ===
\usepackage{tikz}
\usepackage{tcolorbox}
\tcbuselibrary{skins}

% === Tables ===
\usepackage{tabularx}
\usepackage{booktabs}

% === Lists ===
\usepackage{enumitem}
\setlist{nosep, leftmargin=*, itemsep=1pt, topsep=0pt, partopsep=0pt}

% === Section Formatting ===
\usepackage{titlesec}
\titleformat{\section}
    {\Large\bfseries\color{accent}}
    {}
    {0pt}
    {}
    [\vspace{-4pt}{\color{accentlight}\rule{\textwidth}{0.6pt}}\vspace{2pt}]

\titlespacing{\section}{0pt}{10pt}{4pt}

% === Headers and Footers ===
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\small\color{accentlight}\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% === Hyperlinks ===
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=accent,
    urlcolor=accent,
    citecolor=accent,
}

% === Code Listings ===
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small\color{textgray},
    backgroundcolor=\color{accent!4},
    frame=none,
    numbers=none,
    breaklines=true,
    aboveskip=6pt,
    belowskip=6pt,
    xleftmargin=10pt,
    framexleftmargin=0pt,
}

% === Paragraph Spacing ===
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% === Rating Stars ===
\newcommand{\starfilled}{\textcolor{accent}{\faStar}}
\newcommand{\starempty}{\textcolor{black!12}{\faStar}}

% === Custom Boxes ===
\newtcolorbox{bookinfobox}{
    enhanced,
    colback=white,
    colframe=accent,
    boxrule=0pt,
    leftrule=5pt,
    rightrule=0pt,
    toprule=0pt,
    bottomrule=0pt,
    arc=0pt,
    outer arc=0pt,
    sharp corners,
    boxsep=10pt,
    left=16pt,
    right=14pt,
    top=10pt,
    bottom=10pt,
    before skip=8pt,
    after skip=8pt,
}

\newtcolorbox{quotebox}{
    enhanced,
    colback=accent!6,
    colframe=accent!20,
    boxrule=0.5pt,
    arc=3pt,
    boxsep=8pt,
    left=14pt,
    right=14pt,
    top=8pt,
    bottom=8pt,
    before skip=8pt,
    after skip=8pt,
    fontupper=\itshape,
}

% === Icons ===
\usepackage{fontawesome5}
