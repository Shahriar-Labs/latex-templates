% Packages, colours and custom commands for this template.
% !TEX program = pdflatex
% Conference Travel Grant Application
% Edit and compile online at https://letx.app
%
% A clean, structured template for graduate travel grant applications.
% Compiles with pdflatex (TeX Live full). No external files needed.


% === FONT & ENCODING ===
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% === COLOR (table option loads colortbl for \rowcolor) ===
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1B3A5C}
\definecolor{accentlight}{HTML}{3A6B95}
\definecolor{dark}{HTML}{212529}
\definecolor{medium}{HTML}{6C757D}
\definecolor{lightbg}{HTML}{F4F6F9}
\definecolor{rowbg}{HTML}{ECF0F4}
\definecolor{rulecolor}{HTML}{BDC3CE}
\definecolor{pagebg}{HTML}{FBFCFD}

% === PAGE GEOMETRY ===
\usepackage[letterpaper, margin=0.85in, top=0.75in, bottom=0.75in, headheight=0pt, headsep=0pt]{geometry}
\pagecolor{pagebg}

% === PACKAGES ===
\usepackage{fontawesome5}
\usepackage{tikz}
\usetikzlibrary{calc,positioning,backgrounds}
\usepackage{tcolorbox}
\tcbuselibrary{skins}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage[hidelinks]{hyperref}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{ragged2e}

% === SPACING ===
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\abovedisplayskip}{6pt}
\setlength{\belowdisplayskip}{6pt}

% === SECTION STYLING ===
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {}{0em}
  {}
  [\vspace{-0.5em}{\color{accent!40}\rule{\textwidth}{0.6pt}}]

\titlespacing{\section}{0pt}{1.0em}{0.6em}

\titleformat{\subsection}
  {\large\bfseries\color{accent}}
  {}{0em}
  {}

\titlespacing{\subsection}{0pt}{0.8em}{0.3em}

% === LIST STYLING ===
\setlist[itemize]{leftmargin=*, nosep, itemsep=2pt, topsep=2pt}

% === TCOLORBOX STYLES ===
\newtcolorbox{infobox}{
  enhanced,
  colback=white,
  colframe=rulecolor!60,
  boxrule=0.4pt,
  left=12pt,
  right=12pt,
  top=8pt,
  bottom=8pt,
  arc=4pt,
  before skip=4pt,
  after skip=4pt,
}

\newtcolorbox{sigbox}{
  enhanced,
  colback=white,
  colframe=accent,
  boxrule=1pt,
  left=16pt,
  right=16pt,
  top=14pt,
  bottom=14pt,
  arc=5pt,
  before skip=6pt,
  after skip=6pt,
}
