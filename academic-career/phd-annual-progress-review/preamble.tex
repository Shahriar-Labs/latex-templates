\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{letterpaper, margin=0.75in, headheight=15pt, headsep=15pt, footskip=20pt}

% Color Palette (no trailing spaces)
\usepackage{xcolor}
\definecolor{primary}{HTML}{0F2A4A}
\definecolor{secondary}{HTML}{0D9488}
\definecolor{accent}{HTML}{B45309}
\definecolor{bglight}{HTML}{F8FAFC}
\definecolor{neutral}{HTML}{1E293B}
\definecolor{border}{HTML}{E2E8F0}
\definecolor{lightteal}{HTML}{F0FDFA}
\definecolor{statusgreen}{HTML}{16A34A}
\definecolor{statusorange}{HTML}{EA580C}

% Font
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% Packages
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{tcolorbox}
\usepackage{array}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{fontawesome5}
\usepackage{tikz}
\usetikzlibrary{positioning, calc, shapes.geometric, backgrounds, patterns}

% Setup links
\usepackage{hyperref}
\hypersetup{
  hypertexnames=false,
  colorlinks=true,
  linkcolor=secondary,
  citecolor=secondary,
  urlcolor=secondary,
  pdftitle={PhD Annual Progress Review},
  pdfauthor={Alexander Vance}
}

% Fix parskip / tabular misalignment
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

% Section Titles Customization
\titleformat{\section}{\color{primary}\sffamily\large\bfseries}{\thesection}{1em}{}[{\color{border}\titlerule[1pt]}]
\titleformat{\subsection}{\color{primary}\sffamily\normalsize\bfseries}{\thesubsection}{1em}{}

% Custom Info Box
\newtcolorbox{infobox}[2][]{
  colback=bglight,
  colframe=primary,
  coltitle=white,
  fonttitle=\bfseries\sffamily\small,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.2pt,
  left=10pt,
  right=10pt,
  top=8pt,
  bottom=8pt,
  #1
}

% Custom Notice Box
\newtcolorbox{noticebox}[2][]{
  colback=bglight,
  colframe=accent,
  coltitle=white,
  fonttitle=\bfseries\sffamily\small,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.2pt,
  left=10pt,
  right=10pt,
  top=8pt,
  bottom=8pt,
  #1
}

% Custom Status Box
\newtcolorbox{statusbox}[2][]{
  colback=lightteal,
  colframe=secondary,
  coltitle=white,
  fonttitle=\bfseries\sffamily\small,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.2pt,
  left=10pt,
  right=10pt,
  top=8pt,
  bottom=8pt,
  #1
}

% Signature box
\newtcolorbox{sigbox}[2][]{
  colback=white,
  colframe=border,
  coltitle=primary,
  fonttitle=\bfseries\sffamily\small,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.0pt,
  left=10pt,
  right=10pt,
  top=8pt,
  bottom=8pt,
  #1
}
