% Packages, colours and custom commands for this template.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%  Statement of Purpose — Elegant Letter-Style Template
%%  Compiles with: latexmk -pdf main.tex
%%  Edit & compile online at https://letx.app
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\usepackage{amsmath}

% --- Fonts ---
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[T1]{fontenc}

% --- Page geometry ---
\usepackage[paper=letterpaper, margin=0.9in, top=0.85in, bottom=0.85in]{geometry}

% --- Colors ---
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{2C5F8A}
\definecolor{dark}{HTML}{2D2D2D}
\definecolor{softgray}{HTML}{999999}
\definecolor{rulecolor}{HTML}{CCCCCC}
\definecolor{sidebarbg}{HTML}{F2F5F9}

% --- Graphics & boxes ---
\usepackage{tikz}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usetikzlibrary{shadows}
\usepackage{fontawesome5}
\usepackage{tabularx}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}

% --- Paragraph setup ---
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% --- Hyperref setup ---
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  pdftitle={Statement of Purpose — Priya Sharma},
  pdfauthor={Priya Sharma}
}

% --- Section formatting ---
\titleformat{\section}
  {\color{accent}\normalfont\large\bfseries}
  {}
  {0pt}
  {}
  [\vspace{-0.4em}{\color{rulecolor}\rule{\linewidth}{0.4pt}}\vspace{0.4em}]

\titlespacing{\section}{0pt}{11pt}{5pt}

% --- Sidebar box ---
\newtcolorbox{sidebarBox}{
  colback=sidebarbg,
  colframe=sidebarbg,
  arc=3pt,
  boxrule=0pt,
  left=9pt,
  right=9pt,
  top=9pt,
  bottom=9pt,
  before skip=0pt,
  after skip=0pt,
}

% --- Sidebar section heading ---
\newcommand{\sideheading}[1]{%
  \vspace{2pt}%
  {\color{accent}\bfseries\small #1}%
  \vspace{3pt}%
  \par%
}

% --- Contact row helper ---
\newcommand{\contactitem}[2]{\faIcon{#1}\hspace{0.35em}{\small #2}}
