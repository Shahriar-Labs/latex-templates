% --- Encoding & Fonts ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{mathpazo}                 % Palatino-based serif for body text
\usepackage[scaled=0.92]{helvet}      % Helvetica-like sans for headers/labels
\usepackage{microtype}
\usepackage{parskip}                  % modern spacing, no paragraph indent

% --- Page Geometry ---
\usepackage[
  letterpaper,
  left=1in, right=1in, top=1.1in, bottom=1in,
  headheight=26pt, headsep=14pt
]{geometry}

% --- Colours (deep-navy / academic-crimson dossier palette) ---
\usepackage[table]{xcolor}
\definecolor{inknavy}{HTML}{16243D}   % primary text, headings
\definecolor{crimson}{HTML}{7A1F2B}   % accent, part markers, rules
\definecolor{slate}{HTML}{55606D}     % secondary / muted text
\definecolor{gold}{HTML}{9C6B12}      % metric highlight
\definecolor{paper}{HTML}{F7F4EF}     % warm box background
\definecolor{hairline}{HTML}{D8D2C4}  % subtle rules and borders

% --- Section Headings ---
\usepackage{titlesec}
\titleformat{\section}
  {\sffamily\color{inknavy}\Large\bfseries}
  {\thesection}{0.7em}{}
  [{\color{hairline}\titlerule}]
\titleformat{\subsection}
  {\sffamily\color{crimson}\large\bfseries}
  {\thesubsection}{0.6em}{}
\titlespacing*{\section}{0pt}{1.4em}{0.6em}
\titlespacing*{\subsection}{0pt}{1.0em}{0.4em}

% --- Lists ---
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.4em, itemsep=0.25em, topsep=0.2em}
\setlist[enumerate]{leftmargin=1.6em, itemsep=0.3em, topsep=0.2em}

% --- Tables ---
\usepackage{booktabs}
\usepackage{tabularx}

% --- Boxes ---
\usepackage[most]{tcolorbox}
\tcbuselibrary{skins}

\newtcolorbox{casebox}{
  colback=paper, colframe=inknavy,
  boxrule=0pt, leftrule=3pt, arc=0mm, outer arc=0mm,
  top=3mm, bottom=3mm, left=4mm, right=4mm,
  before=\par\noindent\vspace{0.3em}, after=\vspace{0.3em}
}

\newtcolorbox{tabcard}[1]{
  colback=paper, colframe=hairline, boxrule=0.4pt,
  colbacktitle=inknavy, coltitle=white,
  fonttitle=\sffamily\bfseries\small,
  title={#1}, arc=0.4mm, outer arc=0.4mm,
  top=2mm, bottom=2mm, left=2.5mm, right=2.5mm
}

\newcommand{\metriccard}[3]{%
  \begin{tcolorbox}[
    colback=paper, colframe=hairline, boxrule=0.5pt, arc=0.6mm,
    top=2mm, bottom=2mm, left=1mm, right=1mm, valign=center
  ]
  \centering
  {\color{crimson} #1}\\[0.25em]
  {\color{inknavy}\sffamily\bfseries\Large #2}\\[0.1em]
  {\color{slate}\footnotesize #3}
  \end{tcolorbox}%
}

% --- Part Dividers ---
\newcommand{\dossierpart}[3]{%
  \vspace{1.6em}
  {\color{hairline}\rule{\linewidth}{0.6pt}}\\[0.4em]
  {\sffamily\color{crimson}\MakeUppercase{\small Part #1}}\\[0.15em]
  {\sffamily\color{inknavy}\bfseries\LARGE #2}\\[0.35em]
  {\color{slate}\small #3}\\[0.3em]
  {\color{crimson}\rule{\linewidth}{1.6pt}}
  \addcontentsline{toc}{part}{Part #1 --- #2}
  \vspace{0.9em}
}

% --- TikZ (timeline graphic on cover) ---
\usepackage{tikz}
\usetikzlibrary{positioning, calc, shapes.geometric}

% --- Icons ---
\usepackage{fontawesome5}

% --- Bibliography ---
\usepackage[round]{natbib}
\bibliographystyle{plainnat}

% --- Header / Footer ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\sffamily\footnotesize\color{slate}Rostova --- Dossier for Promotion \& Tenure}
\fancyhead[R]{\sffamily\footnotesize\color{slate}Department of Computer Science}
\fancyfoot[C]{\sffamily\footnotesize\color{slate}\thepage}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{{\color{hairline}\hrule height\headrulewidth\vskip-\headrulewidth}}

% --- Hyperlinks (load last) ---
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=inknavy,
  citecolor=crimson,
  urlcolor=crimson,
  pdftitle={Dossier for Promotion and Tenure -- Elena Rostova},
  pdfauthor={Elena Rostova}
}
