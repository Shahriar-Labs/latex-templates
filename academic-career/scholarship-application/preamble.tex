% Packages, colours and custom commands for this template.
% Scholarship Application Template — edit & compile online at https://letx.app
\usepackage{amsmath}

% === Packages ===
\usepackage[margin=0.7in,top=0.55in,bottom=0.55in]{geometry}
\usepackage[table]{xcolor}
\usepackage{lato}\renewcommand{\familydefault}{\sfdefault}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{tcolorbox}
\tcbuselibrary{skins}

% === Colors ===
\definecolor{accent}{HTML}{1A5276}
\definecolor{accentlight}{HTML}{D6EAF8}
\definecolor{neutral}{HTML}{566573}
\definecolor{pagetint}{HTML}{FCFDFD}
\pagecolor{pagetint}

% === Spacing fixes ===
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% === Section formatting ===
\titleformat{\section}
  {\color{accent}\Large\bfseries}
  {}
  {0pt}
  {}
  [\vspace{-0.2em}{\color{accent}\rule{\textwidth}{0.6pt}}\vspace{0.4em}]
\titlespacing{\section}{0pt}{1em}{0.3em}

% === Helper commands ===
\newcommand{\contactrow}[2]{%
  {\color{neutral}\faIcon{#1}}\hspace{0.35em}{\small #2}%
}

\newcommand{\refereeentry}[4]{%
  \textbf{#1}\par
  {\small\color{neutral}#2}\par
  {\small\color{neutral}#3}\par
  {\small\color{accent}\faEnvelope\hspace{0.25em}#4}\par
  \vspace{0.7em}
}

\newcommand{\factrow}[2]{%
  {\color{accent}\bfseries #1}\hspace{0.6em}{\small #2}\par\vspace{0.15em}
}

% === Hyperref ===
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
}
