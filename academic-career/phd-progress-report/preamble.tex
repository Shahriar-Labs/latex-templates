% Packages, colours and custom commands for this template.
% PhD Annual Progress Report — elegant template
% Edit or compile online at https://letx.app
% Compile: latexmk -pdf main.tex  (pdflatex, zero errors)


% ── Packages ────────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

\usepackage[
  top=22mm, bottom=22mm,
  left=22mm, right=22mm
]{geometry}

\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{tcolorbox}
\usepackage{array}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{hyperref}

% ── Colour palette ───────────────────────────────────────────────────────────
\definecolor{accent}{HTML}{1B4F8A}      % deep slate-blue (single accent)
\definecolor{accentlt}{HTML}{D9E6F5}    % very light tint for header bg
\definecolor{neutral}{HTML}{4A4A4A}     % body text grey
\definecolor{rulecolor}{HTML}{BBCFE8}   % light rule / table line colour

% Fix parskip / tabular misalignment (AGY_BRIEF rule 3)
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}

% ── Typography ───────────────────────────────────────────────────────────────
\color{neutral}
\hypersetup{colorlinks=true, urlcolor=accent, linkcolor=accent}

% ── Section headings (titlesec) ──────────────────────────────────────────────
\titleformat{\section}
  {\large\bfseries\color{accent}}
  {}
  {0pt}
  {}
  [\vspace{1pt}{\color{rulecolor}\hrule height 0.8pt}\vspace{4pt}]

\titlespacing*{\section}{0pt}{14pt}{4pt}

% ── Helper macros ─────────────────────────────────────────────────────────────
% Key/value info box row — baseline-l key column (AGY_BRIEF rule 2)
\newcommand{\inforow}[2]{%
  \textcolor{accent}{\textbf{#1}} & #2 \\[2pt]
}

% Status badge
\newcommand{\done}{\textcolor{white}{\bfseries\footnotesize\ Completed\ }}
\newcommand{\inprog}{\textcolor{white}{\bfseries\footnotesize\ In Progress\ }}
\newcommand{\pending}{\textcolor{white}{\bfseries\footnotesize\ Planned\ }}

\newcommand{\statbadge}[1]{%
  \ifnum1=1
    \expandafter\csname stat@#1\endcsname
  \fi
}
\newcommand{\statCompleted}{%
  \colorbox{accent!80!black}{\done}}
\newcommand{\statInProgress}{%
  \colorbox{accent}{\inprog}}
\newcommand{\statPlanned}{%
  \colorbox{neutral!60}{\pending}}

% Supervisor comments box — named 'rvcomment' to avoid collision (AGY_BRIEF rule 4)
\tcbuselibrary{skins, breakable}
\newtcolorbox{rvcomment}{
  enhanced,
  breakable,
  colback=accentlt,
  colframe=accent,
  leftrule=3pt,
  toprule=0pt,
  bottomrule=0pt,
  rightrule=0pt,
  arc=0pt,
  left=8pt, right=8pt,
  top=7pt, bottom=7pt,
  fontupper=\small,
}

% ── Title block ──────────────────────────────────────────────────────────────
