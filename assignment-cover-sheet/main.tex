% Elegant Assignment Cover Sheet -- LaTeX Template
% Edit and compile online at https://letx.app
% Compiles with: latexmk -pdf main.tex

\documentclass[11pt,a4paper]{article}
\usepackage{amsmath}

% -- Encoding & Fonts ----------------------------------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{sourcesanspro}
\renewcommand{\familydefault}{\sfdefault}
\usepackage{fontawesome5}

% -- Layout --------------------------------------------
\usepackage[margin=2.2cm,top=1.8cm,bottom=2cm]{geometry}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% -- Colors --------------------------------------------
\usepackage{xcolor}
\definecolor{accent}{HTML}{1B2A4A}
\definecolor{accentlight}{HTML}{E6EAF2}
\definecolor{accentmid}{HTML}{8896B0}
\definecolor{darktext}{HTML}{2C2C2C}

% -- Tables --------------------------------------------
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}

% -- Boxes ---------------------------------------------
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}
\usetikzlibrary{shadows}

% -- Graphics ------------------------------------------
\usepackage{tikz}
\usepackage{graphicx}

% -- Lists ---------------------------------------------
\usepackage{enumitem}

% -- Section Formatting --------------------------------
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries\color{accent}}
  {\thesection}
  {1em}
  {}
  [{\color{accent}\titlerule[1.2pt]}]
\titlespacing*{\section}{0pt}{20pt}{10pt}

\titleformat{\subsection}
  {\large\bfseries\color{accent!80}}
  {\thesubsection}
  {1em}
  {}
\titlespacing*{\subsection}{0pt}{14pt}{6pt}

% -- Headers / Footers ---------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\footnotesize\color{accentmid}\itshape CS301\ ---\ Problem Set 3}
\fancyfoot[C]{\footnotesize\color{accentmid}\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% -- Links ---------------------------------------------
\usepackage[hidelinks]{hyperref}

% -- Code Listings -------------------------------------
\usepackage{listings}
\lstset{
  basicstyle=\small\ttfamily,
  numbers=left,
  numberstyle=\tiny\color{accentmid},
  frame=single,
  framesep=8pt,
  xleftmargin=16pt,
  framexleftmargin=8pt,
  rulecolor=\color{accentmid!50},
  backgroundcolor=\color{accentlight!50},
  breaklines=true,
  showstringspaces=false,
  tabsize=2,
  commentstyle=\color{accent!50}\itshape,
  keywordstyle=\color{accent}\bfseries,
  stringstyle=\color{accent!70},
}

% -- Custom Environments -------------------------------
\newtcolorbox{coursebox}{
  colback=accentlight!70,
  colframe=accent,
  boxrule=0.6pt,
  arc=0pt,
  left=14pt,
  right=14pt,
  top=11pt,
  bottom=11pt,
}

\newenvironment{studenttable}
  {\tabularx{\linewidth}{@{}>{\color{accent}\bfseries}l@{\hspace{9mm}}>{\raggedright\arraybackslash}X@{}}}
  {\endtabularx}

% -- Begin Document ------------------------------------
\begin{document}

% ═══════════════════════════════════════════════════════
%  COVER PAGE
% ═══════════════════════════════════════════════════════
\thispagestyle{empty}

% --- Top row: logo placeholder + institution ---
\begin{minipage}[c]{0.16\textwidth}
  \begin{tikzpicture}[baseline=(logo.base)]
    \node[draw=accent,fill=accentlight,inner sep=0pt,minimum width=22mm,minimum height=22mm] (logo)
      {\color{accent}\fontsize{18}{18}\selectfont\faUniversity};
  \end{tikzpicture}
\end{minipage}%
\hfill
\begin{minipage}[c]{0.78\textwidth}
  \raggedleft
  {\color{accent}\Large\scshape University of Technology Sydney}\\[2pt]
  {\color{accentmid}\small\MakeUppercase{Department of Computer Science}}\\[2pt]
  {\color{accentmid}\footnotesize Faculty of Engineering \& Information Technology}
\end{minipage}

\vspace{4mm}
{\color{accentmid!40}\hrule height 0.6pt}

\vspace{22mm}

% --- Title ---
\begin{center}
  {\color{accent}\fontsize{26}{32}\selectfont\bfseries\MakeUppercase{Assignment}\\[2pt]
  \MakeUppercase{Cover Sheet}}\\[6pt]
  {\color{accent}\rule{64mm}{1.4pt}}
\end{center}

\vspace{16mm}

% --- Course information block ---
\begin{coursebox}
  \begin{center}
    {\color{accent}\large\bfseries CS301\ ---\ Advanced Algorithms}\\[4pt]
    {\color{darktext}\large Problem Set 3:\ Graph Algorithms}\\[2pt]
    {\color{accentmid}\small Semester 2, 2026}
  \end{center}
\end{coursebox}

\vspace{14mm}

% --- Student details ---
\newcommand{\tabgap}{\\[9pt]}
\begin{studenttable}
  Student Name    & Emily Chen     \tabgap
  Student ID      & 2024-07-156    \tabgap
  Program         & BSc Computer Science (Honours) \tabgap
  Instructor      & Dr.\ Robert Kim \tabgap
  Due Date        & 15 July 2026   \tabgap
  Word Count      & 2,450 words \\
\end{studenttable}

\vspace{18mm}

% --- Barcode-readable identifier ---
\begin{center}
  \begin{tikzpicture}
    \draw[fill=accentlight!80,draw=accent!25] (0,0) rectangle (82mm,18mm);
    \pgfmathsetseed{42}
    \foreach \x in {0.5,0.9,1.3,1.8,2.2,2.7,3.0,3.5,3.9,4.3,4.8,5.1,5.6,6.1,6.5,6.9,7.3,7.8} {
      \pgfmathsetmacro{\w}{0.6 + rnd*0.9}
      \draw[fill=accent] (\x*4.2mm,2.5mm) rectangle +(\w*2mm,13mm);
    }
    \node[below=2.5mm,font=\footnotesize\color{accentmid}] at (41mm,0)
      {Assignment ID:\ CS301-PS3-2026-07-156};
  \end{tikzpicture}
\end{center}

\vfill
{\color{accentmid!45}\hrule height 0.5pt}
\vspace{3mm}
{\footnotesize\color{accentmid}\itshape Generated \today\ for Emily Chen\ (2024-07-156)\hfill Page 1 of 4}

\newpage

% ═══════════════════════════════════════════════════════
%  CONTENT
% ═══════════════════════════════════════════════════════

\section{Problem 1:\ Dijkstra's Algorithm --- Runtime Analysis}

Given a directed graph $G = (V, E)$ with non-negative edge weights, Dijkstra's algorithm finds
the shortest path from a source vertex $s$ to every other vertex. The standard implementation
uses a binary min-heap priority queue, achieving worst-case time complexity
$O\big((|V| + |E|)\,\log|V|\big)$.

\subsection{Setup}

All benchmarks were run on an Corex Core i7-13700K with 32\,GB RAM under Linux 6.1. Graphs were
generated using the Erd\H{o}s--R\'enyi model with varying edge probabilities to control density.
Each data point represents the median of 50 independent runs; variance across runs was below
$2\%$ in all cases.

\subsection{Empirical Results}

\begin{table}[h]
  \centering
  \caption{Runtime measurements across graph densities and sizes. All times in milliseconds.}
  \label{tab:runtimes}
  \begin{tabular}{@{}lrrrr@{}}
    \toprule
    \textbf{Graph Class} & \textbf{$|V|$} & \textbf{$|E|$} & \textbf{Time (ms)} & \textbf{Mem.\ (MB)} \\
    \midrule
    Small sparse   & 100   & 198       & 0.41  & 0.8  \\
    Small dense    & 100   & 2\,475    & 1.89  & 1.4  \\
    Medium sparse  & 1\,000  & 1\,998    & 5.72  & 6.1  \\
    Medium dense   & 1\,000  & 249\,683  & 148.3 & 22.7 \\
    Large sparse   & 10\,000 & 19\,996   & 81.40 & 58.3 \\
    Large dense    & 10\,000 & 24.9\,M   & 18\,244 & 3\,210 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Discussion}

Three patterns emerge from Table~\ref{tab:runtimes}:

\begin{enumerate}[leftmargin=*,itemsep=3pt]
  \item \textbf{Sparse graphs} scale near-linearly with $|V|$, consistent with the
    $O(|V|\log|V|)$ bound when $|E| = \Theta(|V|)$. Doubling $|V|$ roughly doubles the
    runtime.
  \item \textbf{Dense graphs} exhibit quadratic behaviour as the $|E|\log|V|$ term dominates.
    The jump from medium dense to large dense confirms $O(|V|^2\log|V|)$; a $10\times$ increase
    in vertices produces a $\sim 125\times$ runtime increase.
  \item \textbf{Memory} tracks $O(|V| + |E|)$ storage for the adjacency list and distance
    array, with heap overhead adding roughly $2\times$ on dense inputs.
\end{enumerate}

A Fibonacci heap would reduce the dense-case bound to $O(|E| + |V|\log|V|)$, but its constant
factors make binary heaps faster on graphs with fewer than roughly $10^5$ vertices in practice.

\section{Problem 2:\ Priority Queue Implementation}

The benchmark harness uses the Python implementation shown in Listing~\ref{lst:dijkstra}.
The key optimisation is \emph{lazy deletion}: rather than implementing a full
\texttt{decrease\_key} operation, stale heap entries are silently skipped when popped.

\begin{lstlisting}[
  language=Python,
  caption={Dijkstra's algorithm with binary heap and lazy deletion.},
  label={lst:dijkstra}
]
import heapq
from collections.abc import Mapping

def dijkstra(graph: Mapping, source: str) -> dict[str, float]:
    """Compute single-source shortest paths via binary heap.

    Args:
        graph: adjacency map {vertex: [(neighbor, weight), ...]}
        source: starting vertex label

    Returns:
        distance map {vertex: shortest_path_length}
    """
    dist = {v: float('inf') for v in graph}
    dist[source] = 0.0
    pq: list[tuple[float, str]] = [(0.0, source)]

    while pq:
        d, u = heapq.heappop(pq)
        if d > dist[u]:          # stale entry -- skip
            continue
        for v, w in graph[u]:
            nd = d + w
            if nd < dist[v]:
                dist[v] = nd
                heapq.heappush(pq, (nd, v))
    return dist
\end{lstlisting}

\subsection{Correctness}

The algorithm maintains the invariant that when vertex $u$ is popped with distance $d$, that
distance is optimal. Proof sketch: all edge weights are non-negative, so any alternative path
through an unvisited vertex $v$ (with $d(v) \ge d$) would have length at least $d$. Lazy
deletion on line~21 correctly handles vertices that appear multiple times with superseded
distances --- only the smallest-distance entry for each vertex is ever processed.

\section{Problem 3:\ Algorithmic Alternatives}

For specific graph families, algorithms other than general-purpose Dijkstra offer better
constant factors or tighter asymptotic bounds. Table~\ref{tab:alternatives} summarises the
trade-offs.

\begin{table}[h]
  \centering
  \caption{Shortest-path algorithms compared by graph characteristics.}
  \label{tab:alternatives}
  \begin{tabular}{@{}llll@{}}
    \toprule
    \textbf{Algorithm} & \textbf{Complexity} & \textbf{Suitable For} & \textbf{Limitation} \\
    \midrule
    BFS                         & $O(V+E)$            & Unweighted graphs       & Fails with weights \\
    Dial's algorithm            & $O(V+E)$            & Small integer weights   & Bucket count $\propto$ max weight \\
    0--1 BFS (deque)            & $O(V+E)$            & Edge weights $\in\{0,1\}$ & Binary weights only \\
    Bellman--Ford               & $O(VE)$             & Negative edges allowed  & Slower; detects cycles \\
    A* search                   & $O((V+E)\log V)$    & Goal-directed, heuristic & Needs admissible heuristic \\
    Dijkstra (binary heap)      & $O((V+E)\log V)$    & General, $w \ge 0$      & Baseline choice \\
    Dijkstra (Fibonacci heap)   & $O(E+V\log V)$      & Dense, $w \ge 0$        & High constant factor \\
    \bottomrule
  \end{tabular}
\end{table}

For the problem instances in this assignment, the binary-heap Dijkstra implementation in
Listing~\ref{lst:dijkstra} provides the best balance of simplicity, correctness guarantees,
and empirical performance. The implementation passes all provided test cases and handles
edge cases including disconnected components and zero-weight edges correctly.

\end{document}
