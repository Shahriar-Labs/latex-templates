\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{geometry}
\geometry{a4paper, margin=0.7in, headheight=20pt, headsep=10pt, footskip=15pt}

% Custom colors (Slate/Navy theme for finance proposal)
\usepackage{xcolor}
\definecolor{primary}{HTML}{0F172A}
\definecolor{secondary}{HTML}{1E293B}
\definecolor{accent}{HTML}{2563EB}
\definecolor{accentlight}{HTML}{EFF6FF}
\definecolor{success}{HTML}{0D9488}
\definecolor{successlight}{HTML}{F0FDFA}
\definecolor{neutral}{HTML}{334155}
\definecolor{border}{HTML}{E2E8F0}
\definecolor{bglight}{HTML}{F8FAFC}

% Font: Lato (clean, professional sans-serif)
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% Math packages
\usepackage{amsmath}
\usepackage{amssymb}

% Tables
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{array}

% Header and Footer
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{fontawesome5}

% TikZ for diagrams
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, calc, shapes.geometric, backgrounds, patterns, fit}

% tcolorbox setup
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

\tcbset{
  proposalbox/.style={
    enhanced,
    colback=bglight,
    colframe=border,
    coltitle=primary,
    fonttitle=\bfseries\normalsize,
    boxrule=0.8pt,
    top=4pt,
    bottom=4pt,
    left=8pt,
    right=8pt,
    arc=3pt,
    before skip=6pt,
    after skip=6pt
  },
  accentbox/.style={
    enhanced,
    colback=accentlight,
    colframe=accent,
    coltitle=accent,
    fonttitle=\bfseries,
    boxrule=1.2pt,
    leftrule=5pt,
    arc=3pt,
    top=4pt, bottom=4pt, left=8pt, right=8pt,
    before skip=6pt, after skip=6pt
  }
}

% Hyperref
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  urlcolor=accent,
  linkcolor=primary,
  citecolor=accent,
  pdftitle={RFP Technical Proposal Response: Enterprise Ledger Integration Hub},
  pdfauthor={Nexa Solutions Inc.}
}

% Section Titles Styling
\usepackage{titlesec}
\titleformat{\section}
  {\color{primary}\large\bfseries}
  {\thesection}{0.6em}{}
  [\vspace{1pt}{\color{accent}\hrule height 1.2pt}\vspace{2pt}]

\titleformat{\subsection}
  {\color{secondary}\normalsize\bfseries}
  {\thesubsection}{0.5em}{}
  [\vspace{1pt}]

\titlespacing*{\section}{0pt}{8pt}{3pt}
\titlespacing*{\subsection}{0pt}{6pt}{2pt}

% Headers & Footers
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0.5pt}
\setlength{\headheight}{22pt}

\fancyhead[L]{\small\color{neutral}\bfseries RFP Response: Enterprise Ledger Integration Hub}
\fancyhead[R]{\small\color{neutral}\faBuilding\ Nexa Solutions}
\fancyfoot[L]{\small\color{neutral}\faFileContract\ RFP-2026-FI-089}
\fancyfoot[R]{\small\color{neutral}Page \thepage\ of \pageref{LastPage}}

% Column types for tabularx
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\newcolumntype{C}{>{\centering\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}

% Compliance status badge command
\newcommand{\compliancebadge}[1]{%
  \ifx#1C%
    \tikz[baseline=(char.base)]\node[draw=success, fill=successlight, text=success, rounded corners=2pt, inner xsep=4pt, inner ysep=1.5pt, font=\scriptsize\bfseries, anchor=base](char){COMPLIANT};%
  \else
    \ifx#1P%
      \tikz[baseline=(char.base)]\node[draw=accent, fill=accentlight, text=accent, rounded corners=2pt, inner xsep=4pt, inner ysep=1.5pt, font=\scriptsize\bfseries, anchor=base](char){PARTIAL};%
    \fi
  \fi
}

\usepackage{enumitem}
\setlist[itemize]{leftmargin=*,itemsep=1.5pt,parsep=0pt,topsep=1.5pt}
\setlist[enumerate]{leftmargin=*,itemsep=1.5pt,parsep=0pt,topsep=1.5pt}

% Paragraph formatting
\setlength{\parindent}{0pt}
\setlength{\parskip}{2.5pt}
