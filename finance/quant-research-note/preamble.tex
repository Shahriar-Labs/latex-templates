% Packages, colours and custom commands for this template.
% Quant Research Note — elegant, modern, compile-clean with pdflatex
% Edit and compile online at https://letx.app
% Target: Two Sigma / Citadel / Point72 / AQR
\usepackage{amsmath}

% ── Encoding & Geometry ──────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=0.50in, top=0.45in, bottom=0.40in, headheight=14pt]{geometry}

% ── Colors (restrained navy / finance palette) ───────────────────
\usepackage[table]{xcolor}
\definecolor{navy}{HTML}{1A2744}
\definecolor{gold}{HTML}{B8944C}
\definecolor{bg}{HTML}{F8F6F0}
\definecolor{dark}{HTML}{2C3E50}
\definecolor{lightrule}{HTML}{C8BFB0}
\definecolor{posgreen}{HTML}{1F7A4C}
\definecolor{negred}{HTML}{C0392B}
\definecolor{softblue}{HTML}{E8EDF4}
\definecolor{midgray}{HTML}{7F8C8D}
\pagecolor{bg}

% ── Fonts ────────────────────────────────────────────────────────
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

% ── Packages ─────────────────────────────────────────────────────
\usepackage{tcolorbox}
\tcbuselibrary{skins}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{hyperref}

% ── Paragraph spacing (hard rule: both 0 to avoid table misalignment)
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\abovedisplayskip}{4pt}
\setlength{\belowdisplayskip}{4pt}

% ── Hyperref ─────────────────────────────────────────────────────
\hypersetup{colorlinks=true, linkcolor=navy, urlcolor=navy!80!black,
  pdfauthor={A. Chen, PhD}, pdftitle={Quantitative Research Note — Mean-Reversion Strategy}}

% ── Section formatting ───────────────────────────────────────────
\titleformat{\section}
  {\normalsize\bfseries\color{navy}}
  {}
  {0pt}
  {}
  [\vspace{-0.6\baselineskip}\rule{\linewidth}{0.4pt}\vspace{-0.2\baselineskip}]
\titlespacing{\section}{0pt}{8pt}{4pt}

% ── Custom boxes ─────────────────────────────────────────────────
\newtcolorbox{metricbox}{
  enhanced, sharp corners, boxrule=0pt, left=8pt, right=8pt, top=6pt, bottom=6pt,
  colback=navy, coltext=white, fontupper=\sffamily\small
}
\newtcolorbox{alertbox}{
  enhanced, sharp corners, boxrule=0.6pt, left=8pt, right=8pt, top=5pt, bottom=5pt,
  colback=softblue, colframe=navy!30, fontupper=\sffamily\footnotesize
}

% ── Helper commands ──────────────────────────────────────────────
\newcommand{\metarow}[2]{#1 & {\color{white!85!bg}\hfill#2} \\[1.2pt]}
\newcommand{\headitem}[1]{\vspace{3pt}{\footnotesize\color{midgray}\bfseries\MakeUppercase{#1}}\vspace{1pt}}
