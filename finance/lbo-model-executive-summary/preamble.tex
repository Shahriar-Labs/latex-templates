% ============================================================
%  PREAMBLE — packages, colours, commands, geometry
% ============================================================

% ---- Page geometry -----------------------------------------
\usepackage[
  top=22mm, bottom=24mm,
  left=22mm, right=22mm,
  headheight=14pt
]{geometry}

% ---- Core packages -----------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{calc}
\usepackage{etoolbox}
\usepackage{ifthen}

% ---- Math --------------------------------------------------
\usepackage{amsmath}
\usepackage{amssymb}

% ---- Colour ------------------------------------------------
\usepackage[table]{xcolor}

% ---- Brand palette -----------------------------------------
%   Navy      — primary headings, rules
%   Slate     — body / secondary text
%   Gold      — accent / KPI highlights
%   IceBlue   — table row shading, panel backgrounds
\definecolor{NavyDark}{HTML}{0B2545}
\definecolor{NavyMid}{HTML}{134074}
\definecolor{GoldAccent}{HTML}{C9952A}
\definecolor{IceBlue}{HTML}{E8EFF8}
\definecolor{SlateGrey}{HTML}{4A5568}
\definecolor{LightRule}{HTML}{CBD5E0}
\definecolor{WhiteBg}{HTML}{FFFFFF}
\definecolor{SuccessGreen}{HTML}{276749}
\definecolor{AlertRed}{HTML}{9B2335}

% ---- Typography --------------------------------------------
\usepackage{lmodern}
% Heading font via NFSS (no fontspec needed)
\renewcommand{\rmdefault}{lmr}

% ---- Header / footer ---------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \color{NavyMid}\footnotesize\textbf{CONFIDENTIAL}%
  \quad\textcolor{LightRule}{|}%
  \quad Nexa Capital Partners%
}
\fancyhead[R]{%
  \color{SlateGrey}\footnotesize Project Meridian — LBO Executive Summary%
}
\fancyfoot[C]{%
  \textcolor{LightRule}{\rule{0.4\textwidth}{0.4pt}}\\[2pt]
  \color{SlateGrey}\scriptsize\thepage%
}

% ---- Section styling ---------------------------------------
\usepackage[explicit]{titlesec}

\titleformat{\section}
  {\color{NavyDark}\large\bfseries}
  {}
  {0pt}
  {%
    \begin{tcolorbox}[
      enhanced, sharp corners,
      colback=NavyDark, colframe=NavyDark,
      left=8pt, right=8pt, top=4pt, bottom=4pt,
      width=\linewidth, boxrule=0pt
    ]
    \color{white}#1%
    \end{tcolorbox}%
  }
\titlespacing{\section}{0pt}{16pt}{6pt}

\titleformat{\subsection}
  {\color{NavyMid}\normalsize\bfseries}
  {}
  {0pt}
  {\textcolor{GoldAccent}{\rule[-.5ex]{3pt}{13pt}}\hspace{6pt}#1}
\titlespacing{\subsection}{0pt}{12pt}{4pt}

% ---- Tables ------------------------------------------------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{array}
\usepackage{colortbl}
\renewcommand{\arraystretch}{1.35}
\setlength{\tabcolsep}{7pt}

% ---- Coloured boxes ----------------------------------------
\usepackage[most]{tcolorbox}

% KPI card style
\tcbset{
  kpicard/.style={
    enhanced, sharp corners,
    colback=IceBlue, colframe=NavyMid,
    boxrule=0.6pt,
    left=8pt, right=8pt, top=6pt, bottom=6pt,
    fonttitle=\bfseries\small\color{NavyMid},
    title style={colback=NavyMid},
    attach boxed title to top left={yshift=-2mm, xshift=4mm},
    boxed title style={sharp corners, colback=NavyMid, colframe=NavyMid,
      left=4pt, right=4pt, top=2pt, bottom=2pt}
  },
  sidenotebox/.style={
    enhanced, sharp corners,
    colback=GoldAccent!8, colframe=GoldAccent,
    boxrule=0.6pt,
    borderline west={3pt}{0pt}{GoldAccent},
    left=8pt, right=8pt, top=5pt, bottom=5pt
  },
  riskbox/.style={
    enhanced, sharp corners,
    colback=AlertRed!6, colframe=AlertRed,
    boxrule=0.5pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt
  }
}

% ---- TikZ / pgfplots ---------------------------------------
\usepackage{tikz}
\usetikzlibrary{positioning, calc, backgrounds, patterns}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

% ---- Lists -------------------------------------------------
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.4em, itemsep=2pt, topsep=3pt}
\setlist[enumerate]{leftmargin=1.6em, itemsep=2pt, topsep=3pt}

% ---- Utilities ---------------------------------------------
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\captionsetup{
  font={small,color=SlateGrey},
  labelfont={bf,color=NavyMid},
  skip=5pt
}

% ---- Hyperlinks (loaded last) ------------------------------
\usepackage[
  colorlinks=true,
  linkcolor=NavyMid,
  citecolor=NavyMid,
  urlcolor=NavyMid,
  pdftitle={Project Meridian -- LBO Executive Summary},
  pdfauthor={Nexa Capital Partners},
  pdfsubject={Leveraged Buyout Executive Summary},
  pdfkeywords={LBO, private equity, Meridian, Nexa Capital}
]{hyperref}

% ---- Custom commands ---------------------------------------
% Monetary shorthand
\newcommand{\mn}[1]{\$#1\,mn}
\newcommand{\bn}[1]{\$#1\,bn}

% KPI display: \kpirow{label}{value}{unit}
\newcommand{\kpirow}[3]{%
  \textbf{\color{NavyMid}#1} & \textbf{\color{GoldAccent}#2} & \color{SlateGrey}#3 \\
}

% Coloured rule
\newcommand{\colourrule}[1][NavyDark]{%
  {\color{#1}\hrule height 0.8pt}\vspace{4pt}%
}

% Section running label (right-aligned small caps)
\newcommand{\sectiontag}[1]{%
  \hfill\textcolor{SlateGrey}{\footnotesize\textsc{#1}}%
}

% Positive/negative delta
\newcommand{\posnum}[1]{\textcolor{SuccessGreen}{\textbf{#1}}}
\newcommand{\negnum}[1]{\textcolor{AlertRed}{\textbf{#1}}}
