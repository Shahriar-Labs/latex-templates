% Packages, colours and custom commands for this template.
% ── Core packages ────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[margin=2cm,top=2.2cm,bottom=2.5cm]{geometry}
\usepackage{microtype}
\usepackage{lmodern}

% ── Colour ───────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{NexaNavy}{HTML}{0D2B45}
\definecolor{NexaTeal}{HTML}{00A896}
\definecolor{NexaGold}{HTML}{F4A025}
\definecolor{NexaLight}{HTML}{F0F5F9}
\definecolor{NexaMid}{HTML}{D0DDE8}
\definecolor{NexaDark}{HTML}{1C3D5A}
\definecolor{TextBody}{HTML}{2C3E50}
\definecolor{TextMuted}{HTML}{6B7C93}

% ── Graphics & drawing ───────────────────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,decorations.pathmorphing}
\usepackage{graphicx}

% ── Boxes ────────────────────────────────────────────────────────────────────
\usepackage[most]{tcolorbox}

% ── Tables ───────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{array}
\usepackage{multirow}

% ── Lists & layout ───────────────────────────────────────────────────────────
\usepackage{enumitem}
\usepackage{multicol}
\usepackage{calc}

% ── Math ─────────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

% ── Typography ───────────────────────────────────────────────────────────────
\usepackage{fontawesome5}
\usepackage{setspace}
\usepackage{parskip}

% ── Hyperlinks ───────────────────────────────────────────────────────────────
\usepackage[hidelinks,colorlinks=false]{hyperref}

% ─────────────────────────────────────────────────────────────────────────────
%  GLOBAL STYLES
% ─────────────────────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\color{TextBody}

% Section heading style
\usepackage{titlesec}
\titleformat{\section}
  {\color{NexaNavy}\Large\bfseries}
  {}
  {0em}
  {}
  [\vspace{-4pt}\color{NexaTeal}\rule{\linewidth}{2pt}\vspace{4pt}]

\titleformat{\subsection}
  {\color{NexaDark}\normalsize\bfseries}
  {}
  {0em}
  {}

\titlespacing{\section}{0pt}{14pt}{6pt}
\titlespacing{\subsection}{0pt}{10pt}{3pt}

% ── tcolorbox styles ─────────────────────────────────────────────────────────
\tcbset{
  nexacard/.style={
    enhanced, breakable,
    colback=NexaLight, colframe=NexaTeal,
    arc=4pt, boxrule=1.2pt,
    left=8pt, right=8pt, top=6pt, bottom=6pt,
    fonttitle=\bfseries\small\color{white},
    attach boxed title to top left={yshift=-2mm, xshift=6mm},
    boxed title style={colback=NexaTeal, arc=3pt, boxrule=0pt},
  },
  statbox/.style={
    enhanced,
    colback=#1, colframe=#1,
    arc=4pt, boxrule=0pt,
    left=6pt, right=6pt, top=8pt, bottom=8pt,
    halign=center,
  },
  highlight/.style={
    enhanced,
    colback=NexaNavy!8, colframe=NexaNavy,
    arc=3pt, boxrule=0.8pt,
    left=8pt, right=8pt, top=5pt, bottom=5pt,
  },
  milebox/.style={
    enhanced,
    colback=white, colframe=NexaMid,
    arc=3pt, boxrule=0.8pt,
    left=7pt, right=7pt, top=5pt, bottom=5pt,
  },
  warnbox/.style={
    enhanced,
    colback=NexaGold!12, colframe=NexaGold,
    arc=3pt, boxrule=1pt,
    left=7pt, right=7pt, top=5pt, bottom=5pt,
  },
}

% ── Custom helpers ────────────────────────────────────────────────────────────
\newcommand{\kpi}[3]{%
  \begin{tcolorbox}[statbox=#1]
    {\color{white}\fontsize{20}{24}\selectfont\bfseries #2}\\[2pt]
    {\color{white!80!#1}\small #3}
  \end{tcolorbox}%
}

\newcommand{\sectionicon}[1]{\textcolor{NexaTeal}{#1}\hspace{4pt}}

% ─────────────────────────────────────────────────────────────────────────────
%  COVER PAGE HELPER
% ─────────────────────────────────────────────────────────────────────────────
\newcommand{\coverpage}{%
\begin{tikzpicture}[remember picture, overlay]
  \fill[NexaNavy] (current page.north west)
    rectangle ([yshift=-6.5cm]current page.north east);
  \fill[NexaTeal] ([yshift=-6.5cm]current page.north west)
    rectangle ([yshift=-7.1cm]current page.north east);
  \fill[NexaGold] ([xshift=2cm,yshift=-5.8cm]current page.north west)
    circle (0.25cm);
  \draw[NexaTeal!40, line width=1.2pt]
    ([xshift=-3cm,yshift=-2cm]current page.north east) circle (2.2cm);
  \draw[NexaTeal!20, line width=0.8pt]
    ([xshift=-3cm,yshift=-2cm]current page.north east) circle (3.8cm);
  \fill[NexaNavy!10] (current page.south west)
    rectangle ([yshift=1.2cm]current page.south east);
  \fill[NexaTeal] ([yshift=1.2cm]current page.south west)
    rectangle ([yshift=1.6cm]current page.south east);
\end{tikzpicture}%
\vspace*{1.6cm}
\begin{center}
  {\color{NexaTeal}\small\bfseries\MakeUppercase{Business Plan}}\\[6pt]
  {\color{white}\fontsize{32}{38}\selectfont\bfseries NEXA}\\[2pt]
  {\color{white}\fontsize{18}{22}\selectfont\bfseries ANALYTICS}\\[6pt]
  {\color{NexaMid}\small Transforming Data Into Decisions}
\end{center}

\vspace{1.4cm}

\begin{center}
\begin{tcolorbox}[
  enhanced, colback=NexaTeal!15, colframe=NexaTeal,
  arc=4pt, boxrule=1pt,
  width=0.72\linewidth,
  halign=center, left=10pt, right=10pt, top=8pt, bottom=8pt,
]
  {\color{NexaNavy}\bfseries\large AI-Powered Business Intelligence for SMEs}\\[4pt]
  {\color{TextMuted}\small Seed Round \textbullet{} \$2.4M Target \textbullet{} Series A Ready 2026}
\end{tcolorbox}
\end{center}

\vspace{1.0cm}

\noindent
\begin{minipage}[t]{0.32\linewidth}
  \kpi{NexaTeal}{\$18B}{Addressable Market}
\end{minipage}\hfill
\begin{minipage}[t]{0.32\linewidth}
  \kpi{NexaNavy}{340\%}{Projected YoY Growth}
\end{minipage}\hfill
\begin{minipage}[t]{0.32\linewidth}
  \kpi{NexaGold}{Q3 2027}{Break-Even Target}
\end{minipage}

\vspace{1.2cm}

\begin{center}
\begin{tabular}{@{}ccc@{}}
  \color{TextMuted}\faCalendar\enspace{\small July 2025} &
  \hspace{2cm}\color{TextMuted}\faUser\enspace{\small Prepared by: Clara Whitmore, CEO} &
  \hspace{2cm}\color{TextMuted}\faLock\enspace{\small Confidential}
\end{tabular}
\end{center}
\vspace{0.4cm}
}

% ─────────────────────────────────────────────────────────────────────────────
%  DOCUMENT
% ─────────────────────────────────────────────────────────────────────────────
