\section{Systems Engineering Process}

\subsection{Engineering Lifecycle Model}
The M-ARS program utilizes a tailored systems engineering V-model (Figure 1). This structure guarantees that validation criteria are established early during requirements analysis and verification activities are planned in parallel with detailed design, minimizing integration risks.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    scale=0.8,
    every node/.style={font=\sffamily\scriptsize, align=center},
    phase/.style={rectangle, draw=SteelBlue, fill=WarmBg, rounded corners=3pt, thick, minimum width=2.5cm, minimum height=1cm, text=NavyBlue},
    arrow/.style={-latex, thick, NavyBlue},
    dashedarrow/.style={-latex, dashed, thick, OrangeAccent}
]
    % Left side of V
    \node (n1) [phase] at (0,4) {1. Mission Needs\\ \& Concept};
    \node (n2) [phase] at (1.5,2.5) {2. Requirements\\ Analysis};
    \node (n3) [phase] at (3.0,1.0) {3. Architectural\\ Design};
    
    % Bottom of V
    \node (n4) [phase] at (4.5,-0.5) {4. HW/SW\\ Implementation};
    
    % Right side of V
    \node (n5) [phase] at (6.0,1.0) {5. Component\\ Verification};
    \node (n6) [phase] at (7.5,2.5) {6. System\\ Integration \& Test};
    \node (n7) [phase] at (9.0,4) {7. Operations\\ \& Maintenance};
    
    % V arrows
    \draw [arrow] (n1) -- (n2);
    \draw [arrow] (n2) -- (n3);
    \draw [arrow] (n3) -- (n4);
    \draw [arrow] (n4) -- (n5);
    \draw [arrow] (n5) -- (n6);
    \draw [arrow] (n6) -- (n7);
    
    % Horizontal verification arrows
    \draw [dashedarrow] (n3) -- (n5) node[midway, above] {Integration Plan};
    \draw [dashedarrow] (n2) -- (n6) node[midway, above] {Verification Criteria};
    \draw [dashedarrow] (n1) -- (n7) node[midway, above] {Validation Plan};
\end{tikzpicture}
\caption{M-ARS Systems Engineering Lifecycle V-Model}
\label{fig:vmodel}
\end{figure}

\subsection{Requirements Management \& Traceability}
All system and subsystem requirements are captured and managed within the Vertex Traceability Database. To ensure successful execution, requirements must be traced bidirectionally from user needs to physical architectures and verify that each requirement maps to at least one system verification test procedure.

Key system requirements include:
\begin{enumerate}[label=\lbrack REQ-\arabic*\rbrack, noitemsep, topsep=3pt]
  \item The M-ARS platform shall operate autonomously for a minimum duration of 180 minutes under nominal payload load.
  \item The onboard compute suite shall process sensor feeds with target detection latency not exceeding 50 milliseconds.
  \item All external communications shall be encrypted using the AES-256 standard via the Vertex module.
\end{enumerate}

\subsection{Technical Reviews and Milestones}
A series of formal technical reviews will be conducted to assess design maturity and mitigate development risk:
\begin{itemize}[noitemsep, topsep=3pt]
  \item \textbf{System Requirements Review (SRR):} Validates that user needs are properly translated into engineering requirements and that the project scope is baselined.
  \item \textbf{Preliminary Design Review (PDR):} Verifies that the allocated baseline is mature enough to proceed with detailed subsystem designs and interface controls.
  \item \textbf{Critical Design Review (CDR):} Determines if the detailed design satisfies performance metrics and establishes the product baseline for manufacturing.
  \item \textbf{Test Readiness Review (TRR):} Assesses the completeness of test procedures, test environments, and calibration rigs prior to formal verification.
\end{itemize}

% --- SECTION 3 ---
