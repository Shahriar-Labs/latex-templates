% Packages, colours and custom commands for this template.
% Aerospace Technical Report — MIL-STD compliant engineering document template
% Edit and compile online at https://letx.app
% Compiles with: latexmk -pdf main.tex (pdflatex, TeX Live full)


% ── Encoding and fonts ──────────────────────────────────────────────────
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{tgpagella}
\usepackage{textcomp}

% ── Page geometry ───────────────────────────────────────────────────────
\usepackage{geometry}
\geometry{a4paper, left=25mm, right=25mm, top=28mm, bottom=32mm, headheight=15pt, footskip=18pt}

% ── Colors ──────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}
\definecolor{accent}{HTML}{1A3C5E}
\definecolor{accentlight}{HTML}{3A6B9E}
\definecolor{bggray}{HTML}{F2F4F6}
\definecolor{bggraydark}{HTML}{E4E7EB}
\definecolor{rulegray}{HTML}{9BA5B0}
\definecolor{classred}{HTML}{8B1A1A}
\definecolor{darktext}{HTML}{1E1E1E}

% ── Graphics and diagrams ───────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{circuitikz}
\usetikzlibrary{arrows.meta, positioning, calc, fit, backgrounds, shadows, shapes.geometric}

% ── Tables ──────────────────────────────────────────────────────────────
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}

% ── Headers and footers ─────────────────────────────────────────────────
\usepackage{fancyhdr}
\usepackage{lastpage}

% ── Section formatting ──────────────────────────────────────────────────
\usepackage{titlesec}

% ── Lists ────────────────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist{nosep, leftmargin=*, itemsep=2pt, topsep=4pt}

% ── Math and symbols ────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

% ── Misc ────────────────────────────────────────────────────────────────
\usepackage{hyperref}
\usepackage{fontawesome5}
\usepackage{multicol}
\usepackage{etoolbox}
\usepackage{float}

\hypersetup{
  colorlinks=true,
  linkcolor=accentlight,
  urlcolor=accentlight,
  citecolor=accentlight,
  pdfauthor={Northrop Grumman Systems Corporation},
  pdftitle={Advanced Flight Control System Upgrade — Technical Report},
}

% ── Spacing ─────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

% ── Title format ────────────────────────────────────────────────────────
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{accent}}
  {\filleft\footnotesize\color{accentlight}\MakeUppercase{\chaptertitlename}\ \thechapter}
  {16pt}
  {\Huge}
\titlespacing*{\chapter}{0pt}{24pt}{18pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{accent}}
  {\thesection}
  {1em}
  {}
\titlespacing*{\section}{0pt}{14pt}{6pt}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{accent!85!black}}
  {\thesubsection}
  {1em}
  {}
\titlespacing*{\subsection}{0pt}{10pt}{4pt}

\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries}
  {\thesubsubsection}
  {1em}
  {}
\titlespacing*{\subsubsection}{0pt}{8pt}{3pt}

% ── Header/footer style ─────────────────────────────────────────────────
\fancypagestyle{mainpage}{
  \fancyhf{}
  \fancyhead[L]{\small\color{rulegray}\docnum}
  \fancyhead[C]{\small\color{rulegray}\MakeUppercase{\docclass}}
  \fancyhead[R]{\small\color{rulegray}Page \thepage\ of \pageref{LastPage}}
  \fancyfoot[L]{\textcolor{classred}{\rule{\textwidth}{0.4pt}}\\[1pt]
    \scriptsize\bfseries\color{classred}\MakeUppercase{\docclass}}
  \renewcommand{\headrulewidth}{0.4pt}
  \renewcommand{\headrule}{\vskip 2pt\color{accent}\hrule width\headwidth height 0.4pt\vskip-2pt}
}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[L]{\textcolor{classred}{\rule{\textwidth}{0.4pt}}\\[1pt]
    \scriptsize\bfseries\color{classred}\MakeUppercase{\docclass}}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Document metadata macros ────────────────────────────────────────────
\newcommand{\docnum}{DOC-2024-AERO-0042}
\newcommand{\docrev}{Revision~C}
\newcommand{\docdate}{15 March 2024}
\newcommand{\docclass}{UNCLASSIFIED}
\newcommand{\contractor}{Northrop Grumman Systems Corporation}
\newcommand{\contractnum}{FA8615-24-C-6042}
\newcommand{\programname}{Advanced Flight Control System (AFCS) Upgrade}
\newcommand{\aircraft}{F-15E Strike Eagle}
\newcommand{\cdrl}{CDRL A001--A012}

% ── Helper: classification banner ───────────────────────────────────────
\newcommand{\classbanner}{%
  \noindent\textcolor{classred}{\rule{\textwidth}{1.2pt}}\\[1pt]
  \noindent{\small\bfseries\color{classred}\MakeUppercase{\docclass}}\\[1pt]
  \noindent\textcolor{classred}{\rule{\textwidth}{1.2pt}}\medskip
}

% ── Helper: requirement citation ────────────────────────────────────────
\newcommand{\req}[1]{\textbf{AFCS-#1}}

% ── Helper: document reference ──────────────────────────────────────────
\newcommand{\docref}[1]{\textbf{[#1]}}

% ── Helper 1-row sub-table for key/value display ────────────────────────
\newenvironment{kvrow}[1]{%
  \par\medskip\noindent
  \begin{tabularx}{\linewidth}{@{}>{\color{accent}\bfseries}l@{\hspace{10mm}}>{\raggedright\arraybackslash}X@{}}
    #1
}{%
  \end{tabularx}\par\smallskip
}

% ── Allow display-style fractions inline ────────────────────────────────
\everymath{\displaystyle}
