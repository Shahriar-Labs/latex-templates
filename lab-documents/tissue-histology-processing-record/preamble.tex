\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}

% ─── Page geometry ─────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=20mm, bottom=22mm,
  left=17mm, right=17mm,
  headheight=15pt,
  headsep=10pt,
  footskip=14pt
]{geometry}

% ─── Colour palette --- Histopathology (Hematoxylin violet + Eosin rose) ──
\usepackage[table,dvipsnames]{xcolor}
\definecolor{histoink}{HTML}{2E2038}
\definecolor{histoviolet}{HTML}{5C3A87}
\definecolor{historose}{HTML}{B23A6B}
\definecolor{histolavender}{HTML}{F5EEF8}
\definecolor{histomuted}{HTML}{6B6478}
\definecolor{histogreen}{HTML}{2E8B57}
\definecolor{histoamber}{HTML}{B8791E}
\definecolor{histoborder}{HTML}{D8CFE0}
\definecolor{histopanel}{HTML}{FAF7FB}

\color{histoink}

% ─── Core packages ─────────────────────────────────────────────────
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{array}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{multirow}
\usepackage{enumitem}
\usepackage{calc}
\usepackage{etoolbox}
\usepackage[skip=4pt]{parskip}
\usepackage{ragged2e}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage[hidelinks,pdfusetitle]{hyperref}
\usepackage{fontawesome5}
\usepackage{titlesec}
\usepackage[most]{tcolorbox}
\usepackage[numbers,sort&compress]{natbib}

% ─── TikZ ───────────────────────────────────────────────────────────
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,decorations.pathmorphing}
\tcbuselibrary{skins,breakable,hooks,fitting}

% ─── Section styling ────────────────────────────────────────────────
\titleformat{\section}
  {\color{histoviolet}\large\bfseries\sffamily}
  {\colorbox{histoviolet}{\parbox[c][1.05em][c]{1.7em}{\centering\color{white}\thesection}}}
  {8pt}
  {}
  [\vspace{1pt}\color{historose}\hrule height 1pt\vspace{5pt}]

\titleformat{\subsection}
  {\color{histoviolet}\normalsize\bfseries\sffamily}
  {\thesubsection.}
  {6pt}
  {}

\titlespacing*{\section}{0pt}{11pt}{5pt}
\titlespacing*{\subsection}{0pt}{8pt}{3pt}

% ─── tcolorbox styles ────────────────────────────────────────────────
\tcbset{
  histbox/.style={
    enhanced,
    boxrule=0.8pt,
    arc=3pt,
    left=8pt, right=8pt, top=4pt, bottom=4pt,
    fonttitle=\bfseries\small\sffamily,
    breakable
  }
}

\newtcolorbox{infobox}[2][]{
  histbox,
  colframe=histoviolet,
  colback=histolavender,
  colbacktitle=histoviolet,
  coltitle=white,
  title={\faInfoCircle\hspace{4pt}#2},
  #1
}

\newtcolorbox{warnbox}[2][]{
  histbox,
  colframe=historose,
  colback=historose!8!white,
  colbacktitle=historose,
  coltitle=white,
  title={\faExclamationTriangle\hspace{4pt}#2},
  #1
}

\newtcolorbox{qcbox}[2][]{
  histbox,
  colframe=histogreen,
  colback=histogreen!6!white,
  colbacktitle=histogreen,
  coltitle=white,
  title={\faCheckCircle\hspace{4pt}#2},
  #1
}

\newtcolorbox{plainbox}[1][]{
  enhanced,
  colback=histopanel, colframe=histoborder,
  boxrule=0.6pt, arc=3pt, left=8pt, right=8pt, top=6pt, bottom=6pt,
  before skip=4pt, after skip=4pt,
  #1
}

% ─── Headers and footers ─────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \begin{tikzpicture}[remember picture,overlay]
    \fill[histoviolet] (0,0) rectangle (\paperwidth,0.55cm);
  \end{tikzpicture}%
  \vspace{-8pt}%
  \textcolor{white}{\footnotesize\bfseries MERIDIAN DIAGNOSTICS --- HISTOPATHOLOGY CORE}%
}
\fancyhead[R]{%
  \vspace{-8pt}%
  \textcolor{white}{\footnotesize HISTO-2026-04821\hspace{8pt}Rev.~2}%
}
\fancyfoot[C]{\footnotesize\color{gray} Page~\thepage~of~\pageref{LastPage}}
\fancyfoot[L]{\footnotesize\color{gray} Meridian Diagnostics --- Confidential Lab Record}
\fancyfoot[R]{\footnotesize\color{gray} Printed: \today}

% ─── Custom column types ─────────────────────────────────────────────
\newcolumntype{P}[1]{>{\RaggedRight\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{L}[1]{>{\RaggedRight\arraybackslash}p{#1}}

% ─── Custom units / field helpers ─────────────────────────────────────
\newcommand{\degC}{\textsuperscript{\(\circ\)}C}
\newcommand{\uL}{\textmu L}
\newcommand{\infofield}[2]{{\color{histoviolet}\bfseries\small #1:}~{\small #2}}

% ─── Status badge macros ───────────────────────────────────────────────
\newcommand{\qcpass}{\mbox{\color{histogreen}\small\faCheckCircle\ \textbf{PASS}}}
\newcommand{\qcfail}{\mbox{\color{historose}\small\faTimesCircle\ \textbf{FAIL}}}
\newcommand{\statuscomplete}{\mbox{\color{histogreen}\small\faCheckCircle\ \textbf{COMPLETE}}}
\newcommand{\statuspending}{\mbox{\color{histoamber}\small\faClock\ \textbf{PENDING}}}
\newcommand{\statusinprocess}{\mbox{\color{histoviolet}\small\faFlask\ \textbf{IN PROCESS}}}

% ─── List settings ────────────────────────────────────────────────────
\setlist[itemize,1]{
  label=\textcolor{historose}{\faChevronRight},
  leftmargin=1.2em,
  itemsep=2pt,
  parsep=0pt
}
\setlist[enumerate,1]{
  label=\textcolor{histoviolet}{\textbf{\arabic*.}},
  leftmargin=1.5em,
  itemsep=2pt,
  parsep=0pt
}

% ─── Bibliography ──────────────────────────────────────────────────────
\bibliographystyle{plain}
