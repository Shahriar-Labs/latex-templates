% Packages, colours and custom commands for this template.
% Laboratory Safety Manual — main.tex
% Elegant, modern template. Edit and compile online at https://letx.app
% Compile with: latexmk -pdf main.tex


%% ── Packages ──────────────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}
\usepackage[left=2.2cm, right=2.2cm, top=2.4cm, bottom=2.4cm]{geometry}
\usepackage{xcolor}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{microtype}
\usepackage{tikz}
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, most}
\usepackage{fontawesome5}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{colortbl}

%% ── Colour palette ────────────────────────────────────────────────────────
\definecolor{accent}{HTML}{1A5276}
\definecolor{accentlight}{HTML}{D6EAF8}
\definecolor{neutraldark}{HTML}{2C3E50}
\definecolor{neutralmid}{HTML}{7F8C8D}
\definecolor{neutralrule}{HTML}{BDC3C7}
\definecolor{warnorange}{HTML}{D35400}
\definecolor{warnbg}{HTML}{FDEBD0}
\definecolor{dangerred}{HTML}{922B21}
\definecolor{dangerbg}{HTML}{FADBD8}
\definecolor{infoteal}{HTML}{117A65}
\definecolor{infobg}{HTML}{D5F5E3}
\definecolor{rowshade}{HTML}{EBF5FB}

%% ── Spacing reset ─────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}

%% ── Hyperref ──────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  pdfauthor={Environmental Health \& Safety Office},
  pdftitle={Laboratory Safety Manual},
}

%% ── Section styling ───────────────────────────────────────────────────────
\titleformat{\section}
  {\large\bfseries\color{accent}}
  {\thesection.}{0.6em}{}
  [\vspace{1pt}\color{neutralrule}\titlerule]
\titlespacing{\section}{0pt}{14pt}{6pt}

\titleformat{\subsection}
  {\normalsize\bfseries\color{neutraldark}}
  {\thesubsection.}{0.5em}{}
\titlespacing{\subsection}{0pt}{8pt}{3pt}

%% ── tcolorbox styles ──────────────────────────────────────────────────────
\newtcolorbox{warnbox}[1][]{
  enhanced, breakable,
  colback=warnbg, colframe=warnorange, arc=3pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  fonttitle=\bfseries\color{warnorange},
  title={\faExclamationTriangle\enskip WARNING},
  #1
}

\newtcolorbox{dangerbox}[1][]{
  enhanced, breakable,
  colback=dangerbg, colframe=dangerred, arc=3pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  fonttitle=\bfseries\color{dangerred},
  title={\faExclamationCircle\enskip DANGER},
  #1
}

\newtcolorbox{infobox}[1][]{
  enhanced, breakable,
  colback=infobg, colframe=infoteal, arc=3pt,
  left=8pt, right=8pt, top=5pt, bottom=5pt,
  fonttitle=\bfseries\color{infoteal},
  title={\faInfoCircle\enskip IMPORTANT},
  #1
}

%% ── Helpers ───────────────────────────────────────────────────────────────
\newcommand{\accentrule}{%
  \noindent\color{accent}\rule{\linewidth}{2pt}\par}

%% ── Header / footer ───────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\color{neutralrule}\hrule height 0.4pt}
\fancyhead[L]{\small\color{neutralmid}\textbf{Meridian BioSciences --- Lab Safety Manual}}
\fancyhead[R]{\small\color{neutralmid}Doc No.\ EHS-LSM-2025}
\fancyfoot[C]{\small\color{neutralmid}Page \thepage\ of \pageref{LastPage}}

%% ══════════════════════════════════════════════════════════════════════════
