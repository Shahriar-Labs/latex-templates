%% preamble.tex — packages, colours, geometry, custom commands
%% Equipment Calibration Log — Nexa Analytical Laboratories

%% ── Engine & encoding ────────────────────────────────────────────────────────
\usepackage[
  T1]{fontenc}
\usepackage[utf8]{inputenc}

%% ── Page geometry (landscape,
  % no double-specification) ───────────────────────
\usepackage[
  landscape,
  top=14mm,
  bottom=14mm,
  left=14mm,
  right=14mm,
  headheight=18pt,
  headsep=6pt
]{geometry}

%% ── Colour ───────────────────────────────────────────────────────────────────
\usepackage[table]{xcolor}

\definecolor{NexaBlue}{HTML}{1B3A6B}
\definecolor{NexaTeal}{HTML}{1A9E8F}
\definecolor{NexaSilver}{HTML}{DDE3EC}
\definecolor{NexaWarn}{HTML}{C0392B}
\definecolor{NexaPass}{HTML}{1B7A4E}
\definecolor{NexaRowA}{HTML}{F2F6FB}
\definecolor{NexaRowB}{HTML}{FFFFFF}
\definecolor{NexaGold}{HTML}{B8860B}
\definecolor{HeaderBg}{HTML}{1B3A6B}
\definecolor{SubHeaderBg}{HTML}{2B5299}
\definecolor{LightGray}{HTML}{F7F9FC}

%% ── Fonts ────────────────────────────────────────────────────────────────────
\usepackage{lmodern}
\usepackage{microtype}

%% ── Math ─────────────────────────────────────────────────────────────────────
\usepackage{amsmath}
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\usepackage{amssymb}

%% ── Tables ───────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{array}
\usepackage{longtable}

%% ── Graphics & TikZ ──────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning, calc, shapes.geometric, backgrounds, patterns, decorations.pathmorphing}

%% ── tcolorbox ────────────────────────────────────────────────────────────────
\usepackage[most]{tcolorbox}
\tcbuselibrary{skins, breakable, most}

%% ── Headers & footers ────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\usepackage{lastpage}

%% ── Icons ────────────────────────────────────────────────────────────────────
\usepackage{fontawesome5}

%% ── Miscellaneous ────────────────────────────────────────────────────────────
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{ragged2e}
\usepackage{calc}
\usepackage{ifthen}
\usepackage{xspace}
\usepackage{soul}
\usepackage{float}
\usepackage{pdflscape}
\usepackage{pifont}

%% ── Hyperref (load last) ─────────────────────────────────────────────────────
\usepackage[
  colorlinks=true,
  linkcolor=NexaBlue,
  urlcolor=NexaTeal,
  pdftitle={Equipment Calibration Log -- Nexa Analytical Laboratories},
  pdfauthor={Nexa Analytical Laboratories},
  pdfsubject={Calibration Log}
]{hyperref}
\hypersetup{
  pdfkeywords={calibration, instrument, metrology, quality assurance}
}

%% ── Column types ─────────────────────────────────────────────────────────────
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{L}[1]{>{\RaggedRight\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\RaggedLeft\arraybackslash}p{#1}}

%% ── Pass / Fail badge ────────────────────────────────────────────────────────
\newcommand{\passbadge}{%
  \tikz[baseline=(n.base)]\node[
    fill=NexaPass, text=white, rounded corners=3pt,
    inner sep=2pt, font=\bfseries\scriptsize
  ](n){\faCheck\; PASS};}

\newcommand{\failbadge}{%
  \tikz[baseline=(n.base)]\node[
    fill=NexaWarn, text=white, rounded corners=3pt,
    inner sep=2pt, font=\bfseries\scriptsize
  ](n){\faTimes\; FAIL};}

\newcommand{\adjbadge}{%
  \tikz[baseline=(n.base)]\node[
    fill=NexaGold, text=white, rounded corners=3pt,
    inner sep=2pt, font=\bfseries\scriptsize
  ](n){\faWrench\; ADJ};}

%% ── Section rule ─────────────────────────────────────────────────────────────
\newcommand{\sectionrule}{%
  \noindent\textcolor{NexaTeal}{\rule{\linewidth}{1.5pt}}\par\smallskip}

%% ── Page style ───────────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyhead[L]{%
  \colorbox{NexaBlue}{\color{white}\small\bfseries\faFlask\; NEXA ANALYTICAL LABORATORIES}%
  \hspace{4pt}\textcolor{NexaBlue}{\small Equipment Calibration Log}}
\fancyhead[C]{\textcolor{NexaBlue}{\small\bfseries Form No:\ NAL-CAL-001\quad Rev.\ 04}}
\fancyhead[R]{\textcolor{NexaBlue}{\small\faCalendar\; Fiscal Year 2025--2026}}
\fancyfoot[L]{\textcolor{NexaBlue}{\tiny\faLock\; Controlled Document -- Do Not Reproduce Without Authorization}}
\fancyfoot[C]{\textcolor{NexaBlue}{\small\thepage\ of \pageref{LastPage}}}
\fancyfoot[R]{\textcolor{NexaBlue}{\tiny Approved: Q.A.\ Manager \quad ISO~9001:2015 Compliant}}

%% ── Utility ──────────────────────────────────────────────────────────────────
\newcommand{\nalfield}[2]{\textbf{\textcolor{NexaBlue}{#1:}}\; #2}
\newcommand{\tol}[1]{\(\pm\,#1\)}
