\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ---------- Page Geometry ----------
\usepackage[
  a4paper,
  top=30mm,
  bottom=30mm,
  left=32mm,
  right=32mm,
  headheight=15pt,
  headsep=8mm,
  footskip=12mm
]{geometry}

% ---------- Fonts & Math ----------
\usepackage{tgpagella}
\usepackage{newpxmath}
\let\Bbbk\relax
\let\openbox\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\let\Bbbk\relax
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{microtype}

% ---------- Colors ----------
\usepackage[table]{xcolor}
\definecolor{primary}{HTML}{1A365D}      % Deep corporate navy
\definecolor{secondary}{HTML}{0D9488}    % Muted teal
\definecolor{accent}{HTML}{B45309}       % Warm amber
\definecolor{bglight}{HTML}{F8FAFC}      % Cool white / light gray
\definecolor{textdark}{HTML}{1E293B}     % Very dark blue-gray for body text
\definecolor{grayrule}{HTML}{E2E8F0}     % Slate gray for rules

% ---------- Running Headers ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
\fancyhead[L]{\small\scshape\color{primary}\leftmark}
\fancyhead[R]{\small\itshape\color{secondary}\rightmark}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{grayrule}\leaders\hrule height\headrulewidth\hfill}}

% ---------- Section Headings ----------
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries\color{primary}}
  {\filleft\small\scshape\color{secondary}\chaptertitlename\ \Roman{chapter}}
  {10pt}
  {\filleft\Huge}
  [\vspace{10pt}{\color{secondary}\hrule height 1.5pt}]
\titlespacing*{\chapter}{0pt}{40pt}{30pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{primary}}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{18pt}{8pt}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{secondary}}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{14pt}{6pt}

% ---------- Drop Caps ----------
\usepackage{lettrine}
\renewcommand{\LettrineFontHook}{\color{primary}\bfseries}
\setcounter{DefaultLines}{3}

% ---------- Tables ----------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\renewcommand{\arraystretch}{1.3}

% ---------- Captions ----------
\usepackage{caption}
\captionsetup{font={small,color=textdark},labelfont={bf,color=primary},labelsep=period}

% ---------- Bibliography ----------
\usepackage[backend=biber,style=numeric,sorting=none,refsection=chapter]{biblatex}

% ---------- TikZ ----------
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,arrows.meta}

% ---------- Custom Callouts ----------
\usepackage[most]{tcolorbox}
\newtcolorbox{theorembox}[2][]{
  enhanced,
  colback=bglight,
  colframe=primary,
  coltitle=white,
  fonttitle=\bfseries\sffamily,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.5pt,
  left=12pt,
  right=12pt,
  top=10pt,
  bottom=10pt,
  before skip=14pt,
  after skip=14pt,
  breakable,
  #1
}

\newtcolorbox{insightbox}[2][]{
  enhanced,
  colback=bglight,
  colframe=secondary,
  coltitle=white,
  fonttitle=\bfseries\sffamily,
  title={#2},
  arc=4pt,
  outer arc=4pt,
  boxrule=1.2pt,
  left=12pt,
  right=12pt,
  top=10pt,
  bottom=10pt,
  before skip=14pt,
  after skip=14pt,
  breakable,
  #1
}

% ---------- List Spacing ----------
\usepackage{enumitem}
\setlist{nosep,leftmargin=*}

