\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}

% ---------- Page Geometry ----------
\usepackage[
  a4paper,
  top=2.8cm,
  bottom=2.8cm,
  left=3.0cm,
  right=3.0cm,
  headheight=15pt,
  headsep=8mm,
  footskip=12mm
]{geometry}

% ---------- Fonts & Math ----------
\usepackage{tgpagella}
\usepackage{newpxmath}
\let\Bbbk\relax
\let\openbox\relax
\let\Bbbk\relax
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{microtype}

% ---------- Colors ----------
\usepackage[table]{xcolor}
\definecolor{Burgundy}{HTML}{7A1C2E}
\definecolor{SlateGrey}{HTML}{4A5568}
\definecolor{AmberGold}{HTML}{D97706}
\definecolor{WarmBg}{HTML}{FAF9F6}
\definecolor{BorderGrey}{HTML}{E2E8F0}

% ---------- Running Headers ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection\ #1}}
\fancyhead[L]{\small\scshape\color{Burgundy}\leftmark}
\fancyhead[R]{\small\itshape\color{SlateGrey}\rightmark}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{BorderGrey}\leaders\hrule height\headrulewidth\hfill}}

% ---------- Section Headings ----------
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\Huge\bfseries\color{Burgundy}}
  {\filleft\small\scshape\color{AmberGold}\chaptertitlename\ \Roman{chapter}}
  {10pt}
  {\filleft\Huge}
  [\vspace{10pt}{\color{AmberGold}\hrule height 1.5pt}]
\titlespacing*{\chapter}{0pt}{40pt}{30pt}

\titleformat{\section}
  {\normalfont\Large\bfseries\color{Burgundy}}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{18pt}{8pt}

\titleformat{\subsection}
  {\normalfont\large\bfseries\color{SlateGrey}}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{14pt}{6pt}

% ---------- Tables ----------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\renewcommand{\arraystretch}{1.3}

% ---------- Captions ----------
\usepackage{caption}
\captionsetup{font={small,color=SlateGrey},labelfont={bf,color=Burgundy},labelsep=period}

% ---------- Bibliography ----------
\usepackage[backend=biber,style=numeric,sorting=none]{biblatex}
\addbibresource{references.bib}

% ---------- TikZ ----------
\usepackage{tikz}
\usetikzlibrary{positioning,calc,shapes.geometric,backgrounds,patterns,arrows.meta}

% ---------- Custom Callouts ----------
\usepackage[many]{tcolorbox}
\newtcolorbox{questionbox}[2][]{
  enhanced,
  breakable,
  colback=WarmBg,
  colframe=Burgundy,
  coltitle=Burgundy,
  fonttitle=\bfseries\sffamily,
  title={Question: #2},
  attach title to upper,
  after title={\par\medskip},
  arc=3pt,
  boxrule=1.2pt,
  left=12pt,
  right=12pt,
  top=10pt,
  bottom=10pt,
  before skip=14pt,
  after skip=14pt,
  #1
}

\newtcolorbox{dossierbox}[1][]{
  enhanced,
  breakable,
  colback=WarmBg,
  colframe=SlateGrey,
  arc=3pt,
  boxrule=0.8pt,
  left=10pt,
  right=10pt,
  top=10pt,
  bottom=10pt,
  #1
}

% ---------- List Spacing ----------
\usepackage{enumitem}
\setlist{nosep,leftmargin=*}

% ---------- FontAwesome ----------
\usepackage{fontawesome5}

% ---------- Custom Commands ----------
\newcommand{\sys}[1]{\textbf{#1}}

% ---------- Hyperref ----------
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=Burgundy,
  citecolor=Burgundy,
  urlcolor=AmberGold,
  pdftitle={PhD Viva Voce Preparation Dossier},
  pdfauthor={Alexander Mercer}
}