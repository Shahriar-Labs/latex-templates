% Packages, colours and custom commands for this template.
% ============================================================
%  Product User Manual — SoundWave Pro X1 Wireless Earbuds
%  Editable and compilable online at https://letx.app
%  Compile: latexmk -pdf main.tex
% ============================================================

% ── Packages ─────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lato}
\renewcommand{\familydefault}{\sfdefault}

\usepackage[
  top=22mm, bottom=24mm,
  left=22mm, right=22mm,
  includeheadfoot
]{geometry}

\usepackage{xcolor}
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable, listings}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{graphicx}
\usepackage{fontawesome5}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{fancyhdr}
\usepackage{tikz}
\usepackage{textcomp}
\usepackage{parskip}
\usepackage{multicol}

% ── Suppress parskip glue in tables ──────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{4pt}

% ── Colours ──────────────────────────────────────────────────
\definecolor{accent}{HTML}{1A6FA8}      % restrained steel blue
\definecolor{accentlt}{HTML}{E8F3FB}    % very light tint for fills
\definecolor{neutral}{HTML}{4A4A4A}
\definecolor{rulecolor}{HTML}{CCCCCC}
\definecolor{warnyellow}{HTML}{FFF4CC}
\definecolor{warnborder}{HTML}{D4A017}
\definecolor{dangerred}{HTML}{FDE8E8}
\definecolor{dangerborder}{HTML}{C0392B}
\definecolor{tipgreen}{HTML}{E8F5EC}
\definecolor{tipborder}{HTML}{27AE60}
\definecolor{coverbg}{HTML}{0E2A42}

% ── Hyperref setup ───────────────────────────────────────────
\hypersetup{
  colorlinks=true,
  linkcolor=accent,
  urlcolor=accent,
  pdftitle={SoundWave Pro X1 — User Guide},
  pdfauthor={SoundWave Audio Inc.},
}

% Section heading wrapper (titlesec-safe: no #1 in nested macros)
\newcommand{\sectionrule}{%
  \noindent\textcolor{rulecolor}{\rule{\linewidth}{0.4pt}}%
}
\newcommand{\sectionbg}[1]{%
  \colorbox{accentlt}{\makebox[\dimexpr\linewidth-2\fboxsep]{\strut\hfill\mbox{#1}\hfill}}%
}

\titleformat{\section}[block]
  {\large\bfseries\color{accent}}
  {}{0pt}
  {\sectionbg}
  [\vspace{2pt}\sectionrule]

\titlespacing*{\section}{0pt}{14pt}{6pt}

\titleformat{\subsection}[block]
  {\normalsize\bfseries\color{neutral}}
  {}{0pt}
  {}

\titlespacing*{\subsection}{0pt}{8pt}{2pt}

% ── tcolorbox environments ────────────────────────────────────
\newtcolorbox{warningbox}[1][]{
  enhanced,
  colback=warnyellow,
  colframe=warnborder,
  leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
  arc=2pt,
  fontupper=\small,
  title={\faExclamationTriangle\enspace\textbf{Warning}},
  coltitle=warnborder,
  attach boxed title to top left={yshift=-2mm, xshift=4mm},
  boxed title style={colback=warnyellow, colframe=warnborder, sharp corners},
  breakable,
  #1
}

\newtcolorbox{dangerbox}[1][]{
  enhanced,
  colback=dangerred,
  colframe=dangerborder,
  leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
  arc=2pt,
  fontupper=\small,
  title={\faBan\enspace\textbf{Danger}},
  coltitle=dangerborder,
  attach boxed title to top left={yshift=-2mm, xshift=4mm},
  boxed title style={colback=dangerred, colframe=dangerborder, sharp corners},
  breakable,
  #1
}

\newtcolorbox{tipbox}[1][]{
  enhanced,
  colback=tipgreen,
  colframe=tipborder,
  leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
  arc=2pt,
  fontupper=\small,
  title={\faLightbulb\enspace\textbf{Tip}},
  coltitle=tipborder,
  attach boxed title to top left={yshift=-2mm, xshift=4mm},
  boxed title style={colback=tipgreen, colframe=tipborder, sharp corners},
  breakable,
  #1
}

\newtcolorbox{infobox}[1][]{
  enhanced,
  colback=accentlt,
  colframe=accent,
  leftrule=4pt, toprule=0.5pt, bottomrule=0.5pt, rightrule=0.5pt,
  arc=2pt,
  fontupper=\small,
  title={\faInfoCircle\enspace\textbf{Note}},
  coltitle=accent,
  attach boxed title to top left={yshift=-2mm, xshift=4mm},
  boxed title style={colback=accentlt, colframe=accent, sharp corners},
  breakable,
  #1
}

% ── Numbered step list ────────────────────────────────────────
\newlist{steps}{enumerate}{1}
\setlist[steps]{
  label=\textcolor{white}{\textbf{\arabic*.}},
  leftmargin=2.2em,
  labelsep=0.5em,
  itemsep=3pt,
  parsep=0pt,
  before=\setlength{\parskip}{0pt},
}
% Patch label background
\setlist[steps]{
  label=\protect\colorbox{accent}{\color{white}\scriptsize\bfseries\,\arabic*\,},
  leftmargin=2.6em,
  labelsep=0.6em,
}

% ── Header / Footer ──────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}
\fancyhead[L]{\small\color{neutral}\textbf{SoundWave Pro X1}}
\fancyhead[R]{\small\color{neutral}User Guide \textperiodcentered{} v2.1}
\fancyfoot[C]{\small\color{neutral}\thepage}
\fancyfoot[R]{\small\color{neutral}\faGlobeAmericas\enspace soundwaveaudio.com}

% ── Spec/troubleshoot table helper ───────────────────────────
\newcolumntype{K}{>{\color{accent}\bfseries}l}

% ============================================================
